# CLAUDE.md — buscosun: Projekt-Verfassung für Claude-Code-Agenten

> Stand: 2026-09-03 (Statusblock: **SAT2h — V-SAT-18: das Komposit einer Kachel** `audit/brandradar-satellitenbilder.md` §12.10 (Jans „setze V-SAT-18 um“): der V-Eintrag skizzierte einen **Worker** für `dnbrTileRgba` + `createImageBitmap` — die Messung hat beide Hälften der Skizze widerlegt. **`createImageBitmap` kostet 1,1 ms**, nicht die halbe Task; und die 28 ms der Schleife sind nicht Rechnung, sondern **Verpackung**: `boaOf`/`nbrOf` geben `number | null` zurück (viermal je Pixel ⇒ V8 boxt jeden Double) und `const [r,g,bl,al] = dnbrRgba(…)` läuft je Pixel über das **Iterator-Protokoll**. Drei Prüfstände tragen den Befund: `dnbr-composite-bench.mjs` (Bausteine einzeln — `decodeTileU16` JS-Anteil 1,76 ms, Komposit 28,5 ms, Allokation 0,01 ms), `dnbr-loop-bisect.mjs` (Bisektion mit der alten Schleife als **Gleichheits-Orakel**: die null-Union ist der Haupttäter mit 4,5–8,1×, die Destrukturierung 1,0–1,5×) und eine Browser-Sonde, die die Schleife zwischen der `Uint8ClampedArray(1048576)`-Allokation und dem `createImageBitmap`-Aufruf klammert (**21–38 ms je 512²-Kachel**, davon `createImageBitmap` 1,1). **Gebaut wurde daher kein Worker, sondern die billige Schleife** — alles in `burnIndex.ts`, `FireCogViewer.tsx`/`worldCover.ts`/`cogTiff.ts` unberührt: NBR beider Szenen inline mit **`NaN` statt `null`**, Klassensuche über `CLS_MIN/R/G/B/A` (typisierte Spalten, `…from(DNBR_CLASSES, …)` — **abgeleitet**, nie danebengeschrieben), dieselbe Kur für `swirTileRgba` (E3 — `swirChannel` hat denselben Defekt, 26,1 ms). Die **Fachregeln bleiben stehen und werden weiter aufgerufen** (`sclPreMasked`/`sclPostMasked`/`sclPostUnsure`/`wcDamped`); `boaOf`/`nbrOf`/`dnbrRgba`/`swirChannel` bleiben exportiert als lesbare Quelle für Legende und Selbstverifikation (Muster `wcTileName` in SAT2f). **Eine Feinheit ist tödlich, wenn man sie übersieht:** die Abfrage lautet `!(d >= min0)`, nicht `d < min0` — bei `NaN` ist jeder Vergleich falsch, ein nodata-Pixel bekäme sonst die **oberste** Klasse und der Szenenrand würde dunkelviolett. **Der Beleg ist die Gleichheit:** der Verifier führt die lesbare Regelfassung als Referenzschleife mit und vergleicht Byte für Byte über eine 64²-Kachel, die JEDEN Sonderfall deterministisch enthält (nodata je Band einzeln, beide Bänder geklemmt ⇒ Summe 0, exakte Klassenkanten, jede SCL-Klasse 0…11, jede WorldCover-Klasse) — in allen vier Aufrufformen plus SWIR mit beiden Skalen; `dnbr-loop-bisect.mjs` prüft denselben Vertrag an 512². **Negativ-Kontrolle:** `!(d >= min0)` probeweise durch `d < min0` ersetzt ⇒ **8 Sonden fallen** (288/296), danach byte-genau zurückgestellt — ein Gleichheitstest, der immer besteht, beweist nichts. **Gemessen (Node, ein Isolat je Variante, identische Last): 35,0/43,6/53,9/54,5 → 10,3/13,7/9,5/15,6 ms (3,2–5,7×)**; **Browser (Prod-Preview, Hürtgenwald 16.08., Kaltstart je Lauf): dNBR-Schleife Median 27,6 → 11,7 bzw. 15,8 ms, SWIR 10,0–13,0 ms, `createImageBitmap` unverändert 0,9–1,9 ms, größte Bildlücke 30–49 ms, Konsole 0.** Der Unterschied der Faktoren ist **kein Widerspruch, sondern das Einschwingen**: in einer Sitzung läuft die Schleife nur 6–12 Mal, die ersten ein bis zwei Kacheln liegen höher (25,3 · 22,8, danach 8,9–15,9) — der Prüfstand zeigt den eingeschwungenen Wert, der Browser den echten. `verify:fire-detail` **296/296** (+14), typecheck + Build + Budget grün (totalJs 1 089,1/1 109,8, +0,4 KB, KEINE Ratsche), Bild unverändert (`sat2h-dnbr-nach.png`: Narbe am Fadenkreuz, beide Chips, Dämpfungssatz, **kein dunkelvioletter Szenenrand** — genau das wäre das Bild des NaN-Fehlers). **Damit ist es die zweite widerlegte „ab-in-den-Worker“-Skizze dieser Linie** (nach „ein Komposit je rAF“ in §12.9.6) und die dritte Wiederholung desselben Musters — SAT2f ersetzte Strings durch eine Ganzzahl, SAT2h eine Typ-Union durch `NaN`: beide Male lag der Aufwand nicht in der Fachrechnung, sondern in der **Form, in der Werte durch sie transportiert wurden**. Neu **V-SAT-19** (größter Rest ist jetzt die Sampler-Phase, 12 Sampler in Tasks von 51–65 ms — V-SAT-17 hat sie verteilt, nicht verbilligt; erst messen, ob `wcGeoGrid` oder `wcMapBlock` den Ausschlag gibt, eigenes Gate). **Drei Mess-Lehren:** ein Mikro-Prüfstand mit mehreren Varianten in EINEM Isolat misst **Verhältnisse, keine Absolutwerte** (dieselbe Referenzschleife kam auf 28/50/66/86 ms, je nach Nachbarschaft ⇒ neuer Modus `ONE=<Label>`, ein Isolat je Variante); ein Leistungsanker misst die **Maschine** mit (Chrome + Preview + Altlasten liefen mit — Vergleiche stehen deshalb in EINEM Lauf nebeneinander oder werden mit ihrer Last benannt); und **Byte-Gleichheit braucht eine Negativ-Kontrolle**. Falle am Rand: PowerShell `Set-Content -Encoding utf8` kodiert eine UTF-8-Datei ohne BOM **doppelt** — Wiederherstellung lief über eine vorher angelegte Byte-Kopie, nie über `git checkout` (der Baum ist uncommitted). Davor **SAT2g — V-SAT-17: der Kachelbau über Frames** `audit/brandradar-satellitenbilder.md` §12.9 (Jans „ja starte damit“ auf den Vorschlag, **erst die eine Zahl zu messen** und Frame-Budget gegen Worker an ihr zu entscheiden): **gemessen, dann gebaut.** Eine Sonde über die `Float64Array(262144)`-Allokation markiert jeden `wcGeoGrid`-Aufruf, ohne das Bündel anzufassen — Ergebnis: der eine Task von **237/245 ms enthält SECHS Sampler-Läufe und NULL Komposite**; der Kontrolllauf `?wc=0` baut dieselben sechs Kacheln, aber jedes Komposit sitzt in seinem **eigenen** ~50-ms-Task, weil die Netzankunft sie ohnehin auseinanderzieht. **Damit war die eigene Skizze aus §12.8.6 („ein Komposit je rAF“) widerlegt:** der Klumpen entsteht am **Start** der Bauten — `drawPyramid` (`FireCogViewer.tsx:549`) stößt bis zu **12** Kachelbauten in EINER `draw()` an, und die Sampler-Closure rechnet `wcGeoGrid` **synchron beim Aufruf**, während `wcMapBlock` hinter dem IFD-`await` als **Mikrotask am Ende desselben Tasks** abfläuft; belegt durch die Abstände der sechs `wcGeoGrid`-Aufrufe (**11,7·10,3·10,5·6,3·6,2 ms** — inline müssten dort ~37 ms stehen). Aufschlüsselung des Tasks: ~20 ms Draw-Vorlauf + 6×`wcGeoGrid` ~55 ms + 6×`wcMapBlock` ~170 ms (**~28 ms je 512²** — der Prüfstand sagt 102 ms je 1024², ein Viertel der Pixel ⇒ 25,5 ms: zwei Messwege, dieselbe Zahl). **Gebaut wurde daraufhin ein Frame-Budget in vier Zeilen, alles in `FireCogViewer.tsx`** (`worldCover.ts`/`burnIndex.ts`/`cogTiff.ts` unberührt): `WC_STARTS_PER_FRAME = 1`, `drawPyramid(…, maxStarts = 12)` mit dem **alten Wert als Vorgabe** (jeder Bestandsaufruf läuft wortgleich weiter), der Deckel **überspringt** einen Start und merkt ihn vor (`frame.deferred`, `continue` — nie `break`, es wird nichts gestrichen), `if (frame.deferred) schedule();` am Draw-Ende (rAF-koalesziert ⇒ genau ein weiterer Durchlauf, keine Schleife), und er greift **nur beim gedämpften dNBR** (`d.wc ? WC_STARTS_PER_FRAME : 12`) — ohne Dämpfung kostet ein Start praktisch nichts, sonst verlangsamte die Phase den Normalfall für einen Sonderfall. **Gemessen (Prod-Preview, Hürtgenwald 16.08., Kaltstart je Lauf): 1×237 ms mit 6 Samplern → 2–3 Tasks von 60/62/64/69 ms mit je 1 Sampler; größte Bildlücke 63,6 bzw. 83 ms statt ≥ 237 ms; letztes Kachelbild 3 224 statt ~3 290 ms nach dem Klick — das Bild ist NICHT später fertig** (der Bau ist netz-, nicht rechengebunden). `?wc=0` unverändert (0 WC-Anfragen, kein Dämpfungssatz), Dämpfungssatz + Chip stehen, Konsole 0 neu (die 20 Fehler sind Altbestand unter `vite preview`: `/_firms`, `/gwis`, `/effis`, `/wsapp`). `verify:fire-detail` **282/282** (+9), typecheck + Build + Budget grün (totalJs 1 088,7/1 109,8, +0,1 KB, KEINE Ratsche). **Gate-Frage 5 ist damit ✔ statt „teils“** — kein Task über 200 ms. **Gratis-Gewinn:** `sameView()` prüfte bisher nur vor dem *Platzieren* (`:556`), nicht vor dem *Bauen* — weil jedes Bild die Kachelliste aus der aktuellen Sicht neu ableitet, wird eine weggezoomte Kachel jetzt gar nicht erst gebaut (einzige Stelle der Phase, die Rechenzeit **spart** statt sie zu verteilen). Neu **V-SAT-18** (Komposit einer 512²-Kachel 50–85 ms — **mit und ohne** Dämpfung, größter verbliebener Einzelposten, Worker-Skizze, eigenes Gate). **Zwei Lehren:** ein Erfolg kann wie ein Messausfall aussehen (die `longtask`-Sonde schweigt unter 50 ms — erst ein rAF-Bildlücken-Zähler zeigt, dass die Seite durchatmet); und eine Textsonde, die eine **Argumentliste wörtlich** regext, bricht bei jedem angehängten Parameter (`[dnbr]` prüfte `true\);` und schlug fehl, obwohl die Aussage galt — sie prüft jetzt die Aussage). Davor **SAT2f — V-SAT-16: die Pixel-Schleife des WorldCover-Samplers ohne Strings** `audit/brandradar-satellitenbilder.md` §12.8 (Jans „setze das noch um"): der 1-Sekunden-Blocker aus SAT2e ist behoben — die Zuordnung „Ausgabe-Pixel → (WorldCover-Kachel, Pixel darin)" verglich Kacheln über ihren **Namen** und baute je 1024²-Kachel ~1 M Strings (`wcTileName`), ~1 M Template-Literal-Schlüssel, zwei Map-Suchen und **zweimal** `names.indexOf` je Pixel; jetzt trägt eine **Ganzzahl** dieselbe Information (`wcTileCode` = `((⌊lat/3⌋+30) << 7) + (⌊lon/3⌋+60)`, `wcNameFromCode` als Rückweg und EINZIGE Stelle, an der ein Name entsteht — `wcTileName` delegiert, es bleibt EINE Namensregel). Die Schleife ist als pures `wcMapBlock(grid, nPix, slots, onNeed)` herausgelöst (vorher steckte sie hinter `await` in einer Closure und war nicht prüfbar), Slot-Felder liegen als typisierte Spalten, der Merker „Kachel schon angefordert" ist ein flaches `Uint8Array` statt `Set<string>`, und die Felder der aktuellen Kachel bleiben in Registern (Nachladen nur beim Kachelwechsel — EIN Rumpf, keine Duplikation der Formel). **Der Beleg ist die Gleichheit, nicht die Geschwindigkeit:** `audit/brandradar-satellitenbilder/wc-sampler-bench.mjs` führt die Schleife von vorher wortgleich als `refMapBlock` mit und rechnet über acht Fälle (1 und 2 Kacheln × Ebenen 18000/9000 × Blöcke 256²/1024²) **jedes `tileOf[k]`, jedes `pixOf[k]` und den kompletten Kachel-Anforderungssatz samt Reihenfolge** nach — identisch, also ist die Dämpfung Pixel für Pixel unverändert (BW-1-Muster). Prüfstand **17–27× schneller** (1 813 → 102 ms je 1024²-Block); **Browser (Prod-Preview, Hürtgenwald 16.08., Kaltstart je Lauf): PC 1 028 → 209/227 ms, Spiegel 936 → 230 ms, ohne WorldCover 0 → 0 ms** (der `?wc=0`-Kontrolllauf baut dieselben 42 dNBR-Kacheln und erzeugt NULL Long Tasks ⇒ V-WF-13-konforme Zuordnung: gemessen wird WorldCover, nicht der dNBR-Bau und nicht die offstage rendernde Hauptkarte V-BD2-1). `verify:fire-detail` **273/273** (+12), typecheck + Build + Budget grün (totalJs 1 088,6/1 109,8, +2,8 KB lazy), Konsole 0/0. **Gate-Frage 5 bleibt ehrlich TEILS:** 209–230 ms liegen weiter knapp über 200 ms — der Rest ist nicht mehr die Namensschleife (die kostet noch ~100 ms je 1024²-Block), sondern Abruf + Deflate-Dekode + Komposit in EINEM Zug ⇒ **V-SAT-17** (Kachelbau über Frames deckeln; Darstellungsfrage, berührt `worldCover.ts` nicht, eigenes Gate). **Zwei geprüfte Irrwege stehen im Text, damit sie niemand wiederholt:** (1) die Verschmelzung von `wcGeoGrid` in die Zuordnungsschleife spart 16 MB Speicherverkehr je Kachel und ist trotzdem LANGSAMER (Node 135 → 133, Browser 209 → **236** ms) — gebaut, gemessen, zurückgebaut (§12.8.2 E6); (2) die Division durch `WC_PX_DEG` durch eine Multiplikation zu ersetzen ist wirkungslos (60,0 gegen 60,4 ms). **Prüfstands-Lehre:** der Bench-Fall „51°-Grenze (2 Kacheln)" schnitt die Grenze GAR NICHT — der Zwei-Kachel-Pfad war nie gemessen; aufgefallen ist es nur, weil die Verifier-Sonde die Kachelzahl FORDERT statt dem Etikett zu glauben (Bench druckt sie seitdem mit). Davor **RD3 — Radar als fertige Bilder im Daten-Repo** `audit/radar-datenrepo.md` §14 (Jans Auftrag „Niederschlagsradar + KONRAD als Bild in buscosun-data, AT/CH dazu, Client lädt direkt"; Plan-Freigabe + „fertig umsetzen"): der RD1-Spiegel DERIVIERT jetzt je Slot — RV/INCA/rzc als **Graustufen-PNGs mit exakt den `precipToU8`-Bytes** (byte-verlustfrei: alle 17 Verbraucher lesen ohnehin nur u8/vMax 20), KONRAD als **cells.json** (deep-equal zur Parser-Ausgabe, schema-versioniert) nach `radar/img/v1/`; Kindprozess `radar-derive.mjs` mit den CLIENT-Decodern (BW-1), Derive-Fehler nimmt nie den Roh-Push, `DERIVE=0`-Notaus; INCA über GeoSphere-`/metadata` (die reftime hinkt dem Slot 15–30 min hinterher — gemessen), rzc über STAC-ETag (Dateiname nicht berechenbar: Suffixe nl/tl/vl). **V-RD-12:** die Decoder kommen vom Zweig `rd3-producer` (origin/main + Derive-Module — main hinkt dem Arbeitsbaum hinterher, und ein Push auf main wäre ein Netlify-Deploy = Jans Gate); nach dem nächsten Deploy den Workflow auf main zurückstellen. Client: Bild-CDN zuerst mit Gate-Leiter (RV-Bild 270 s über Tar-Gate 240 s; KONRAD 330 s; rzc 240 s + Frische-Fenster; INCA deterministisch via metadata), alles-oder-nichts je Slot, 404 zählt nie in den Latch, Kill-Switch `?radarimg=0|1` (D-31; `?radarcdn=0` bleibt der äußere Schalter), Rückblick = 9 × f000.png statt 8 Voll-Tars, Frühstart wärmt meta+f000 statt des 1,4-MB-Tars. M0 gemessen: RV nass 662-KB-Tar → 790 KB PNGs (je ~32 KB) / trocken 137 → 114 KB, Derive 1,3–1,9 s; INCA 824 KB → 63,5 KB; rzc 33 KB → 1,4 KB; KONRAD 342-KB-XML → 55 KB JSON (12 KB gz); 26 frische CDN-Pfade nach EINEM Push in 0,6–6,9 s komplett sichtbar; Budget nass-extrem ≈ 119 < 150 MB. **In Produktion belegt** (Lauf 33736849695, derive:true; Prod-Preview): Regenradar Kassel „25 Frames (0…+120 min) · Quelle Daten-Repo (PNG)" mit 26 Bild-Anfragen und 0 Tar-Bytes, INCA/rzc-PNG-Zeilen, KONRAD cells.json 2,4 KB übertragen statt ~255-KB-XML + 79-KB-Listing, Gate-Leiter live beobachtet (Slot-Alter 4:24 ⇒ Tar-CDN), `?radarimg=0` ⇒ exakt RD2, Wetterkarte 0 Tar, Konsole 0 Fehler. Neu `verify:radar-repack` **30/30** (Byte-Identität aller 4 Quellen über den echten Kindprozess-Producer), radar-runs 52/52, routing 105/105 (Warm-Sonden auf den RD3-Vertrag), layer-erstbild 37/37, radar-sampling 25/25, typecheck/Build/Budget grün; `scripts/lib/bz2.mjs` aus dem ICON-Producer herausgelöst (Re-Export erhält den verify:repack-Vertrag). **Nachtrag 2026-09-03 nach Jans Deploy (§14.5–14.7):** V-RD-12 erledigt (Producer zieht wieder `main`, Zweig gelöscht, Lauf 33752414327 deriviert alle vier Quellen, DWD→Push 9–16 s unverändert), V-RD-16 belegt (ICON-Force-Push 10:07 UTC überlebt, je 12 Slots). **Drei Defekte gefunden und behoben:** (1) der GeoSphere-`/metadata`-Endpunkt flattert (gemessen 200/502/503) und hing im CDN-Fehlerpfad — ein FREMDhost-Fehler vergiftete den GETEILTEN Sitzungs-Latch und schaltete den Bildweg auch für RV/KONRAD/rzc ab; jetzt eigenes catch (zwei Hosts, zwei Fehlerkonten). (2) INCA ohne `/metadata` hatte gar keinen Bildversuch, also gerechnete Stempel (`guessIncaStamps`, Gate 28 min aus der Telemetrie reftime→Push 16,7/22,8 min) als Ausfall-Rückfall NACH dem Direktweg; **Drill: GeoSphere komplett blockiert, dennoch 12 Frames aus dem Daten-Repo, staleFromMs 31 min, Latch bleibt heil**. (3) **Altbestand CH:** MeteoSwiss listet ein rzc-Asset, BEVOR es abrufbar ist (jüngstes 403, drei ältere 200) — der Leser nahm nur das jüngste und warf, das CH-Radar fiel zeitweise ganz aus; jetzt drei Kandidaten + Spiegel-Rückfall. Dieselbe Annahme steckte in zwei Verifiern (die sich dadurch selbst übersprangen) — behoben, `verify:layer-erstbild` wieder 37/37 statt 32/37. **§14.7 Ladeweg auf ICON-D2-Niveau:** das Netz war nie das Problem (26 Dateien/1,1 MB in **1,4 s**), sondern die Dekodierung von 33 MPixel je Lauf **auf dem Hauptthread** (5,4 s) — der Tar-Weg läuft seit BW-5 im Worker. Gebaut: `src/sources/grayPng.ts` (Graustufen-PNG zu 1 Byte/Pixel ohne den RGBA-Umweg des Canvas, Canvas bleibt benannter Rückfall) + PNG-Dekodierung im vorhandenen `radolanWorker` (zweiter Auftrag `pngs`, Hauptthread-Rückfall wie bei `decodeRvTarOffMain`). **Belegt: „25 Frames (0…+120 min) · Quelle Daten-Repo (PNG)" bei Long Tasks 0, damit ist V-RD-14 geschlossen.** `verify:radar-repack` **41/41** (B1b/B1c Byte-Gleichheit des neuen Lesers über 25 echte Frames, A23–A31 Rückfälle/Gates/Latch-Isolation/Off-Main), alle übrigen Verifier grün, Build + Budget grün. Offen: V-RD-13 (rzc-Frische-Fenster), V-RD-15 (Rückblick-Browser-Beleg), V-RD-17 (metadata-Häufigkeit), **V-RD-18** (belastbarer Dekoder-Vergleich nur auf ruhiger Maschine — hier schwankten Läufe zwischen 27 s und 126 s), V-RD-4. Client-Änderungen seit dem Deploy erneut uncommitted. Davor **SAT2e — V-SAT-15: eigener jsDelivr-Spiegel als benannter WorldCover-Ersatzweg** `audit/brandradar-satellitenbilder.md` §12.7 (Jans „mache das"): Fallback-Ordnung **PC (10 m, unverändert) → Spiegel (37 m) → aus**; die 20-MB-Dateigrenze von jsDelivr erzwingt die 9000-px-Ebene (18 000 px = 22,9 MB drüber) und die 150-MB-Paketgrenze das EIGENE statische Repo **`jppetry/buscosun-worldcover`** (Daten-Repo trüge +116 MB nicht, dazu Zwei-Schreiber-Force-Push) — EIN unveränderlicher Commit, Client pinnt den SHA (`cc3ce559…`, lokal committet, nie `@main`, Sticky-404-Disziplin: kein Abruf vor dem Push). 17 DACH-Kacheln inkl. Selfkant/Genf/Neißeaue-Randfälle, **Summe 115,78 MB**, Kachel-Nutzlasten VERBATIM remuxt (`writeTiledTiff` aus dem Fixture-Writer herausgelöst, `wcRemux.mjs` pur, Producer `wc:mirror` mit harten Budget-Asserts + Byte-Gleichheits-Stichproben je Kachel); Client: `wcSource()` als EINE Wegwahl-Stelle (Latch 2 harte Fehler je Weg — 404 zählt nie, erster PC-Fehler fällt sofort durch), via-präfixierte Caches + `|wc`/`|wcm`-Kachelschlüssel, `?wcm=1|0` (Query schlägt Speicher), `?wc=0` unverändert, Satz sagt im Spiegel-Fall „37 m statt 10 m", eine console-Zeile; PC gesund ⇒ Abrufstrom byte-identisch. `verify:fire-detail` **261/261** (+24; zwei Alt-Sonden nachgezogen: `|wc`-Regex, `_tok = null`-Negativsonde vs `_resetWc`), typecheck + Build + Budget grün (totalJs 1 085,8/1 109,8, +0,7 KB lazy). **Gate GSAT2e gemessen (§12.7.4, Prod-Preview):** Repo angelegt + Commit gepusht (Origin-SHA == Client-Pin), CDN Range **206** auch im Browser-`fetch` (Magic `II/42`; `Range` ist safelisted ⇒ kein Preflight-Problem), `--check` CDN-Kachel byte-gleich zur AWS-Quelle; `?wcm=1` ⇒ 3 jsDelivr-Abrufe (299 KB), **0 PC-Anfragen**, Satz + Chip + EINE console-Zeile; **PC gesund ⇒ 0 Spiegel-Abrufe** (Rule 2); PC-Ausfall simuliert ⇒ Latch kippt, Dämpfung läuft über den Spiegel weiter; beide Wege tot ⇒ Dämpfung fällt, Overlay unversehrt, Ernte-Satz zurück; `?wc=0` ⇒ 0 WC-Anfragen jeder Art; Kaltstart zeigt Dämpfung und Spiegel-Satz im SELBEN Render; Konsole 0 neu (die 404 sind `/_firms/*`, unter `vite preview` erwartbar). **Neuer Befund V-SAT-16 (Bestandsfehler aus SAT2d, korrigiert dessen „max 52 ms"):** Long Tasks bei dNBR+Zoom **936 ms über den Spiegel, 1 028 ms über den PC, 0 ms ohne WorldCover** ⇒ die Last ist die Pixel-Schleife in `prepareWcSampler` (je 1024²-Kachel ~1 M `wcTileName`-Strings + `indexOf` + Template-Map-Schlüssel; nachgemessen 26 → 100 → 496 ms), NICHT der Transport — diese Phase verschlechtert nichts, hebt den Bestandsfehler aber; Fix-Skizze (Ganzzahl-Index statt Strings) steht in §12.7.6, bewusst nicht in dieser Phase gebaut. PowerShell-Falle erneut: `npm run … -- --probe` reicht Flags nicht durch (Vollbau lief statt Probe — folgenlos, gleiche Asserts). Davor **SAT2d — WorldCover-Landbedeckung dämpft die Ernte-Sprenkel** `audit/brandradar-satellitenbilder.md` §12 (Jans Go „setze das jetzt um: V-SAT-14"; in der GEMESSENEN Form): Quelle **ESA WorldCover 2021 v200** (10 m, CC BY 4.0) — die 3°-COGs sind EXAKT die Bauart des SAT2a-Lesers (uint8 · Deflate · Predictor 1 · 1024²-Kacheln · Pyramide, Kachel 94 MB, IFDs 18 828 B); **der ESA-AWS-Bucket ist CORS-los**, der Browser-Weg läuft über den **Microsoft Planetary Computer** (anonymer SAS-Token `ACAO *` ~1 h + Azure-Blob byte-identisch, Range 206 + Preflight-OK; Kachelname deterministisch, kein STAC, alle DACH-Landkacheln per HEAD belegt). **Zwei Sondenbefunde formten die Regel:** an der Zülpicher Börde tragen 13,4 % der Acker-Pixel ein falsches Signal ≥ 0,27 (bebaut nur 2,8 %), aber die Hürtgenwald-Narbe liegt teils auf Klasse 30 „Gras" (Heide) ⇒ gedämpft (HALBE Deckkraft, nie gelöscht — Getreidefeldbrände existieren) werden nur 40/50/60/70/80 (Acker/bebaut/vegetationsarm/Schnee/Wasser), Baum/Strauch/GRAS/Feucht bleiben voll; und die exakte UTM→Grad-Inverse kostet 51,5 ms je 256²-Kachel ⇒ 16er-Stützgitter + bilinear (≤ 0,5 m im Verifier). Bau additiv: `worldCover.ts` (neu; Token-Cache mit Ablauf-Wächter nie memoiert, Header-/Kachel-Caches entmemoieren Fehlversuche, `prepareWcSampler` mit Ebenenwahl je Anzeigeauflösung, Blockecken finden die 51°-Grenzkacheln), `burnIndex.ts` `wcDamped` + `dnbrTileRgba(…, wcCls?)` (SCL-unsicher ODER Landbedeckung halbieren zusammen EINMAL; ohne wcCls byte-gleich), Viewer entscheidet die Dämpfung VOR dem ersten Kachelbau (Schlüssel trägt `|wc`), jeder Abruf catch ⇒ null (Ausfall nimmt nur die Dämpfung), Satz nennt Quelle + Lizenz, Chip „blasser: Acker/Siedlung — Ernte statt Brand möglich", Kill-Switch `?wc=0`, Lizenzregister-Eintrag. **Belegt (Prod-Preview, Canvas-Diff derselben 5-km-Sicht):** 7,1 % der Bildpixel blasser, **Narbenkern 0/272 Pixel verändert**; Netz 1 Token + 7 Blob-Abrufe (6 Kacheln ≈ 0,5 MB, Zoom auf 1 km braucht KEINE weiteren), `?wc=0` ⇒ 0 WC-Anfragen + Ernte-Satz zurück, 0 Netlify-Bytes, Konsole 0/0 neu, Long Tasks max 52 ms. `verify:fire-detail` **237/237** (+25), typecheck + Build + Budget grün (totalJs 1 085,1/1 109,8, +2,1 KB im Lazy-Chunk, KEINE Ratsche). Neu V-SAT-15 (jsDelivr-Range-Spiegel als benannter Ersatzweg, nicht gebaut). Davor **SAT2c — SCL-Maske am dNBR-Overlay** `audit/brandradar-satellitenbilder.md` §11 (Jans Go „mache das als nächstes"; V-SAT-13 in der GEMESSENEN Form): das `scl`-Asset ist ein Leser-Zwilling der 20-m-Bänder (uint8 · Deflate · Predictor 2 · EXAKT das B8A/B12-Gitter, Vollkachel 5–12 KB, der vorhandene `decodeTile`-Pfad liest es unverändert). **Zwei Sondenbefunde haben den Auftragstext korrigiert:** (1) am Nachher-Bild 24.08. lag die GESAMTE sichtbare Narbe unter den SCL-Wolkenklassen 10/8 (Sen2Cor klassifiziert großflächig Zirrus, Goldwert 0,600 trotzdem gemessen) ⇒ eine binäre Nachher-Maske löscht das Signal — deshalb **asymmetrisch**: Vorher-Szene hart maskiert (vor dem Brand existiert keine Narbe — beweisbar sicher; Klassen 1/3/6/8/9/10/11), Nachher-Szene setzt Wolke/Schatten (3/8/9) auf HALBE Deckkraft, dünner Zirrus (10) und „dunkel" (2) laufen durch, hart nur die Nie-Narbe-Klassen 1/6/11; (2) die **„Landbedeckungs-Dämpfung" der Ernte-Sprenkel ist mit SCL WIDERLEGT** — SCL kennt keine Wald-Klasse (Juli-Felder heißen „Vegetation" wie der Wald) und die frische Brandfläche selbst wird am klaren Brandtag Klasse 5 „unbewachsen" wie ein Stoppelfeld ⇒ nicht gebaut, echte Landbedeckungsquelle = **V-SAT-14** (Jans Gate). Bau additiv: `burnIndex.ts` (`sclPreMasked`/`sclPostMasked`/`sclPostUnsure`, `dnbrTileRgba(…, preScl?, postScl?)` — ohne SCL byte-gleich, Verify-Gleichheitsprobe), `bands.scl` OPTIONAL, Viewer lädt SCL-Header/-Kacheln FEHLERTOLERANT (jeder Abruf catch ⇒ null — ein Ausfall nimmt nur die Maske, nie das Overlay), Satz + Legenden-Chip „blasser: Wolke/Schatten nachher möglich" nur bei aktiver Maske. **Belegt (Prod-Preview):** Narbe unverändert am Fadenkreuz (blassere Klasse-8-Flecken sichtbar), +2 SCL-Dateien ≈ 60–90 KB, 0 Netlify-Bytes, Long Tasks max 52 ms (Zoom bis Vollauflösung), Konsole 0/0, mobil 0 Überlauf/keine neuen Ziele. `verify:fire-detail` **212/212** (+13), typecheck + Build + Budget grün (totalJs 1 083,0/1 109,8, +0,4 KB, KEINE Ratsche). Davor **SAT2b — SWIR-Falschfarbe + dNBR-Overlay auf demselben Leser** `audit/brandradar-satellitenbilder.md` §10 (Jans Go „starte das"; V-SAT-9 umgesetzt): der 10-m-Viewer hat drei Modus-Chips **„Echtfarbe | SWIR | Verbrannt (dNBR)"** — `cogTiff.ts` additiv (`littleEndian` in der IFD, `decodeTileU16`: Predictor 2 wirkt bei 16 bit auf den WERTEN, `subTileU16` für B04s 512er- über B8A/B12s 256er-Gitter — Vielfache, kein Resampling), neues pures `burnIndex.ts` (BOA = max(0, DN·scale+offset) mit Skala JE SZENE aus `raster:bands` — seit Baseline 04.00 offset −0,1, Archiv älter 0; NBR/dNBR, `DNBR_CLASSES` als EINE Quelle für Canvas UND Legende, Goldwert 0,600 am gemessenen Narbenpixel), `fireSatImagery.ts` additiv (`S2CogRef.bands`+`square`, `fetchS2PreCogRef`: Vorher-Szene NUR desselben MGRS-Granulats ⇒ identisches Pixelgitter, 35 Tage Rückschau, **Anker = Brandbeginn `fireStartIso`, nicht der Betrachtungstag** — sonst war die „Vorher"-Szene der Brandtag selbst, §10.4 (1)), dNBR als Overlay ÜBER der Echtfarbe (nur gewählte Ebene — gestapelte Halbtransparenz verdoppelte die Deckkraft). **Zwei Befunde aus dem laufenden Bild:** ein latenter SAT2a-Fehler legte den Viewer lahm (Effekt-Aufräumung cancelte den rAF, setzte die ID nie auf 0 ⇒ `schedule()` zeichnete nie wieder, sobald die Aufräumung einen ausstehenden Draw traf — der neue Modus-Dep löste es aus); die frische Narbe ist im SWIR dunkel-oliv, NICHT „rot-orange" (SWIR-Reflektanz ~0,07 — Satz korrigiert), und Ernte-Feldwechsel erzeugen dNBR-Sprenkel (steht im Satz: „belastbar ist die zusammenhängende Fläche am Brandort"). **Belegt (Prod-Preview, Hürtgenwald 24.08.):** dNBR „Vergleich 25.07. → 24.08." zeigt die Narbe als kompakte rot-violette Fläche exakt am Fadenkreuz; ganze Sitzung (3 Modi + 2-km-Zoom) = 3 STAC + 34 Range-206 auf 5 Dateien, 16,8 MB, 0 Netlify-Bytes; Long Tasks max **196 ms** (einmalig, 4-Kachel-Komposit ⇒ V-SAT-12 Real-Device); Konsole 0/0; mobil 9 Ziele 44 px, 0 Kollisionen (Chips stapeln vertikal); `?sat10=0` ⇒ SAT1 unverändert. `verify:fire-detail` **199/199** (+46 inkl. u16-Rundläufe LE/BE × Predictor 1/2 und burnIndex-Goldwert), typecheck + Build + Budget grün (totalJs 1 082,6/1 109,8, KEINE Ratsche; eagerJs 103,1; Viewer-Chunk 9,0 KB gz lazy). Neu V-SAT-12 (Real-Device-Task), V-SAT-13 (SCL-Wolkenmaske + Landbedeckungs-Dämpfung, Jans Gate). Davor **SAT2a — eigener COG-Leser + zoombarer 10-m-Viewer im Brand-Dossier** `audit/brandradar-satellitenbilder.md` §9 (Jans Plan-Freigabe; Darstellung: Canvas im 600-px-Rahmen + Vollbild-Knopf): die Sentinel-2-TCI-Originale (10 m, 347 MB je Szene) direkt vom CORS-offenen AWS-Bucket per Range-Requests — `src/fire/detail/cogTiff.ts` (handgeschriebener Tiled-TIFF-Leser, Muster `gribDecode.ts`: IFDs aus EINEM 16-KB-Range, `DecompressionStream('deflate')` + Predictor 2, `needMoreBytes` als iterativer Vertrag, alles Fremde = benannter `cog-unsupported`-Fehler), `sentinelGeo.ts` (WGS84-UTM, Goldwert CN Tower < 1 m; Zone IMMER aus `proj:epsg` — Granulate überlappen Zonen), `fireSatImagery.ts` additiv (`fetchS2CogRef`: zweite on-demand Tages-STAC-Suche, weil Earth Search Asset-Felder nicht projiziert; `pickGranule`: Fenster-Deckung schlägt Wolke; Kill-Switch `?sat10=0` injektierbar) + `FireCogViewer.tsx` (eigener Lazy-Chunk 5,9 KB gz, lädt erst mit Klick; Canvas 2D ⇒ kein STOPP-Fall; Ziehen/Rad `passive:false`/Pinch/44-px-Knöpfe, Kachel-Cache Deckel 24 + in-flight 4, Fadenkreuz + Maßstab + ehrliche Byte-Statuszeile, Vollbild als PORTAL an body — fixed bliebe sonst im overflow:hidden gefangen —, Esc-Kaskade, Glättung AUS oberhalb der Datenauflösung). Die Georeferenz kommt GRATIS aus STAC (`proj:transform`/`proj:epsg` — keine GeoTIFF-Geo-Tags); Landsat bleibt draußen (USGS-Bucket requester-pays, 30 m) mit Satz im Fuß. **Belegt:** Hürtgenwald 24.08. — Überblick 4 Kacheln/2,0 MB/20 m/px mit „volle 10 m hier ≈ 11 MB" VOR dem Zoom, 10-m-Sicht 8 Kacheln/9,4 MB mit klar aufgelöster Narbe; Netz exakt 1 Tages-STAC + 9 Range-206 auf EINER Datei, 0 Netlify-Bytes; `?sat10=0` ⇒ SAT1-Karte byte-gleich; mobil (echte 375-px-Emulation, §26.4-Zusicherung) alle 6 Ziele 44 px, 0 px Überlauf; **Long Tasks am Prod-Build: 0 im ganzen Viewer** ⇒ Worker-Eskalation entfällt gemessen (V-SAT-8 zu). `verify:fire-detail` **153/153** (+53 inkl. Fixture-Writer-Rundläufe byte-gleich LE/BE × Predictor 1/2), typecheck + Build + Budget grün (totalJs 1 079/1 109,8, KEINE Ratsche; eagerJs 103,1 unverändert). Neu V-SAT-9 (SAT2b SWIR/dNBR auf demselben Leser, Jans Gate), V-SAT-10 (Wisch-Vergleich), **V-SAT-11** (Historie-Dossier existiert mobil gar nicht — Altbestand BD2f, betrifft auch Wetterlage/Verlauf). Davor **SAT1 — Satellitenbilder „Vorher | Während | Nachher" im Brand-Dossier umgesetzt** `audit/brandradar-satellitenbilder.md` §7 (Jans Plan-Freigabe; Darstellung Umschalter + Tagesleiste, Einbau Live- UND Ereignis-Dossier): `src/fire/detail/fireSatImagery.ts` (pur, Muster `fireWeatherAtPoint.ts` — EINE STAC-Anfrage über `sentinel-2-l2a`+`landsat-c2-l2`, `classifyScenes` wählt die letzte/erste Szene ≤ 40 % Wolken statt „Tag davor", jede leere Phase mit benanntem Grund, Kill-Switch `?sat=0`) + `SatImageryBlock` (`FireSatImagery.tsx`, strukturelle Props, fetch→Blob damit **`Data-Present: false` ein benannter Zustand** ist, Blob-Sitzungs-Cache) in BEIDEN Dossiers hinter der Wetterlage (`.br-ds-satbild` volle Breite Reihe 3); Doku `docs/API.md` §8.8 (Abgrenzung zum Batch-Kontrakt §8.6a) + Lizenzregister. **Belegt:** Hürtgenwald-Ereignis 19 Aufnahmetage, Vorher = 10.08. Landsat 1 % / Während = 14.08. S2 0 % / Nachher 16.08. 40 % mit Wolken-Satz, Narben-Bild am 24.08.; aktiver Brand ⇒ Während/Nachher deaktiviert mit Latenz-Sätzen, Data-Present-Fall live (27.08. in STAC, Bilddienst noch nicht); 1 STAC + 1 Snapshot je Tag, **0 Netlify-Bytes**; `?sat=0` ⇒ Dossier unverändert; mobil min 44 px, kein Überlauf; Long Tasks beim Blättern max 74 ms (Dev). `verify:fire-detail` **99/99** (+32), fire-model 122/122, fire-time 114/114, fire-history 112/113 (Altbestand), typecheck + Build + Budget grün (keine Ratsche). Neu V-SAT-6 (Auto-Weiterrücken bei Data-Present false), V-SAT-7 (Landsat-Tage dünner als GIBS L30). **Nachtrag SAT1a (§7.1, Jans „warum so undeutlich?"):** der 600-px-Abruf tastete mit ~37 m/px ab, GIBS hält ~25 m/px vor ⇒ `SNAP_SCALE = 2` (1200 × 960 angefordert, 600 CSS-px angezeigt; 158 statt 50 KB, sichtbar schärfer + Retina), 100/100; die Restunschärfe ist die 30-m-Physik + Tagesdunst und steht als Satz an der Karte. Davor **SAT0 — Satellitenbilder vorher/nachher je Brand: Diagnose, kein Code** `audit/brandradar-satellitenbilder.md` (Jans Auftrag 2026-08-31): an den Endpunkten gemessen — **NASA GIBS WMTS** führt HLS (Sentinel-2+Landsat harmonisiert, 30 m, Echtfarbe) mit Tages-Zeitachse, CORS `*`, ohne Schlüssel (0 Netlify-Bytes); Kacheln existieren NUR an Aufnahmetagen (Hürtgenwald-Kachel: S30 7 + L30 4 Tage im August, DescribeDomains beantwortet die Ortsfrage NICHT); die **Worldview-Snapshot-API** liefert fertige Einzelbilder (~50 KB, Header `Data-Present`/`Acquisition-Time` CORS-exponiert — zwei `<img>` statt zweiter Karte); Szenenwahl über **Earth Search STAC** (anonym, CORS, `eo:cloud_cover`) — gemessen ist die WOLKE das Problem, nicht die Verfügbarkeit (Brandtag 0 %, Vorher-Kandidaten 73–90 %) ⇒ Algorithmus „letzte wolkenarme Szene vor `firstMs` / erste nach `lastMs`“ + Datums-Stepper; Augenschein: 319-ha-Narbe am 24.08. klar erkennbar, 4-ha-Ereignisse unter der 30-m-Sichtbarkeitsgrenze (Ehrlichkeitssatz Pflicht); 10 m via Copernicus-Browser-Deep-Link (K5) bzw. später AWS-COGs (K4, Range+CORS gemessen, bräuchte TIFF-Leser = D-06-Frage); verworfen: CDSE Sentinel Hub (Schlüssel im Client), EOX Cloudless (Jahresmosaik), VIIRS 375 m, Esri. Empfehlung **SAT1**: Dossier-Karte „Satellitenbild vorher/nachher“ (STAC + 2 Snapshots + Deep-Link, 0 Bytes/0 Deps/kein Schlüssel); Entscheidungen E1–E3 + V-SAT-1…5 = Jans Gate. Davor **ET — Terrain-Bühne + Wetter-Readout der Event-Fläche** `audit/event-terrain.md` (Jans Auftrag 2026-09-01 „Terrain-Ansicht der ausgewählten Fläche + mehrere wetterabhängige Informationen übersichtlich"; Diagnose ET0 §1–§8 + Umsetzung ET1–ET5 §9, Gate GET1): das Event-Ergebnis zeigt die Pflicht-Fläche jetzt als gekippte **Terrain-Bühne** (lazy `EventTerrainMap.tsx` nach dem RouteTerrainMap-Muster: Terrarium-DEM + `setTerrain(1,3)` + Schummerung; **BEARING fest 0**, weil die Messpunkte „Nordwest-Ecke" usw. heißen; liberty-Patch auf `style.load`, BD2e-Dreizeiler, Pfeile mit BEIDEN `'map'`-Alignments) mit den 5 Scan-Messpunkten als Score-Chips (schwächste Ecke nur bei nicht-uniformer Spanne markiert), Windpfeilen und tiefstem/höchstem Rasterpunkt. **Vier ET0-Befunde trugen den Bau:** der Zonen-Scan verwarf je Ecke ein bezahltes `DaySummary` (**V-ET-1** ⇒ ET2 behält es — je Punkt „Böen · gefühlt min · Regenspitze", 0 neue Abrufe, V-EZ-3-Abrufmuster mit Verifier-Wache unangetastet); die Pipeline rechnet Kaltluftsee/Hangexposition längst (`terrainPhysics.ts` ⇒ Caption-Korrektur **V-ET-3**); die Terrain-Technik lag fertig im Repo (kein Shader, kein STOPP & FRAGEN); und die Fläche war im Ergebnis unsichtbar, jenseits 28 h gab es GAR keine Karte (**V-ET-2/V-ET-4** geschlossen — Gelände + Sonne sind bewusst NICHT am Scan oder Wetter-Horizont gegated). **ET3** Gelände-Kennzahlen aus EINEM `sampleElevations`-Batch (`eventTerrain.ts` pur: ≤ 64-Punkte-Raster, Kacheldeckel 16; `enrichElevation` additiv `maxTiles`, Default byte-gleich) — „praktisch eben" ERKLÄRT den Uniform-Satz, statt Scheinunterschiede zu dekorieren; **ET4** je Phase Kompass + Sonnenhöhe + **Horizontverdeckung** (64 Azimute × 24 Stützen mit Erdkrümmungsabzug, EIN Batch je Zone am Scan-Anker; an der Igls-Zone belegt: „Hinter dem Grat ab 19:39 (Grat 1,4° hoch)" VOR dem astronomischen Untergang), Phasen-Klick stellt die Hillshade-Beleuchtung (`hillshade-illumination-anchor: 'map'` — Default `viewport` drehte sie mit der Kamera; Pflichtsatz „Beleuchtung, kein Schattenwurf"); **ET5** Wizard-Schritt 2 „Karte | Gelände" — die Zeichenkarte bleibt MONTIERT (CSS-hidden + `map.resize()` beim Zurückschalten, versteckte Container messen 0 × 0), Vorschau im `preview`-Modus ohne Wetter, Zeichnen gesperrt mit Satz. Zwei Befunde aus dem laufenden Bild: die Windpfeile lagen exakt UNTER den Chips (gleiche Koordinate ⇒ Chip-Offset −22 px), und die ZEICHENkarte startete mit ausgeklappter Attribution (BD2e nachgezogen). Gemessen: Konsole 0/0 (Dev + Prod-Preview, keine ref_length-Warnung), Long Tasks am Prod-Build max **152 ms** (Laden) / 73 ms (Phasen-Wechsel + Fit), mobil per ECHTER 375-px-Emulation (`resize_page` endet bei 500 px `innerWidth` — §26.4-Lehre erneut) alle neuen Ziele ≥ 44 px, kein Überlauf; Gebirgszone (Spanne 91–98, SO-Ecke 12°/gefühlt 6°) UND Flachlandzone (Uniform 81–83 + „praktisch eben" 29–43 m) belegt. `verify:event-zone` **102/102** (+61 inkl. synthetischer Wand 5,706° und Krümmungsterm −0,090°), typecheck grün, `npm run build` grün, Budget totalJs 1 072,0/1 109,8 (eagerJs unberührt 103,1; `EventTerrainMap` eigener Lazy-Chunk). Offen: **V-ET-5** (Altbestand: die eingebettete MapView im Ergebnis startet mit ausgeklappter Attribution), Real-Device-Vorbehalt Mobil-WebGL (E3). Davor **BD2 — Brand-Dossier in die Mitte** `audit/brandradar-detail-mitte.md` (Jans Auftrag, Vorlage `reference/brandradar-detail.dc.html` 1a; reine Darstellungsphase): die Mitte des Brandradars ist **umschaltbar — Topbar-Segment „Karte | Dossier"** (Zustand `stage`, Permalink `ds:1` nur wenn offen, alte Links byte-gleich); ein Klick auf einen Brand in Karte oder Registry öffnet das Dossier. Desktop: links die Registry (250 px, dieselbe `FireFootprintPanel`-Instanz mit Sortierung/Filter/Deckel), Mitte das Dossier (Kopf → Kennzahlen-4er-Raster + Zeilen → Verlauf → Wetterlage in Steel → Einordnung & Bestätigung mit Ursache-Kasten → Merkmale), rechts 300 px (Minikarte · Legende · „Nationale Skalen · nie umgerechnet"), unten das Zeit-Deck als Leiste; Tablet Registry als 64-px-Rail (44-px-Quadrate, „+N" = der ausgesprochene Deckel), Dossier zweispaltig mit 250-px-Kartenspalte; mobil eigene Seite mit Kartenstreifen, Segment 44 px, Bottom-Bar bleibt. **Drei Diagnose-Befunde trugen den Bau:** (B2) **keine zweite `FireMap`** — die wäre ein zweiter Datenpfad mit elf doppelt gebundenen Quellen; die Minikarte (`FireMiniMap.tsx`) ist eine leichte MapLibre-Instanz mit EINEM Feature aus dem vorhandenen `footprintFc` (oder dem Bbox-Kasten, und sagt es); (B3) **die Hauptkarte bleibt beim Bühnenwechsel montiert** (`.fire-center.is-offstage`, kein Remount, Kamera und Layer bleiben); (B1) SVG-`<text>` ohne `font-family` erbte die Browser-SVG-Schrift — der Verlauf trägt jetzt League Spartan ausdrücklich. **Funktionserhalt strukturell:** die Zeilen der Detailkarte sind **exportierte Bausteine** (`DetailKennzahlenRows` … `CauseText`, `RecordStats`), die Readout-Detailkarte UND `FireDossier.tsx` zusammensetzen — der Verifier prüft, dass beide alle nehmen. **Drei Befunde aus dem laufenden Bild (§4.1):** vier Worker-Warnungen kamen **nicht** aus der Minikarte, sondern aus der Hauptkarte, die durch den Fokus-Zoom erstmals Zoom-12-Kacheln lud (positron hat dieselben `ref_length`-Shield-Layer wie liberty ⇒ `patchLibertyRefLength` auf `style.load` in `FireMap`, danach 0 Warnungen); die Vorlage skaliert 600 SVG-Einheiten auf eine 380-px-Spalte und würde 12 px zu 6,5 px machen ⇒ `wideWidth` je Spalte (380/560/340), 12 px rendern als 12 px; dicht liegende Überflüge brauchen entzerrte Beschriftungen. **Gemessen (Playwright, Dev-Server):** Desktop Registry 250 · Dossier 828 · Seite 300, alle 19 Zeilen der Detailkarte im Dossier, kleinste Schrift 12 px, Tablet Rail 64 px + 50 × 44 px, mobil Bottom-Bar 4 × 44, Segment 44, kleinste Schrift 11 px, kein Überlauf; Konsole nur Altbestand (EMS-CORS, GeoSphere-404), **0 Warnungen**. `verify:fire-detail` **59/59** (+15), `verify:fire-model` **122/122** (+4), `verify:fire-time` 114/114, `verify:fire-history` 112/113 (Rest `spreadFc` = Altbestand), typecheck grün, `npm run build` grün, Budget totalJs **1 056,9/1 109,8** KB (Ratsche von 1 053,1; eagerJs unverändert 102,7). **Nachtrag BD2a (2026-08-31, §5):** jeder Brand-Klick auf der KARTE öffnet das Dossier — vorher galt das nur für den Brandflächen-Klick, und der Layer ist standardmäßig aus, sodass der Normalfall die Cluster-Hülle traf und nur markierte; jetzt löst der Hüllen-Klick den Brand auf und öffnet ihn, beide Karten-Rückrufe melden per boolean „Dossier geöffnet" und `FireMap` bricht dann die Popup-Kette ab (sonst stünde das Popup auf der versteckten Karte), die Gleichheits-Wächter entfallen (der wiederholte Klick nach „Bühne zurück" zählt), **BD2b/BD2c (§5.1, Jans Nachfassen „öffnet sich nur ein kleines Panel"):** die Hülle ist am Übersichts-Zoom oft nur wenige Pixel groß (hitTest am Schwerpunkt leer, gemessen) — was Nutzer treffen, ist der DETEKTIONSPUNKT; jetzt öffnen ALLE Wege das Dossier: Punkt (`openDossierForDetection`, Bbox-Auflösung, kleinste gewinnt), Hülle, Brandfläche, EFFIS-Fläche (`openDossierForEffis`), Raster (`openDossierForZone`) und auch Anlagen-Einträge (site, Reiter bleibt Thermalanomalien); der Steckbrief (Popup) bleibt nur, wo kein Registry-Brand dahintersteht. Am synthetischen Klick belegt: die ersten drei Detektionspunkte (ortsfest/Anlage/aktiv) öffnen alle das Dossier, 0 Popups, `ds:1`; `verify:fire-detail` **65/65** (+6). **BD2d (§5.2, Jans Auftrag):** die Sidebars bleiben in JEDER Bühne unverändert — Layer-Dock links, Readout rechts (Registry + Inline-Detailkarte), nur die Mitte wechselt Karte ⇄ Dossier; Minikarte/Legende/Skalen in der zweiten Dossier-Spalte, Zeit-Deck als Leiste im Zentrum; Dossier-Registry, Tablet-Rail und Seitenpanel sind zurückgebaut, Chart-`wideWidth` 360/420/340 für die schmalere Mitte (Tablet EINE Spalte); gemessen Desktop Dock 250 · Zentrum 648 · Readout 480, Tablet 214/456/300, mobil unverändert, `verify:fire-detail` 65/65. **BD2e (§5.3, Jans „Probleme beim Klicken"):** MapLibre öffnet die kompakte Attribution beim Start AUSGEKLAPPT — bei 648 px Kartenbreite ein 612 × 62-px-Block über der unteren Kartenhälfte, der Brand-Klicks schluckte (mit echten `page.mouse`-Klicks reproduziert: 3/5 taten nichts, `map.on('click')` feuerte nie; die synthetischen dispatchEvent-Klicks der BD2a/c-Belege gingen an der DOM-Überdeckung vorbei — Mess-Lehre); beide Karten klappen sie nach `load` zu (ⓘ bleibt, V-BD2-2 damit erledigt), danach 5/5 echte Klicks ⇒ Dossier; `verify:fire-detail` **66/66**. **BD2f (§5.4, Jans „funktioniert nicht bei 30 Tagen/Saison"):** das war der Historie-Modus (BH3) — `selectHistory` markierte nur die Zeile; jetzt öffnet der Ereignis-Klick (Karte + Liste) das **Ereignis-Dossier** in der Mitte: `HistoryDetailBody` herausgelöst (EINE Quelle für Readout-Inline und Mitte — Index-Zeilen + BH4-Wetterlage/Landbedeckung/Evidenz/Merkmale), Minikarte nimmt `MiniMapTarget` (strukturell) statt `FireRecord`, Kopf mit EFFIS-/Anlagen-/NRT-Abzeichen, Leerzustand benannt, Hash trägt `bh`+`ds`; mit echten Klicks belegt (Monat, 1 231 Punkte: Dossier mit Wetterlage am Brandtag, Wiederholung nach „Bühne zurück" mit frischer Projektion — die Kamera steht nach der Auswahl auf dem Ereignis); `verify:fire-detail` **67/67**. **BD2g (§5.5, Jans „Informationsgehalt gering, Formatierung stimmt nicht"):** die `.br-detail-dl`-Styles hingen an `.br-history` (im Dossier ungestylte Blockzeilen), und die Mitte ließ die Shard-Detektionen liegen — jetzt `HistoryDossierBody` (geteilter Hook mit der Inline-Karte): vier Kennzahl-Kacheln (`historyStatTiles`), **FRP-Verlauf aus den Shard-Detektionen** über `groupPasses` (nowMs = letzte Detektion, kein wochenlanger Nachlauf-Schraffur; Einzelüberflug wird gesagt), Wetterlage als Steel-Kacheln, Einordnung & Evidenz inkl. Landbedeckung, Merkmale als Tabelle; belegt an „Hürtgenwald (Düren)" (10 Balken, 3☀/7☾, 92 % Nadelwald, keine Schrift < 12 px, `bd2g-history-dossier.png`); MW-Label-Wächter gegen die Y-Achse; `verify:fire-detail` **67/67**. Offen: **V-BD2-1** (Hauptkarte rendert offstage weiter), Long Tasks am Prod-Build. Davor **RD2 — der Client liest Radar vom Daten-Repo-CDN** `audit/radar-datenrepo.md` §12 (Jans Auftrag 2026-08-31 „KONRAD- und Radar-Daten von GitHub statt DWD, alles wie vorher, +2 h bleiben"): EINE Wegwahl-Stelle in `radolanRuns.ts` — CDN mit **Zeit-Gate** (RV Slot + 240 s, KONRAD + 330 s; zu frühe Anfragen hielte jsDelivr 62–118 s als 404 fest), Fenster 55 min (< Spiegel-Retention, Rückblick passt durch), jünger als das Gate ⇒ Netlify mit dem aggressiven Rat wie bisher (Frische UNVERÄNDERT — der Tar ist byte-identisch, alle 25 Frames 0…+120 min), KONRAD über gerechnete Zeitstempel statt 78,5-KB-Listing (**H12/V-RD-3 umgesetzt**), 8-s-CDN-Frist, Sitzungs-Latch (2 harte Fehler), Kill-Switch `?radarcdn=0`/`localStorage.radarcdn` (Query schlägt Speicher); ALLE RV-Verbraucher hängen an `fetchRvTar` ⇒ auch Wetterkarte, DE-Punktvorhersage (V-RD-2), Rückblick, Hindcast lesen vom CDN; Konsolenzeile nennt die Quelle. `verify:radar-runs` **52/52** (+30 inkl. 2 Live-CDN-Zeilen), `verify:routing` 105/105, `verify:layer-erstbild` 37/37 (2 Sonden auf den neuen Vertrag), radar-sampling 25/25, typecheck grün, Budget 1 058,2/1 109,8; **im Browser belegt** (Prod-Preview): RV 1 725 KB vom CDN + „Quelle Daten-Repo (jsDelivr)", KONRAD 37 KB ohne Listing, Cache-API-Reuse, `?radarcdn=0` ⇒ Netlify samt frischerem Lauf, Konsole 0/0 ⇒ **Netlify-Radar-Posten DE im Normalbetrieb ≈ 0**. Davor **RD1 — Radar-Spiegel im Daten-Repo IN PRODUKTION** `audit/radar-datenrepo.md` §11 (Jans Entscheidung nach dem Messlauf §10: „die Daten werden richtig schnell und gut bereitgestellt — merge mit main, Task immer wieder neu starten"; ToS-Risiko R5 bewusst getragen): `buscosun-data` `main` trägt jetzt `scripts/radar-mirror.mjs` + `.github/workflows/radar.yml` (Quelle `scripts/radar-mirror/` hier) — EIN Job spiegelt ≈ 5 h 45 lang RADOLAN-RV + KONRAD3D per HEAD-Polling (10 s) nach `radar/{rv,konrad3d}/` (Retention 12 = 1 h, `radar/status.json` als Telemetrie), stündlicher Cron `:07` + `concurrency: radar-mirror` stellen den Nachfolger in die Warteschlange, Selbst-Dispatch als Reserve; **jeder Push setzt auf `origin/main` neu auf und kopiert den lokalen Bestand ein** (zwei Schreiber: der ICON-Publisher force-pusht alle 3 h — lokal mit simuliertem Force-Push belegt: kein verlorener Slot, kein abgelehnter Push; Nebeneffekt: die Radar-Historie wird alle 3 h auf einen Wurzel-Commit zurückgesetzt, das Repo wächst nicht). Erster Lauf 16:08:22 UTC (`runs/33262140908`), Erststart holt 11 Slots nach, CDN `@main/radar/rv/…` 200 **4 s nach dem Push**; Messlauf §10: DWD→Push 7–16 s. **Client noch unverändert** (liest weiter `/_dwd_opendata`) ⇒ nächste Phase **RD2**: `radolanRuns.ts`/`dwdKonrad3d.ts` auf CDN zuerst + Netlify-Fallback + konservativer Slot + Kill-Switch. **Nach 31 h Betrieb belegt (30.08. 23:10 UTC):** ununterbrochen, Slots lückenlos, DWD→Push med **11 s** (max 17); Nähte nahtlos (die vielen „cancelled" sind ERSETZTE wartende Läufe — Normalfall der concurrency-Warteschlange, kein Fehler); §10.3 ausgewertet: ohne frühe Anfrage steht ein Slot **≈ 12 s nach DWD** auf dem CDN (jsDelivr neuer Pfad 1–4 s), eine zu frühe Anfrage macht den Pfad 62–118 s unsichtbar ⇒ RD2-Regel: Client fragt frühestens **Slot + 3:40** (zeitgesteuertes Gate statt „ein Slot zurück"); Zweig `radar-test` gelöscht, Log in `audit/radar-datenrepo/`. **§11.4 (31.08.):** Jans „die Ablage wurde gestoppt" war eine FEHLANZEIGE der Warteschlangen-Konstruktion — Daten lückenlos (00:57 UTC: status.json 0 min alt, DWD→Push 13 s), aber der stündliche Cron erzeugte je Tick einen „completed cancelled"-Lauf samt Abbruch-Mail; Umbau `0e363fc`: `radar.yml` ohne Cron (Kette = Selbst-Dispatch), neu **`radar-watchdog.yml`** (stündlich, prüft aktiv/wartend, startet nur bei Stillstand — keine cancelled-Läufe mehr). Offen: V-RD-4 beobachten (GitHub `size` 49 → **98 MiB** in 31 h, GC-Kadenz unbekannt). Davor **RD0 — Radar/KONRAD ins Daten-Repo? Diagnose, kein Code** `audit/radar-datenrepo.md` (Jans Frage vom 2026-08-29): RV-Tar heute Median **1,36 MB** (BW-5 maß einen trockenen Tag), nur DE betroffen; der Weg scheitert am **Takt** — Actions-Jitter am eigenen Daten-Repo-Batch gemessen **p50 +17 min, 12 % ≤ 5 min** —, an der Frische (3,3 → 6–25 min, jsDelivr hält 404 fest), am **Zwei-Schreiber-Rennen** mit dem Force-Push-Publisher und am GitHub-ToS-Satz „don't use Actions as a content delivery network"; **Nachmessung auf Jans Einwand (§8):** der ICON-Weg ist im Normalfall nur **+1 min** hinter dem DWD (17 von 29 Läufen, heute 12z +52 s), weil der Slot 47 min VOR den Daten liegt und der Producer wartet — aber 4 von 29 Läufen hatten 3 h keinen vollständigen Stand (GitHub ließ am 27./28.08. je ~6 von 16 Slots aus) und 4 weitere kamen erst nach +65…+85 min; Beifang **V-RD-7** (Wartebudget zählt ab Job-Start ⇒ pünktliche Starts +5/+6 brechen VOR DWD-Ende ab), **V-RD-8** (spätes Sicherheitsnetz wartet auf den NÄCHSTEN Lauf), **V-RD-9** (unvollständige Läufe werden nicht nachgeholt). **§9 (Jans Frage „dauerhaft laufende Action?"):** möglich als selbst-nachstartende Job-Kette (`workflow_dispatch` mit Standard-Token erzeugt immer einen Lauf, Job-Grenze 6 h, Runner 24/7 belegt) mit HEAD-Polling auf den erwarteten Pfad ⇒ Lauf ≈ 5–5,5 min nach Slot auf dem CDN, im Browser 6,5–11,5 min (heute 3,5–8,5) — ändert die Frische, nicht R3/R4/R5 (eigenes Repo, Retention ≤ 1 h, ToS „Actions as CDN"). **§10 RD-T1 Messlauf** (Jans Auftrag „einen Test machen"): Messgerät `scripts/radar-mirror-test/{mirror-test.mjs,workflow-radar-test.yml}` — spiegelt RV + KONRAD3D auf den **Orphan-Zweig `radar-test`** des Daten-Repos (nie `main`: R3) und stempelt DWD-Ablage · gesehen · Download · Push · CDN je Slot, plus frühe-Anfrage-Gegenprobe (sticky 404); Trockenlauf lokal: Datei 8–13 s nach DWD-Ablage gefunden; Zweig mit Jans Go gepusht (`d2a29ad`), Lauf = Jans Dispatch, Ergebnis in §10.3 nachzutragen. Empfehlung: **nicht bauen** (V-BW-29 bestätigt), stattdessen bei Bedarf **Weg B: Cloudflare Worker als CORS-Proxy** (0 Netlify-Bytes, Frische unverändert, 100 000 Anfragen/Tag frei — neues Konto = Jans Gate E2); V-RD-1…6, H12 (KONRAD-Listing) bleibt als Client-only-Posten. Davor **R3D-8 — das Ergebnis öffnet mit dem Gelände** `audit/route-3d.md` §26 (Jans Auftrag „die Geländekarte mit der flachen Karte am Anfang tauschen"; auf Rückfrage präzisiert: gemeint ist die Karte im **Ergebnis**, nicht der Schnitt): das 2D-Ergebnis zeigt jetzt das **Relief mit der Wetterlage auf der Strecke**, die **flache Karte bleibt als zweite Ansicht** (`Gelände | Karte`). Sie bleibt, weil sie **anderes kann** — Pausen-Marker, Wegpunkt-Vorschläge, Klick „Pause hier", Wetter-Marker mit Popup; sie zu ersetzen hieße, vier Funktionen zu verlieren, der Umschalter kostet einen Knopf. **Damit aus zwei Ansichten keine zwei Wahrheiten werden, wanderte die Logik an EINE Stelle:** `TEMP_STEPS`/`TEMP_COLORS`/`tempStepIndex`/`REL_COLORS` nach `model.ts` (eine Palette ist Daten, keine Komponente; `Scene3D` reicht sie weiter), und `TerrainLayerFlags`, `DEFAULT_TLAYERS`, `loadTLayers`/`saveTLayers`, **`terrainChips()`** (die Chip-Zeile als Daten) sowie **`buildGroundLayers()`** (Temperatur-, Warn- und Pfeilspuren) nach `routeSection.ts`; `Route3DView` exportiert `TerrainChipButton`, beide Ansichten nehmen denselben Knopf. Neu ist nur das **Layout** (`RouteTerrainPanel.tsx`) — ein Schalter, ein Speicherschlüssel, eine Liste: wer im Ergebnis die Temperaturschichten einschaltet, findet sie in der 3D-Ansicht wieder. Der Umschalter steht **über** der Karte (nicht darin: die flache hat feste Höhe mit `overflow: hidden`, die Gelände-Ansicht wächst mit), erscheint erst mit berechnetem Wetter, und die Wahl liegt im `localStorage` (`bsc.route.resultmap`, Standard `terrain`); die Gelände-Ansicht kommt **lazy**. **Zwei Befunde aus dem laufenden Bild (§26.3):** die Chip-Zeile schob die Karte unter den Falz (auf 1440 × 900 begann sie erst bei y ≈ 690) ⇒ im Ergebnis **erst die Karte, dann die Schalter**, in der 3D-Ansicht umgekehrt (dort steht die Bühne ohnehin oben) — der Verifier hält **beide** Reihenfolgen fest; und die liberty-Warnung (V-RL-3) kam zurück, diesmal aus der **flachen** Karte, die die geteilte Korrektur nie kannte ⇒ `RouteMap.tsx` ist jetzt der dritte Aufrufer von `src/map/libertyStyle.ts`, ebenfalls auf `style.load`. **Mess-Lehre (§26.4):** ein Werkzeug, das „erfolgreich" meldet, hat nicht unbedingt gewirkt — die erste Mobil-Messung fand 34- und 38-px-Ziele, weil `resize_page` zwar Erfolg meldete, `window.innerWidth` aber **1600** war und `matchMedia('(max-width: 767px)')` `false`; nach echtem Verkleinern 44 px. **Eine responsive Messung beginnt mit der Zusicherung `innerWidth`/`matchMedia`**, nicht mit dem Rückgabewert des Werkzeugs (dieselbe Klasse wie `getBBox()` ohne `transform`, §18.5). **Gemessen:** Umschalten in beide Richtungen samt Markern und Quellen-Overlay, Wahl überlebt den Wechsel, Long Tasks max **184 ms** (Karte auf-/abbauen), Konsole **eine Warnung weniger** als vorher, mobil kein Ziel < 44 px. `verify:route-3d` **564/564** (+11), `verify:routing` 104/104, typecheck grün, Budget totalJs 1 053,0/**1 053,1** KB. Offen: **V-R3D-16** (Umschalten baut jedes Mal eine MapLibre-Instanz ab und auf — beide zu halten wäre schneller und teurer), V-R3D-14, Real-Device, V-R3D-4, V-R3D-7…13, V-R3D-15. Davor **R3D-7 — Zeitplan: alle Wetteränderungen in Schriftform** `audit/route-3d.md` §24/§25 (Jans Auftrag „einen Zeitplan, in dem man alle Wetterereignisse oder Änderungen entlang der Route in Schriftform übersichtlich sieht"): **Die Diagnose §24 hat zuerst geklärt, dass die dritte Form fehlte** — Perlen (1b) zeigen **Zustände** („wo bin ich um T"), Abschnitte (1c) eine **Bewertung** („wo hält mein Grenzwert nicht"), und für **Ereignisse** („wann ändert sich was") gab es nichts: drei Stunden im selben Regen ergaben in `buildTimeline` dreimal Regen (F1). Elf Ereignisarten sind aus der Kette ableitbar, **ohne einen neuen Abruf** (F2-Tabelle): Start/Ankunft, Regen beginnt/endet, Art wechselt, Windband wechselt, Wind dreht zur Fahrtrichtung, 5-°C-Marken, Gefühlt unter 0 °C, Warnung beginnt/endet, Weg steigt in die Wolkenbasis, Weg über die Schneefallgrenze, Grenzwert reißt/hält wieder. **Drei Regeln tragen die Liste:** (1) **Schwellen werden interpoliert** — km und Uhrzeit aus derselben Interpolation, damit beide denselben Punkt meinen (im Verifier nachgerechnet: 17 → 13 °C über 1 000 m legt die 15-°C-Marke exakt in die Mitte); Zustände übernehmen ihre schon gerechneten Kanten (`segmentEdges`) statt sie neu zu bestimmen; (2) **Bänder statt Zahlen, plus Hysterese** — `steppedBand()` wechselt erst 2 km/h (Wind) bzw. 0,5 K (Temperatur) hinter der Kante, ein Wert der um 45 km/h pendelt (44·46·44·46) erzeugt **keine** Zeile (F4); (3) **der Deckel wird ausgesprochen** — `omitted` sagt die Zahl, nie eine stille Kürzung (V-246). **Umgesetzt:** `route3d/schedule.ts` (pur) + `SchedulePanel` unter der Bühne, **an keinem Modus und an keiner Bühne** (es fasst zusammen, was alle drei zeigen), Klick auf eine Zeile setzt die Position, „Als Text kopieren" nach dem 1c-Muster (**kein Link**, B3), und unter der Liste stehen die strukturellen Lücken — keine Warnungen in AT/CH, keine Schneefallgrenze in DE, **kein Hagel** (§19.5) — genau dort, wo jemand danach suchen würde. **Im Browser gemessen (AT-Alpentour):** acht Zeilen von „15:04 km 0,0 Start · 25 °C, Böen 11 km/h" bis „01:44 km 8,6 Ankunft · 8 °C, Böen 27 km/h"; Klick auf eine Zeile springt auf **km 7,7 · 23:30** und markiert sie; „kopieren" liefert **1 075 Zeichen ohne `http`**; Long Tasks **0** beim Durchklicken aller Zeilen; Konsole ohne Fehler und Warnungen; mobil rückt km unter die Uhrzeit, kein Ziel < 44 px. **Beobachtung (§25.3):** zwei Zeilen zur selben Minute sind kein Fehler — am Gipfel frischt es genau bei der Ankunft auf; die Ordnungsregel `ORDER` stellt sicher, dass „Ankunft" dabei **zuletzt** steht, sonst läse sich der Plan, als passiere nach dem Ziel noch etwas. **Messgrenze:** das Druckbild ist nur im CSS belegt — `window.print()` öffnet einen Dialog und würde die Automatisierung blockieren. `verify:route-3d` **547/547** (+57), `verify:routing` 104/104, typecheck grün, Budget totalJs 1 050,8/**1 050,9** KB. Offen: **V-R3D-14** (der Plan steht nur in der 3D-Ansicht — ob er im 2D-Ergebnis neben den Pausenplan gehört oder mit ihm verschmilzt, ist eine Produktfrage), **V-R3D-15** (Ereignisse zwischen zwei Abtastpunkten fehlen strukturell), Real-Device, V-R3D-4, V-R3D-7…13. Davor **R3D-6 — die Gelände-Ansicht zeigt den Boden, nicht die Atmosphäre** `audit/route-3d.md` §23 (Jans Rückmeldung „jetzt zeigt es aber mehr die Atmosphäre als die Wetterlagen am Boden"): **Die Diagnose nennt drei Ursachen, die zusammenwirkten** — (1) die Wand war der **einzige** Träger: alles, was die Karte über das Wetter sagte, stand senkrecht ÜBER der Strecke, am Boden lag nur eine Linie ohne einen Wert; (2) sie reichte bis zur Schnitt-Decke (höchster Punkt + 1 200 m, über einer Alpentour gut 3,5 km), und weil `windAtAGL` bei `BOUNDARY_LAYER_M` = 1 500 m über Grund **sättigt**, war der obere Teil sogar aussagelos; (3) die Startschalter verstärkten es — an war die Windströmung in 1–3 km Höhe, aus waren die Bodenschichten. **Umgesetzt:** die **Strecke selbst trägt jetzt die Wetterlage am Boden** — Farbe = Temperatur (dieselben Stufen wie das Band im Schnitt), Pfeile auf dem Gelände = Wind zur Fahrtrichtung, blaue Spur = Regen, rote Spur = amtliche Warnung (Level 4/5 dunkler); neu und pur `routeSegments()` (je Spalte ein Stück, Grenzen in der **Mitte** zwischen zwei Abtastpunkten), `segmentCoords()` als die eine Stelle für alle Spuren und `windPicks()`. **Die Wand wird eine bodenfolgende Bahn:** `buildCurtain` bekommt `bandAglM`, die Oberkante liegt 300 m über dem Gelände **dieser** Spalte — unten der Bodenwert, oben, wie schnell er zunimmt. Das ist **keine Pipeline-Änderung** (Shader, Attribut-Layout, Textur-Format und Zeichenaufruf unberührt; `curtainMesh.ts` ist DOM- und WebGL-frei, und `v` wird nur an der echten Oberkante ausgewertet). Dazu 25-m-Höhenschritte statt 150 (die Bahn hätte sonst zwei Stützstellen statt zwölf), eine Textur-Decke bei „höchster Punkt + 400 m", und eine Chip-Zeile mit **zwei Gruppen — „Am Boden" zuerst, „In der Luft" danach** (am Boden startet alles an, in der Luft nur die Bahn). **Vier Befunde kamen erst aus dem laufenden Bild (§23.3):** die Positionsmarke fehlte bis zum ersten Scrubben (ihr Effekt lief vor `readyRef`); die Windpfeile lagen **hinter** der Wand (vor dem Vorhang angelegt, also auch vor ihm gezeichnet); und dann waren sie **immer noch** unsichtbar, obwohl Bilder registriert, elf Merkmale gesetzt und keine Warnung da war — **`icon-rotation-alignment: 'map'` zieht `icon-pitch-alignment` mit** (Standard `auto`), der Pfeil liegt damit flach auf dem Gelände, und bei 64° Kameraneigung schrumpft ein 12-px-Pfeil auf rund 5 px (jetzt 24 px bei Größe 1,4, beide Ausrichtungen ausdrücklich); viertens dürfen Pfeile fehlen — unter 4 m/s setzt `buildColumns` keine Windrelation, und der Chip sagt jetzt „zu schwach" statt stumm zu bleiben. **Gemessen:** Chip-Schalter ≤ 64 ms, Bühnenwechsel 112/163 ms, Leerlauf **0** Long Tasks; Konsole nur die vorbestehenden Brightsky-404 (V-R3D-4); mobil kein Ziel < 44 px, kein Überlauf; jede leere Spur benannt („trocken", „keine Warnung", „ohne Daten", „zu schwach"). `verify:route-3d` **490/490** (+29, u. a. dass `buildCurtain` OHNE Bahn byte-gleiche Stützpunkte liefert), `verify:routing` 104/104, typecheck grün, Budget totalJs 1 048,5/**1 048,5** KB. Offen: **V-R3D-13** (die Bahnhöhe ist fest — wer die ganze Vertikale will, nimmt den Schnitt; ein zweiter Weg zur selben Aussage wäre erst einmal einer zu viel), V-R3D-10 (Regen IN der Wand), Real-Device, V-R3D-4, V-R3D-7…9, V-R3D-11/12. Davor **R3D-5 — die Route komplett in 3D** `audit/route-3d.md` §21/§22 (Jans Frage „gibt es auch die Möglichkeit, die Route komplett in 3D zu sehen — ebenso alle Wetterparameter entlang der Route?"; damit ist H3 aus §19.7 entschieden): die 3D-Ansicht hat jetzt **zwei Bühnen** — **`Schnitt`** (axonometrisches SVG mit den drei Modi 1a/1b/1c) und **`Gelände`** (MapLibre-Karte mit echtem Relief und der Wetterwand entlang der Strecke). Der Umschalter steht getrennt von der Modus-Zeile, weil der Modus die **Frage** ist und die Bühne die **Darstellung**; auf der Karte entfällt die Modus-Zeile und ein Satz sagt, wo Zeitkorridor und Grenzwerte geblieben sind. **Die Diagnose §21 hat zehn Befunde am Code gemessen:** (E1) `buildCurtain` nimmt eine **beliebige** Polylinie aus `{lon, lat, distanceM, terrainM}` — nichts darin setzt die gerade Schnittlinie der Atmosphären-Ansicht voraus; (E2) `assembleCrossSection` ebenso, und `AnchorSurface` verlangt genau, was ein Tour-Sample führt ⇒ die Tour speist den **vorhandenen** Vorhang, es entsteht **kein zweiter Rechenweg** (der Verifier rechnet jede Zelle gegen `windAtAGL` nach); (E3, schwer) **die Anker einer Tour gelten NICHT zur selben Zeit** — `estimateInversion` vergleicht den tiefsten mit dem höchsten Anker, zwischen denen auf einer Tour Stunden liegen; „oben wärmer als unten" wäre ein Zeitunterschied, und die Folge bliebe nicht beim Etikett, weil eine erkannte Inversion das Temperaturfeld auf einen isothermen Kaltluftsee umstellt ⇒ additiv `CrossSectionInput.inversion` (`estimate` bleibt Standard), die Tour fährt `none` **und sagt warum**; (E4) **keine Shader-Zeile wird angefasst** — `CurtainLayer` wird nur benutzt (`setCurtain`/`setStreamlines`), also fällt die STOPP-&-FRAGEN-Auflage nicht an; (E5) die Textur trägt Wind **oder** Böen, Temperatur und Wolken — **Regen, Schneefallgrenze und Warnung nicht** ⇒ der Regen färbt die Strecke, die übrigen stehen in der Punkt-Abfrage, und ein Satz sagt es, damit die Abwesenheit in der Wand nicht als „kein Regen" gelesen wird; (E6) Überhöhung ist EINE Zahl an ZWEI Stellen (`setTerrain` und `CurtainLayer`) — und ausdrücklich nicht die des Schnitts; (E7) WebGL-Rückfall nennt den Schnitt als Weg; (E8) die Scrub-Position ist geteilt; (E9) `verifyCurtainMesh()` und `verifySectionGeometry()` hingen an keinem Verifier; (E10) 96 Vorhang-Spalten sind die Auflösung des **Bildes**, nicht die des Wetters (6–14 km). **Umgesetzt (§22):** `route3d/routeSection.ts` (pur: Track → Spalten, Samples → Anker, Deckel „höchster Punkt + 1 200 m"), `route3d/RouteTerrainMap.tsx` (Terrarium-`raster-dem` + `setTerrain` + **Schummerung aus derselben Quelle**, Strecke auf dem Relief, nasse Abschnitte als eigene Linie, `CurtainLayer` unverändert, Streamlines, Positionsmarke, Klick setzt die Position), Karte als **eigener Lazy-Chunk 4,95 KB gzip**. **Im Browser gemessen:** Wand steht auf der Strecke, Positionsmarke wandert (−110 px → 863 px) und die Position überlebt den Bühnenwechsel (km 8,6), fünf Wand-Chips schalten live, Long Tasks 7 × max **124 ms** (Schnitt weiter 0), Konsole ohne Fehler UND ohne Warnungen, mobil 340 px hohe Karte, kein Ziel < 44 px, kein Überlauf. **Vier Befunde kamen erst aus dem laufenden Bild (§22.3):** die liberty-Warnung (V-RL-3) kam zurück — ihre Korrektur lag als **lokale Funktion** in `RadarMap.tsx` ⇒ herausgelöst nach `src/map/libertyStyle.ts`, und sie muss auf **`style.load`** laufen, nicht auf `load` (auf `load` gepatcht blieb die Warnung, gemessen); die Kamera stand quer zur Strecke — **richtig gerechnet und trotzdem unbrauchbar**, weil die Bergflanke zwischen Kamera und Wand stand ⇒ jetzt vom **tiefen Ende** die Strecke hinauf, 42° herausgedreht; die Wand war **3,7 km hoch** und verdeckte das Gelände, obwohl `windAtAGL` bei 1 500 m über Grund sättigt ⇒ gedeckelt; und ein **Umschalter sah aus wie ein Ausschalter** („Mittelwind in der Wand" unbeleuchtet las sich als „kein Wind") ⇒ „Böen statt Mittelwind". **Mess-Lehre (§22.4):** ein laufendes WebGL-Gelände lässt sich im Automations-Browser **nicht zuverlässig fotografieren** (zweimal 120-s-Timeout, dreimal gelungen, bei gleichem Zustand) — erst auf Netzruhe warten, und was ein Bild belegen soll, zusätzlich numerisch absichern. `verify:route-3d` **461/461** (+80, inkl. der bis hier unverdrahteten `verifyCurtainMesh()` und `verifySectionGeometry()`), `verify:routing` 104/104, typecheck grün, Budget totalJs 1 046,9/**1 047,1** KB (Ratsche von 1 038,4; alles im Lazy-Chunk, eagerJs unverändert). Offen: **V-R3D-10** (Regen als eigene Schicht in der Vorhang-Textur — erst damit stünde „alle Wetterparameter" wörtlich in der Wand), **V-R3D-11** (Blickseite beidseitig abtasten statt heuristisch), **V-R3D-12** (Texturbau im Worker), Real-Device, V-R3D-4, V-R3D-7…9. Davor **R3D-4 — Gelände exakt, Wetter exakt auf der Ebene** `audit/route-3d.md` §19/§20 (Jans Auftrag „das terrain (vielleicht von maplibre) exakt abbilden und alle Wetterdaten exakt auf der 3D-Ebene"): **Die Diagnose §19 hat acht Befunde am Code und an echten DEM-Kacheln gemessen** (`audit/route-3d/gelaende-messung.mjs`, importiert die echten Module): (D1) **die Szene zeichnete ihr Gelände durch die WETTER-Samples**, nicht durch den Track — `Scene3D.tsx` gab `scene.columns` an `terrainPath`/`skyPath`, und eine Spalte steht alle 500 m (alpin) bis 2 km (flach); an echtem DEM-Gelände mit 25-m-Track gemessen lag die Kette p95 17–23 m, **max 39,7 m** daneben und verlor im Flachland **77 % des Anstiegs** (47 → 11 hm) — der volle Track liegt dabei längst in der Szene und wurde nur für `bearingAtDist` benutzt; (D2) **die Höhe der Szene war die Behauptung der Datei** — `tourTrack.ts` holt das DEM nur, wenn `elevationUsable()` fehlschlägt, und das prüft Lücken, Nullen und „unplausibel flach", **nie das Gelände** (an den vier Testdateien: Median 2,4 / 28,7 / 90,7 / 115,7 m, Versatz bis +107,8 m) — und es bleibt nicht beim Bild: `s.ele` speist `correctForElevation` (0,65 K je 100 m) und die Wolkenbasis, also auch die 1c-Entscheidung; (D3) die Rückwand war die **Extrusion desselben Profils** — zwei echte Seitenprofile kosten gemessen 4–5 → 9–11 Kacheln (z13), weit unter dem Deckel 64; (D4) die Windwand endete bis 40 m neben dem Berg, weil das Himmelspolygon dasselbe grobe Profil war (fällt mit D1); (D5) **Hagel kann diese Ansicht nicht zeigen** — `SampleWeather` führt kein Hagelfeld, die beiden DACH-Quellen (DWD KONRAD3D, MeteoSchweiz POH/MESHS, letztere nur 1. April–30. September) sind **Beobachtungen**, AT hat gar keine, und **`MapView.tsx:1609` schaltet die Hagel-Layer selbst nur bei `forecastHour === 0`** ⇒ benannte Lücke statt erfundener Zahl (derselbe Fall wie C1/B8); ableitbar wäre Gewitterpotenzial — eine andere Aussage, die anders heißen muss; (D6) Schnee war im Modell und nicht im Bild (nur die Strichfarbe unterschied ihn von Regen); (D7) **der MapLibre-Weg liegt schon im Repo** — `src/threed/TerrainMap.tsx` (Terrarium `raster-dem` + `setTerrain`) und `src/threed/CurtainLayer.ts` (Custom-Layer, `MercatorCoordinate` mit Höhe, Tiefentest an) —, aber er ist ein **anderes Produkt**: die Vorlagen 1a/1b/1c sind Diagramme mit Höhenachse, Windbändern und Grenzwertzonen, eine gekippte Geländekarte trägt davon nichts, und `CurtainLayer` ist WebGL (STOPP & FRAGEN) ⇒ **Empfehlung: das Diagramm bleibt, sein Gelände wird exakt — aus derselben DEM-Quelle, die MapLibres `raster-dem` benutzen würde**; eine Geländekarte als vierter Modus bleibt offen und billig; (D8) der **Ortsbezug des Wetters bleibt der Cluster** (alpin med 0,5 km/max 2,0 km, **flach EIN Cluster für die ganze Strecke**, max 8,9 km) — ein pixelgenaues Gelände darf das nicht überstrahlen ⇒ die Fußzeile nennt ab jetzt **zwei** Auflösungen. **Umgesetzt (§20):** `buildTerrainProfile` als **Hüllkurve** (je Bildspalte tiefster UND höchster Punkt, Knoten an jeder Wetterspalte erzwungen, Deckel 1 400) speist Silhouette, Verdeckungsmaske, Kappe, Routenband, `freeSpan` und Höhenachse; `minTerrainBetween` zieht Windwand und Regensäule bis zum tiefsten Gelände ihrer Spanne herunter, **ohne die Höhe über Grund neu zu rechnen**; `compareToDem` + `ELE_TRUST_M = 50 m` ersetzt Datei-Höhen, die dieses Gelände nicht beschreiben (`TourMeta.elevationSource`/`elevationDeltaM`, `null` heißt **nicht geprüft**, Kill-Switch `?dem=0`); `sampleReliefProfiles` tastet das DEM **links der Fahrtrichtung** in 2 km und 5 km ab (senkrecht auf die *lokale* Richtung) und ersetzt die Extrusion — womit die Tiefenachse zum ersten Mal eine Bedeutung hat (1,0 = 5 km); Schnee fällt senkrecht und gestrichelt; `terrainNote` steht **neben** `resolutionNote`, `HAIL_NOTE` benennt die Lücke. **Im Browser gemessen (Prod-Build):** Schauinsland **162 Profilknoten statt 31 Spalten** (Verdeckungsmaske aus derselben Kante), Abweichung Profil ↔ Spaltengerade **22,2 m**; Berlin **163 statt 7**; Relief 2 × 163 Knoten, Kappe entfällt; **größtes Loch unter der Windwand 0,00 px** über alle 31 Spalten; Schauinsland-Höhen **116 m daneben ⇒ ersetzt** (Fußzeile und Vorschau-Chip sagen es), Berlin **2 m ⇒ unangetastet**; **16 verschiedene DEM-Kacheln**; Long Tasks **0**; Konsole ohne Fehler; mobil **0** Textkollisionen. **Drei Befunde kamen erst aus dem laufenden Bild (§20.4):** die Vorschau nannte ein *Ersetzen* „ergänzt" (ein Flag, zwei Vorgänge ⇒ zwei Chips); das Relief kommt später als die Szene, ein zu früher Screenshot dokumentiert den falschen Zustand; und der erste Nachweis „Wand steht auf dem Boden" maß **alle** Wandsegmente statt je Spalte des unterste und meldete ein 217-px-Loch, das es nicht gibt — dieselbe Lehre wie §18.5 in anderer Gestalt: eine Messung muss denselben Gegenstand fassen wie das Auge. `verify:route-3d` **378/378** (+62), `verify:routing` 104/104, layer-geometry 76/76, radar-sampling 25/25, event-zone 41/41, typecheck grün, Budget totalJs 1 038,3/**1 038,4** KB (Ratsche von 1 036,4; eagerJs unverändert 102,7). Offen: Real-Device, V-R3D-4, **V-R3D-7** (49 Kachel-Abrufe für 16 verschiedene — Dekode, nicht Netz), **V-R3D-8** (Reliefseite bei Rückwärts-Touren), **V-R3D-9** (Schwelle 50 m an echten GPX schärfen). Davor **R3D-3 — Phase 1c „Go / No-Go" umgesetzt** `audit/route-3d.md` §17/§18 (Jans Auftrag „starte mit 1c"): die 3D-Ansicht hat jetzt **drei Modi** auf demselben Pfad — 1a „Wetter entlang der Route", 1b „Zeitkorridor" und **1c „Grenzwerte"** („wo wird es kritisch — und warum genau dort?"). **Die Diagnose §17 hat sechs Vorlagen-Elemente am Code korrigiert:** (C1) eine **Sichtweite führt die Punkt-Kette nirgends** (`SampleWeather`/`PointForecastHour`/`PointSourceSample`; die Suche nach `visibility` trifft nur MapLibre-Layer) ⇒ der Chip „Sicht ≥ 1 km" entsteht **nicht**, der Katalog wird aus dem gebaut, was am Sample hängt (Böen, Wind, Regen, gefühlte Temperatur, amtliche Warnung), und die Fußzeile sagt warum — derselbe Fall wie B8 in 1b; (C2) es gibt **schon** ein Go/No-Go im Repo (`src/threed/goNoGo.ts`, B2B Drohne/Kran) und es beantwortet eine **andere** Frage — ein Anker (höchstes Gelände), Zeitachse, EIN Grenzwert, ZWEI Zustände; die Tour fragt entlang der Strecke, an wandernder Position, gegen mehrere Grenzwerte, mit VIER Zuständen ⇒ eigenes Modul `route3d/gonogo.ts`, geteilt wird nur `windAtAGL` (Nebenbefund: `verifyGoNoGo()` hing an keinem Verifier — jetzt mitgezogen, wie `verifyCrossSection` bei B6); (C3) der **vierte Status ist ein bereits gegebenes Versprechen** — `Route3DView.tsx:621` sagt seit 1a „Bei ‚unklar' nennt die Ansicht keine Entscheidung, nur die Werte", `UNCLEAR_BELOW = 0,45` ist die eine Stelle ⇒ Rangfolge **`no-go > unklar > knapp > go`** (eine belegte Überschreitung bleibt eine, auch bei mäßiger Konfidenz; Unsicherheit wird nie in Richtung Freigabe aufgelöst); (C4) „Frühesten Go-Start suchen" geht — aber **`peakGustMps` durfte dafür nicht benutzt werden**: der Vertrag sagte selbst „nie als absolute Zahl anzeigen" (nicht höhenkorrigiert), ein Vergleich gegen 40 km/h hätte für denselben Ort eine andere Zahl gezeigt als die Abschnittsliste ⇒ `evaluateStartOffsets` ruft jetzt **dasselbe `correctForElevation`** wie der Hauptweg (Anker `pf.query.elevation`, Sample `s.ele`) und trägt zusätzlich `peakWindMps`/`minApparentC`; die **amtliche Warnung bleibt ungeprüft** (asynchron, 17 Kandidaten × alle Samples wären ein zweiter Abfragefächer) und die Suche **sagt es**; (C5) Grenzwerte gehören in `localStorage` (`bsc.route3d.limits`), nicht in den Tour-Speicher — sie überleben die Tour; Startwerte bewusst **nicht je Bewegungsart** („Rennrad 35, Bergwandern 45" behauptete Fachkenntnis, die das Projekt nicht belegen kann) und beschriftet als „**Startwerte — keine Empfehlung**"; (C6/C7) „PDF" kostet **keine Dependency** (`window.print()` wie `HistoryPro`, D-06 unberührt: weiter sieben), und „teilen" kann **keinen Link** teilen (B3: die GPX steht in keiner URL) ⇒ Textbericht nach dem Muster von `GoNoGoPanel.buildReport()`. **Im Browser gemessen (DE-Tour, Freiburg → Schauinsland):** alle **vier** Zustände traten an echten Daten auf — am Tourende fiel die Gefühlt-Konfidenz unter 0,45, während der Wert den Grenzwert hielt ⇒ „unklar", nicht „Go"; die Zahl der Abschnittsliste und die des „Warum"-Diagramms sind **dieselbe** (Böen 52 km/h; der Punkt sitzt auf `REF_AGL` = 10 m, wo `windAtAGL` den Bodenwert unverändert zurückgibt — C9), Grenzwertregler und Bahnen rechnen live durch (Grenzwert 20 ⇒ 1 Abschnitt NO-GO, 90 ⇒ 1 Abschnitt GO). **Vier Befunde kamen erst aus dem laufenden Bild** (§18.4): zwei Bühnen-Karten lagen aufeinander; die Start-Beschriftung lief durch die Legende — **auch in 1a**, also älter als diese Phase; die Zonen-Fahne verdeckte „Ziel · 1279 m" (dieselbe Auflage wie §16.4 (2) ⇒ belegte Kästen); und „unklar · Böen 35 km/h" zeigte den **Messwert**, der den Grenzwert von 40 hielt — unsicher war die Konfidenz ⇒ eine „unklar"-Zeile nennt jetzt den **Grund**, nie eine Zahl. Dabei fiel auf, dass der Langsatz kein deutscher war („Zwischen km 3,8 und 5,8 **Gefühlt ist** zu unsicher") ⇒ `LimitDef.noun` als Nennform nach „für", artikellos oder feminin (Nominativ = Akkusativ, keine Numerus-Falle). **Zwei Mess-Lehren (§18.5):** `getBBox()` **ignoriert `transform`** — die erste Kollisionsprüfung meldete 0, während der Screenshot die Überlappung zeigte (für Gruppen zählt `getBoundingClientRect()` auf den Text-/Rechteck-Elementen selbst, nicht auf ihren Gruppen — die Umkehrung von §16.4); und eine Negativ-Sonde braucht **Wortgrenzen**: `/sicht/i` schlug auf „An**sicht**" im eigenen Ehrlichkeitssatz an (§14.5 in einem deutschen Kompositum). `verify:route-3d` **316/316** (+97, inkl. der bis hier unverdrahteten `verifyGoNoGo()`), `verify:routing` 104/104, layer-geometry 76/76, radar-sampling 25/25, event-zone 41/41, typecheck grün, Budget totalJs 1 036,3/**1 036,4** KB (Ratsche von 1 029,6; eagerJs unverändert 102,7), Long Tasks am Prod-Build **0** (Moduswechsel 17 ms, Bahnen 16,7 ms/Bild, Grenzwertregler 16,7 ms/Schritt), mit CPU-Drossel 4× **ein** Task von 61 ms; Konsole ohne App-Fehler (nur die vorbestehenden Brightsky-404 der Sondierung, V-R3D-4). Offen: Real-Device, V-R3D-4, **V-R3D-5** (Sicht als Grenzwert = neues Feld durch die ganze Punkt-Kette + Adapter je Land), **V-R3D-6** (viele kurze Abschnitte bei Grenzwerten nahe dem Streckenmittel — Produktfrage, keine Datenfrage). Davor **R3D-2 — die vier Restposten vor 1c geklärt** `audit/route-3d.md` §15/§16 (Jans Auftrag „kläre bevor wir mit 1c starten diese punkte noch"): **Prod-Long-Tasks gemessen** (Prod-Build, AT-Alpentour): 3D-Ansicht öffnen **0**, Zeitbahn 122 Bilder **16,4 ms/Bild · 0**, Positionsbahn 16,5 · 0, Moduswechsel 33 ms · 0, Scrubben in 1a 16,5 · 0; mit **CPU-Drossel 4×** 18,3/19,6/22,6 ms je Bild und ebenfalls **0** — der größte Task der ganzen Sitzung ist 61 ms und liegt im Aufbereiten der Strecke. **V-R3D-2 geschlossen:** der DPR-3-Screenshot-Timeout ist in sechs Konfigurationen (drei Breiten, Viewport und fullPage, mit und ohne laufende MapLibre-Karte) **nicht reproduzierbar** — es war ein Zustand des Automations-Browsers, keine Eigenschaft der Ansicht; die dadurch ausgefallene **Sichtprüfung ist nachgeholt** und hat fünf Dinge gefunden, die kein Zahlenwert meldet (§16.4): auf dem Tablet lag „Ziel · 2234 m" unter der Ziel-Perle (die Höhentexte waren je **Breite** statt je **Modus** abgeschaltet); eine Perle verdeckte das Wort **„abgeleitet"** an der Wolkenbasis — eine Ehrlichkeitsauflage ⇒ `layoutCards` kennt jetzt **belegte Kästen**; dabei fiel auf, dass `layoutCards` erst hob und **dann** klemmte (eine Karte konnte auf der landen, der sie gerade ausgewichen war — genau der Fehler aus §14.4); eine **deutsche** Tour sagte „Regen — ohne Daten" und „Warnzone — ohne Daten", obwohl DWD geliefert hatte (die §14.4-Korrektur galt nur für die 1b-Chips) ⇒ neu `LayerAvailability.emptyLabel` an der Stelle, die es weiß: „trocken" / „keine Warnung", und in AT/CH bleibt es bei „ohne Daten", weil dort wirklich die Quelle fehlt; und der Mobil-Zurück-Knopf misst 32 px ⇒ er **bleibt** 32 px (Kopfzeilen-Raster aller Route-Screens), nur seine **Anfassfläche** wächst per `::after` auf 44 px. **V-R3D-3 behoben:** `show3d` beantwortete zwei Fragen mit einem Wert — gemessen baute sich beim „Startzeit übernehmen" nach 20 ms die 3D-Ansicht ab und bei +51 ms eine **komplette 2D-Ansicht samt MapLibre-Kontext** auf, elf Sekunden lang; jetzt trennt `in3d` (Rahmen) von `show3d` (Szene), und ein Wartefeld steht INNERHALB der 3D-Ansicht — nachgemessen **keine** Karte mehr, der Rahmen bleibt stehen. **V-R3D-1 behoben:** `src/route/` hat jetzt genau eine Persistenz — `src/route/tourStore.ts`, **IndexedDB spaltenweise** (100 000 Punkte = vier `Float64Array` ≈ 3,2 MB gegen ~6,5 MB JSON, mehr als `localStorage` je Origin fasst), Samples als **Indizes** statt Kopien (Identität wird beim Entpacken wiederhergestellt), Plan (Richtung, Art, Tempo, Pausen, Startzeit, E-Bike, „berechnet") dabei — **das Wetter nie**, es wäre nach Minuten falsch und wird sichtbar neu geholt. Drei Auflagen: die wiederhergestellte Tour **sagt**, dass sie aus dem Gerätespeicher kommt, nennt den Zeitpunkt und trägt **„verwerfen"** (löscht den Eintrag wirklich, nachgelesen); gespeichert wird erst **ab der Planung**; und die Startzeit folgt der Regel, die die App ohnehin hat (`horizonState`) — −30 min bleibt stehen, −3 h und +20 d rücken auf „jetzt" **und sagen es**. Kill-Switch `?tour=0` / `localStorage.tour = '0'` (löscht nichts). **Gemessen:** F5 und Viewport-Wechsel bringen dieselbe Tour im selben Modus zurück, direkt in `/tourenplanung/3d`. `verify:route-3d` 219/219 (+53), `verify:routing` 104/104, layer-geometry 76/76, radar-sampling 25/25, event-zone 41/41, typecheck grün, Budget totalJs 1 029,5/1 029,6 KB (Ratsche von 1 027,5; eagerJs unverändert 102,7). Konsole: keine App-Fehler; 23 × HTTP 404 von Brightsky an Sondierpunkten westlich einer DE-Tour (im Code ausdrücklich als „outside DE coverage" behandelt ⇒ **V-R3D-4**) und ein GeoSphere-429 aus der eigenen Messlast. Offen: Real-Device, V-R3D-4, **1c** — jetzt ohne Restposten davor. Davor **R3D-1b — Zeitkorridor umgesetzt** `audit/route-3d.md` §13/§14 (Jans Auftrag „starte 1b"): die 3D-Ansicht hat jetzt **zwei Modi** auf demselben Pfad — „Wetter entlang der Route" (1a) und **„Zeitkorridor"** (1b: „wo bin ich um welche Uhrzeit — und was zieht dann über mich hinweg?"), Umschalter über den Chips, Modus im `localStorage`. **Diagnose §13 hat drei Vorlagen-Elemente am Code widerlegt:** (B8) es gibt **nirgends** eine Niederschlagswahrscheinlichkeit in der Punkt-Kette (weder `PointSourceSample` noch `PointForecastHour` noch `SampleWeather`) ⇒ die „Treffer-Wahrscheinlichkeit" wäre erfunden, an ihrer Stelle steht **„Regen je Startzeit"** (gerechnete Regenminuten je Kandidat, mit dem Satz „keine Wahrscheinlichkeit"); (B9) der Radar-Vorlauf ist **je Land verschieden** — DE RADOLAN-RV 0–120 min, AT INCA 0–180 min, **CH ein einziger Snapshot ohne Vorlauf** — und `EnrichmentMeta` reichte ihn gar nicht durch; (B10) „Zelle · Zug 32 km/h" ist aus einem Punktabgriff **nicht ableitbar** (Zellverfolgung wäre KONRAD3D: DE-only, nur konvektiv, und die Vorlagenroute überschreitet die Grenze) — ableitbar ist dagegen exakt das, was das Radarprodukt selbst schon extrapoliert hat: **welcher km-Abschnitt wann nass ist** (`rainWindows`). Dazu (B11) **„Besserer Start" kostet keinen einzigen zusätzlichen Abruf**, aber nur an EINER Stelle: `forecasts[]` und der `RadarNowcastSampler` sind lokale Variablen der Anreicherung und danach weg — die Bewertung läuft deshalb INNERHALB von `enrichSampleWeather` (`startOffsetsMin`, ±2 h im 15-min-Raster; die Reserve `neededHours = ceil(...)+2` deckt sie schon), und (B12) die Startzeit ändert über den Wind auch die **Fahrzeiten** ⇒ die Empfehlung sagt, dass sie mit festen Fahrzeiten rechnet. **Umgesetzt:** `src/route/route3d/corridor.ts` (pur: Regenfenster km × Zeit, Wetterperlen zur ETA, Zeitfenster-Zeilen, Startempfehlung, Radar-Vorlauf-Sätze), `weatherEnrichment.ts` additiv (`RadarHorizon`, `StartWindowEntry`, `START_OFFSETS_MIN`, `evaluateStartOffsets`, `sampleDurationsMin`), `layoutCards` in `scene.ts`, Perlen/getroffener Abschnitt/Windpfeile in `Scene3D`. **Im Browser gemessen (AT-Tour, nass):** Empfehlung „Start um 00:16 statt 22:31: 99 statt 180 Minuten im Regen" → „Startzeit übernehmen" → Tour rechnet **echt** neu (00:16 → 09:14, 8 h 58 statt 8 h 55 — genau die Abweichung, die B12 vorhersagt) → danach sagt die Karte ehrlich „Keine Startzeit im Fenster ±2 h ist spürbar besser"; Zeit führt (Zeitbahn auf 70 % ⇒ 22:20 · km 0,0 → 02:35 · km 6,8, Marker x 62 → 750); Moduswechsel und 3D→2D→3D behalten die Position (km 6,8). **Sieben Befunde kamen erst aus dem laufenden Bild** (§14.4): die Windwand lief im Zeitkorridor ohne Schalter weiter; zwei Perlen klebten aufeinander — und der Abstand **in km** sagt nichts über die Kartenbreite **in px** (`layoutCards`, headless geprüft); die Abstandsregel ist eine **Zeichen**-Auflage und darf nicht für die Liste gelten (sonst fehlt „wo bin ich um 01:00?" während einer Pause); ohne sie stand dieselbe Spalte zweimal; ein einzelnes nasses Sample hatte **Ausdehnung null** (Fenster reicht jetzt bis zur Mitte zum trockenen Nachbarn); **ein fehlender Wert am Tourende kippte die ganze Empfehlung** (jetzt trägt jeder Eintrag seine Abdeckung, ab 90 % vergleichbar, Vorbehalt im Satz); und „ohne Daten" war für den Regen-Chip eine Falschaussage (jetzt „trocken"). **Lehre für Verifier (§14.5):** zwei Textsonden schlugen auf ihren **eigenen Begründungskommentaren** an — Sonden, die prüfen, was NICHT behauptet wird, müssen den Quelltext ohne Kommentare lesen (`codeOnly`). `verify:route-3d` **166/166** (+84), `verify:routing` 104/104, radar-sampling 25/25, layer-geometry 76/76, typecheck grün, Budget totalJs 1 027,4/**1 027,5** KB (Ratsche von 1 024; eagerJs unverändert 102,7), Konsole nur die vorbestehende liberty-Warnung. Offen: Prod-Long-Tasks, V-R3D-1 (Reload verliert die Tour), V-R3D-2 (Screenshots im Automations-Browser laufen bei DPR 3 in den Timeout ⇒ Tablet/Mobil numerisch belegt), V-R3D-3 („Startzeit übernehmen" zeigt kurz das 2D-Ergebnis), 1c. Davor **R3D — 3D-Ansicht „Wetter entlang der Route“ umgesetzt** `audit/route-3d.md` — Jans Auftrag „eigener 3D-Modus für die Tourenplanung, 2D bleibt Default“, Vorlagen `reference/1a-wetter-vorhang-*`. **Diagnose R3D-0** hat vier Auftragsannahmen am Code korrigiert: (B1) die Tour fragt **je Cluster** ab — 6 km alpin / 10 km hügelig / 14 km flach, Höhenband 300 m (`clustering.ts`), nicht „≈ 2 km“; (B2) der Punktforecast ist ein Blend je Land (`DWD ICON-D2/MOSMIX + Live + RADOLAN` / `AROME + INCA + TAWES` / `AROME + SMN`), eine Grenztour hat **zwei** Stacks — „ICON-D2“ allein wäre falsch; (B3) `src/route/` hat **keinerlei Persistenz** (kein localStorage, kein Hash, keine Query) und eine hochgeladene GPX passt in keine URL ⇒ **kein Router-Wechsel**, die 3D-Sicht rendert INNERHALB von `TourView`, der Pfad wird gespiegelt (RT1-Muster); (B4) `/route/3d` bricht die Pfadregeln ⇒ kanonisch **`/tourenplanung/3d`** + 301-Alias über `CROSS_ALIASES`. **Jans Entscheidungen:** Weg A (axonometrischer SVG-Schnitt statt MapLibre-Terrain — die Vorlagen zeigen **kein** Kartenbild, die Mobile-Fassung IST der `sectionImage`-Renderer; Axonometrie ist affin ⇒ `matrix()`, **kein Shader, kein STOPP & FRAGEN**), nur 1a (1b Zeitkorridor / 1c Go-No-Go = eigene Phasen), `/tourenplanung/3d` + Alias. **Umgesetzt:** `src/route/route3d/` (pur: `scene.ts` Projektion + `freeSpan`, `model.ts` Szenenmodell + Kopplung + Ehrlichkeitstexte; React: `Scene3D.tsx`, `Route3DView.tsx`, `route3d.css`), Verdeckung exakt über das **Himmelspolygon** als `clipPath` (Wand und Profil liegen in derselben Schnittebene — dieselbe Aussage wie ein Tiefentest), Windwand **stufig** aus `windBandIndex`/`BAND_COLORS` (nicht die stetige Rampe), Wolkenbasis `lclAgl` mit dem Wort „abgeleitet“, gekoppelte Regler über die **ETA** (entkoppelt = Startzeit durchspielen mit „neu rechnen“), Punkt-Abfrage 320 px inkl. Mini-Vertikalschnitt. **Gemessen im Browser:** 3D → 2D → 3D behält Strecke, Wetter und Scrub-Position (7,9 km), Browser-Zurück führt nach 2D; Desktop/Tablet/Mobil belegt, kein Touch-Ziel < 44 px, Konsole nur die vorbestehende liberty-Warnung. Neu `verify:route-3d` **82/82** (inkl. der bis dahin unverdrahteten `verifyCrossSection`), `verify:routing` **104/104** (+6), typecheck grün, Budget totalJs 1 021,9/**1 024** KB (Ratsche von 1 017,7; 3D ist ein **Lazy-Chunk 10,6 KB gzip**, der Tour-Chunk SANK 47,9 → 39,2 KB). Zwei Fallen: ein Label INNERHALB der Verdeckungsmaske ist unsichtbar (`freeSpan`), und Lazy-Laden verschiebt geteilte CSS (Umschalter → `routeDeck.css`). Offen: Prod-Long-Tasks, V-R3D-1 (Reload verliert die Tour), 1b/1c. Davor **LE2 — H3 + H7 umgesetzt** `audit/layer-erstbild.md` §9 (Jans „setze jetzt H3 und H7 um und messe"): **H3** INCA-NetCDF/rzc-HDF5 parsen im Worker — reine Parser `src/sources/incaParse.ts`/`rzcParse.ts` (wortgleich herausgelöst, Byte-Gleichheit gegen die Referenzschleife an echten Dateien belegt), `hdf5Worker.ts` + Brücke `hdf5OffMain.ts` (ein Worker je Sitzung, `warmHdf5Worker()` im Download-Schatten, Structured Clone statt Transfer ⇒ benannter Rückfall auf denselben Hauptthread-Code, Kill-Switch `?h5worker=0`/`localStorage.h5worker`); **H7** `fetch(…, { priority })`: RV-Tar + Repack-PNGs `high`, Nachbarquellen (INCA/rzc/RV je nach Land, `prioFor` in MapView, `low`-Jobs im Regenradar), KONRAD3D, Brightsky-Fächer, `cape` `low`; **V-LE-12 behoben** (Frühstart-Tar nur mit Ort und `land=de`). **Gemessen A/B verschränkt (alter/neuer Build, kalte isolierte Kontexte):** Regenradar DE ungedrosselt Karte 6,3 → 5,7 s, INCA-Long-Task 597 ms weg; **CPU 4×: 10,5 → 8,1 s (−2,4 s)** — Parse-Long-Task 2 624 ms weg UND bz2-Worker 3,7 → 1,5 s (der Hauptthread-Parse bremste unter dem Prozessbudget auch den Worker); Regenradar AT CPU 4×: INCA-Ende → Stil 3,15 → 1,32 s (Karte 9,5 → 6,0 s, davon 1,7 s Netzstreuung); Wetterkarte Niederschlag: 602-ms-Long-Task weg, DEM bleibt Deckel (H1). **H7 gemessen wirkungslos für den Tar** (Fast 4G 5,0/5,1 s, Slow 4G 21,95/21,96 s): `priority` verteilt keine Bandbreite über Hosts — die LE0-Erwartung „Tar 8,4 → 2,5 s" war falsch hergeleitet; echter Hebel = Sequenzierung (H7b/V-LE-14, H4-verwandt, Jans Entscheidung). Neu `verify:layer-erstbild` **37/37** (Verifier-Zahl 55 Aliase / 56 Harnische), `verify:routing` 98/98, radar-runs 22/22, radar-sampling 25/25, typecheck grün, Budget totalJs 1 009,4/1 017,7 KB (+20 KB jsfive im Worker-Chunk; V-LE-15 Hagel), Konsole 0/0. V-LE-13 (DevTools-CPU-Drossel = Prozessbudget ⇒ Real-Device), V-LE-16, V-LE-17. Davor **LE1 — H2 umgesetzt** `audit/layer-erstbild.md` §8 (Jans „setze H2 um und messe"): Frühstart der Datenabrufe im Router (`src/router/prefetch.ts` → `warmLiveManifest`/`warmRvTar` in den abhängigkeitsfreien Modulen `src/sources/liveManifest.ts` (jetzt Heimat von `liveManifestUrl`/`MANIFEST_TTL_MS`) und `src/sources/radolanRuns.ts` (jetzt Heimat von `guessRvRuns`/`rvTarUrl`/`RV_TAR_CACHE`); Verbraucher `gribManifest`/`iconD2WindSource`/`radolan` nehmen mit `takeWarm…` genau einmal entgegen, sonst alter Weg) + `modulepreload`/CSS-`preload`/`preconnect` je Route-Shell aus Vites `__vite__mapDeps` (`generate-seo.mjs`; CSS-Preload braucht `crossorigin`, sonst lädt Vite doppelt — erster Build hatte 4 Warnungen). **Gemessen lokal, kalt:** Wetterkarte Manifest 1 252 → 133–206 ms, Wind-Erstbild −0,6…−1,2 s, Temperatur −1,9 s; Regenradar Karte −1,8 s (Tar-Start 1 045 → 362 ms); Niederschlag −0,3…−0,5 s (Tar-Download dominiert; ein Ausreißer durch Kern-Kontention = V-LE-11). `verify:routing` **95/95** (+24), `verify:radar-runs` 22/22, typecheck grün, Budget eagerJs 102,5/106,5 KB, Konsole 0/0. Prod-Nachmessung nach dem Deploy (Jans Gate), Erwartung Manifest ≈ 1,6 s statt 3,2 s. Davor **LE0 Layer-Erstbild — Diagnose** `audit/layer-erstbild.md` — Jans Auftrag „Layer-Darstellung nochmals beschleunigen (Regenradar + Wetterkarte), Verbesserungen recherchieren". In **Prod** gemessen (Resource Timing + Long Tasks + Trace, Node-Benches am Material des Tages): Wetterkarte kalt **Wind ≈ 4,2 s, Temperatur ≈ 18,5 s** — 90 Terrarium-Kacheln (11,5 s) + `buildDemImage` **2,9–3,4 s Hauptthread**, Temperatur-PNGs erst danach (V-BW-42); **Niederschlag ≈ 11–12 s** (RV-Tar 2,4 MB Download 8,4 s im Wettlauf mit INCA/Kacheln, bz2 1,3–1,6 s + Dekode 0,5 s in Workern); **Regenradar DE 8–12 s selbst mit warmem Tar** — INCA-NetCDF **2,5 s Hauptthread** (jsfive) für einen DE-Ort, bz2-Worker-Start ≈ 1 s, React-Mount 0,8 s; Datenabrufe beider Seiten starten erst **2,4 s nach dem Aufruf** (nach Mount), Manifeste 0,4–0,75 s TTFB. Latent: 4-s-WASM-Timeout ⇒ **pure-JS-bz2 33 s je Tar** für die Sitzung (V-WF-10, lokal reproduziert). Hebel gereiht: **H1** `dem-v1.png` vorgerechnet wie `hsurf-v1.png` (Temperatur 18,5 → ≈ 4,5 s) → **H2** Manifeste/Stack vor dem Mount + `modulepreload` in den Route-Shells (−1,1…1,7 s je Layer) → **H3** HDF5 im Worker + **H4** eigenes Land zuerst → **H5** bz2+Dekode in einem Worker, 96 MB direkt, Frames-Cache → **H6** WASM-Timeout nicht sitzungsweit → **H7** fetch-Prioritäten; nicht empfohlen H8 Streaming-bz2, H9 SW-Shell (STOPP). V-LE-1…10. **Messfalle:** zwei vergessene Dev-Server vom 25.08. liefen mit 100–150 % CPU (gestoppt) — Hauptthread-Zahlen davor 2–4× zu hoch; lokale Preview für die Netzphase unbrauchbar (`public/latest-*.json` zwei Tage alt ⇒ Scan-Fallback). Nächste Phase = Jans Wahl (§7: DEM-Ablage Daten-Repo vs `public/`, Nachbarland darf später erscheinen). Davor **KL9 „±0 km"** `audit/karten-layer-verortung.md` §15 — Jans Ziel „Layer mit ±0 km Versatz, finde einen Weg (höhere Auflösung)". Gemessen: der Mercator-Rest jedes Meshs folgt **e = Δφ²·tan φ/8·R** (Formel trifft die Simulation auf 1 %); bei lat/lon-Gittern zählt NUR die Breitenunterteilung, bei projizierten (DE1200/INCA/rzc) beide Richtungen ⇒ N². Umgesetzt ohne Shader-Zeile: `quadWarpMesh.ts` ist die EINE Stelle für Mesh-Dichte (`WARP_TARGET_KM` 1 m, Zeilenregel `warpBandDeg`/`warpRowsFor`/`latRowsFor`, `warpMeshGeometry` = uv + Uint16/Uint32-**Index-Puffer** statt expandierter Dreiecke, `warpMeshFromProjection` = 64² exakt + **bikubisch** — 352² direkt mit `psInv` wären 211 ms, so 54 ms, Knotenfehler ≤ 18 mm, `equiFootprintMesh` für die ScalarLayer-Familie statt 128 × 64-Weltmesh). **Alle Meshes ≤ 1 m:** Komposit 8 × 213 (0,9 m, vorher 36 m), Wolken 8 × 328 (0,9 m / 84 m), DE1200 352² (0,8 m / 87 m), INCA 144² (0,9 m / 58 m), rzc 160² (0,8 m / 78 m), Temperatur-Familie Footprint 286 Bänder (0,8 m / **2,0 km**). `verify:layer-geometry` **46/46** (2-D-Simulator, bikubische Knoten gegen direkte Inverse, Rückfall-Anker), radar-sampling 25/25, typecheck grün, Budget 987,9/1017,7 KB, Konsole 0/0. **Befund B11/V-KL-3 (Browser, Prod-Preview):** synthetische Kante auf Zoom 14 liegt trotzdem **−77…−81 m** — unabhängig von der Mesh-Dichte —, MapLibres eigene GeoJSON-Linie exakt; per Transform-Feedback gemessen rechnet die GPU (Intel/ANGLE D3D11) `log(tan())` im Vertex-Shader mit **bis 280 m, Mittel 95 m** Fehler über DACH (`highp` hilft nicht). **V-KL-3 umgesetzt (Jans Go 2026-08-27, §15.7):** die sechs Raster-Vertex-Shader (Rain/Cloud/Scalar/Confidence/Wind-Heatmap ×2) nehmen die Lage als Attribut **`a_merc`** aus `mercatorOf()` (CPU, double, memoisiert) — kein `log(tan())` mehr, alle auf `precision highp` (V-KL-4-Vorsorge; Partikel-Shader bewusst unverändert, ihre Lage entsteht auf der GPU). Browser: Kante auf Zoom 14 jetzt **−1,9 … −5,6 m** (≤ 1 px) auf fünf Breiten über DACH statt −81 m, Footprint-Mesh −2,1 m; `verify:layer-geometry` **56/56** (mercatorOf am Float32-Boden 1,01 m, Text-/Bindungs-Sonden je Shader), Konsole 0/0. Rest = Float32-Boden des Custom-Layer-Pfads. **KL10 (Jans Go 2026-08-27, §15.8):** auch die **Wind-Partikel** rechnen kein `log(tan())` mehr — ihre Lage entsteht auf der GPU (kein CPU-Knoten für `a_merc`), deshalb `mercYTable()` (64 × 64 RGBA8, 32-bit-Festkomma, CPU-double, bounds-unabhängig) + `mercYOf` (zwei NEAREST-Taps + mix, `MERC_TABLE_GLSL` in allen vier Zeichen-Shadern, Einheit 3); Simulation `updateFrag` unverändert. GPU-Messung per Transform-Feedback mit der zurückgelesenen Textur: **max 2,6 m, Mittel 0,6 m** über DACH (vorher 280/95 m); Punkte, Segmente und Globus-`projectTile` kompilieren, Konsole 0/0, `verify:layer-geometry` **68/68**. Verbleibender Partikel-Boden: die 2-Byte-Positionskodierung (~25 m, RGBA8-Pfad tabu). **KL11 (Jans Go 2026-08-27, §15.9): B5 behoben** — `coarsenFrameU8` (neu `src/ml/coarsen.ts`, rein, headless prüfbar; `nowcasterInference` re-exportiert) kachelt flächengewichtet (`blockSpans`: 1100/138 = 7,971 Spalten je Zelle, Randspalten anteilig) statt starrer 8er-Blöcke ⇒ Flow-Nowcast, Regen-Chance, PoP-Schleier und Punkt-PoP waren bis 3,96 km nach Osten gedehnt, jetzt 0,000000 m (Masse erhalten, glatte Teilung identisch); `verify:layer-geometry` **76/76**, Browser: beide Layer 138 × 150 auf dem 353²-Mesh, Konsole 0/0. **Damit hat kein Wetterkarten-Layer im Standardbetrieb mehr einen bekannten km-Versatz.** **Große Verifikation §15.10 (2026-08-27):** headless 76/25/30 + Nachmessung an 759 DACH-Orten (alle Raster ≤ 10⁻¹² km, Flow/PoP 2·10⁻¹³ km statt 3,96 km, Schleier = 8-km-Nächster-Nachbar); Browser Prod-Preview Zoom 14: Niederschlag und Temperatur-Familie an 14 Punkten (5 Breiten × 3 Längen) **−3,6 … −5,5 m**, Wolken −8,4 … −9,7 m, DE1200 352² end-to-end gegen MapLibre-CPU-Linie 2,6–5,3 m, Regenradar DE/AT (Komposit-Mesh) 8,2–9,3 m, Partikel-Rechnung 2,6 m; Konsole 0/0. INCA/rzc-Meshes hängen nur am `radar-coverage`-Layer („Radarsicht"), den das Dock nicht anbietet ⇒ nur headless belegt. Offen: B2 (nur externes Modell, 8–10 km), V-KL-4 Real-Device, V-KL-5 `image`-Sources (Atmosphäre/Globus), KI-Nowcaster-Skill nach der neuen Kachelung ungemessen; Faktor 2 im 32er-Mesh-Nebenbefund. Davor **KL8 Mercator-Platzierung** `audit/karten-layer-verortung.md` §14 — Jans Bild `zell.PNG`: Zellbahnen „unter" dem Radarecho. Gegen das DWD-Radarkomposit derselben Minute gemessen: **die Zellbahnen (KONRAD3D) stehen richtig (0 km), der Niederschlag lag 29 km zu weit nördlich** — `RainLayer` zeichnete das DACH-Komposit als nacktes 4-Eck-Quad, die GPU interpoliert linear in Mercator, die Textur liegt breiten-linear (Fehler über 10,2° Spanne bis 30,5 km bei 50,3 N, an den Rändern 0 ⇒ nie als Versprung sichtbar; Modell +28,8 km, gemessen +29,3 km). Der Kommentar „reguläres lat/lon-Gitter → kein Warp-Mesh nötig" beantwortete die Projektionsfrage und überging die zweite Aufgabe des Warp-Mesh (Unterteilung). **Umgesetzt ohne Shader-Zeile:** `src/scalar/quadWarpMesh.ts` (EINE Stelle, memoisiert je Ecken-Referenz), `compositeWarpMesh()` in `precipComposite.ts` — `build()` liefert `warpLnglat`/`warpN` verpflichtend, MapView + RadarMap reichen sie durch; `CloudLayer` unterteilt sein Quad selbst (natives ICON-D2 14,9° ⇒ rechnerisch 66 km). `verify:layer-geometry` **23/23** (+8: Mercator-Simulation, das Quad muss den Fehler reproduzieren, Mesh-Rest 36 m / 83 m), Browser: Regenkern **1,5 km** vom WN-Maximum statt 29,3 km, Wolken-Korrelationsmaximum bei 0 km statt +66/+133 km, Konsole 0/0. **Offen V-KL-1:** `ScalarLayer`/`ConfidenceLayer`/Wind-Heatmap (128 × 64-Weltmesh) haben denselben Fehlertyp mit ≤ 2,4 km — feineres Mesh oder uv im Fragment-Shader (= STOPP & FRAGEN); Prod-Long-Task-Messung offen. Davor **BW-10 Erstbild schneller** `audit/bandbreite.md` §29 — Jans Frage „PNG statt GRIB, aber nicht schneller?" und sein Bildschirm „Lauf 12z · vor 10 h · Schnellzugriff vor 9 h aufgefrischt" bei live 21z. **Zwei Befunde:** (A) der Repack war nie eine Geschwindigkeitsmaßnahme — GBW3/GBW4 hatten PNG gegen GRIB gemessen (Δ 18 ms bei ~100 ms Streuung): der Decode lief schon im Worker, die Wartezeit ist TTFB, jsDelivr kalt (`MISS, MISS` 0,9–1,6 s) statt Netlify-Durable-Cache warm, und BW-9 stellte `Promise.all(Zeiger, Index)` (0,5–0,94 s) VOR das erste Bild; (B) **`public/sw.js` `ASSET_RE` enthält `json` ⇒ `/latest-{wind,grib}.json` galten als gehashte Assets ⇒ stale-while-revalidate aus `bsc-assets-v3`, das nie abläuft ⇒ jede Sitzung sah das Manifest der VORIGEN** (V-BW-31; `cache: 'no-store'` der App hilft nicht, der Worker greift vor dem HTTP-Cache; live belegt: Alt-Eintrag gelegt → Karte lud `runs/2026082512/wind-011.png` bei Server-Stand 21z). **Umgesetzt (Jans „mache es mit den 3 Latenz-Hebeln zusammen"):** SW `LIVE_RE` + Cache-Namen `v4`; `sectionCovers`/`resolveRepackSection(…, wanted)` befragt Manifest → Zeiger → Index und hört auf, sobald eine Quelle die gewünschten Schritte deckt (Normalfall 0 CDN-Abrufe — erstes Bild **3 ms** nach dem Manifest statt +515 ms); `preconnectDataCdn()` in beiden Manifest-Resolvern (erster CDN-Abruf dns/connect/tls 0); `REPACK_CONCURRENCY 12` nur auf dem PNG-Weg (13 Dateien kalt 4,2 → 1,0 s) **mit Fristen-Trennung** Kopfzeilen 3/6 s / Körper `BODY_TIMEOUT_MS` 30 s (`withDeadline.rearm`) — sonst fiele 3G bei 12 parallelen Abrufen still auf GRIB (schon heute ab ≈ 2 Mbit/s). `verify:repack` **295/295** (+19), Konsole 0/0, Long Tasks nur der vorbestehende DEM-Bau. **Lehren:** der Standard-Kaltstart lädt je Familie 2 Bilder (Concurrency ist dort kein Hebel); **V-BW-42** `fetchIconD2Temp` wartet ~10 s auf `buildDemImage`, bevor es sein erstes Bild anfragt — größter Rest; **V-BW-43** Pollen/UV `.json` weiter unter der SW-Asset-Regel. Deploy = Jans Gate; der erste Aufruf nach dem Deploy kann noch das alte Manifest zeigen (Worker wechselt während des Ladens). Davor **BW-9 Repack-Kette schneller** `audit/bandbreite.md` §28 — gemessen: DWD legt ICON-D2 Schritt 000 bei Lauf + 44 min ab, Schritt 027 (Repack-Horizont) bei ≈ + 66 min, auf die Minute regelmäßig; die Kette lieferte den Repack aber erst bei **Lauf + 105…135 min** (Cron `:25` mit +7…+31 min Jitter zufällig zum DWD-Takt, 8–10 min Producer, Warm-Cron-Slot + Netlify-Build 5–21 min) ⇒ 55–85 min je Lauf GRIB über Netlify. **Umgesetzt:** S1 der Client liest `index.json` **direkt vom CDN** (`resolveRepackSection`, Sitzungs-Cache 60 s, Kill-Switch `?repackidx=0`, Manifest-Abschnitt bleibt Fallback), der Publisher **purgt und prüft nach** (`purgeIndexUntilFresh`); B1 Workflow-Vorlage mit Slots an den 8 Laufstunden bei Lauf + 20 (sicher **vor** den Daten — ein Slot bei + 40 ließe bei spätem Start die Daten schon 12 min liegen; der GitHub-Jitter fällt in die Wartezeit) + **Warteschleife im Producer** (erst auf den Lauf des Slots — `expectedRunOf` —, dann auf den Horizont: `REPACK_WAIT_SEC`, `stepsMissing`/`waitDecision`); V1 `bzip2`-Binary flag-gated (`REPACK_BZIP2=1`; Wind-Lauf aus dem Cache 87,3 s → 6,4 s, 13/13 PNG byte-gleich). Zweite Runde (Jans „baue genau das"): **A** Prefetch-Pool (6 parallele DWD-Abrufe in Schrittfolge, `createFetchPool`/`planUrls`), **B** PNG-Encoder — Diagnose korrigiert: nicht die Filterwahl, das Deflate L9 auf gefilterten Zeilen kostete **21,2 s je Lauf**; jetzt `Z_RLE` **0,46 s**, Dateien in Summe −0,9 % (Filter-Rewrite byte-gleich zur Referenz `encodePngReference`, Rundlauf decode == Eingabe im Verifier; PNG-Dateien ändern sich, Werte nicht), **D** Producer rechnet **Schritt für Schritt, sobald die Dateien liegen** (Hauptschleife schrittweise statt Familie für Familie, Wartemodus je Schritt; Wind 13/13 PNG byte-gleich, `ref`-Kette belegt). Erwarteter **Versatz DWD → CDN ≈ 1,5–2 min** (heute 40–70 min); vollständiger Lauf ≈ 1 min statt 8–10. **Produktionsmessung §28.9** (Lauf #42, neuer Producer, alte Vorlage): Klon von buscosun-web **188 s** (⇒ Sparse-Checkout in der Vorlage, lokal 5 s), Repack 393 s ohne bzip2, Publish 56 s; **jsDelivr-Origin hält `@main/index.json` trotz Purge und `MISS, MISS`** ⇒ **S1b:** Publisher schreibt je Lauf einen Zeiger `runs/<run>/index.json` (neuer Pfad ⇒ frisch), Client wählt Zeiger → Index → Manifest nach Schrittzahl (`repackRunPointerUrl`). Lauf #43 (16:37 UTC, Lauf 15z, alter Slot, Producer mit Zeiger): Repack 361 s (JS-bz2), Zeiger auf jsDelivr **2,3–2,5 min nach Push**, `repack.json` 3,4–3,7 min ⇒ neue Pfade ehrlich **1–4 min** (jsDelivr hält vorher angefragte 404 am Origin fest); lokaler Volllauf 10 Familien mit bzip2 **67,3 s**. **Die `build.yml` liegt seit 20:20 UTC im Daten-Repo** (Commit `3faf09b`, mit Jans Go aus der Session gepusht); **Produktionsmessung Lauf 21z (#48, 21:31–22:07 UTC): DWD Schritt 027 um 22:06:43 → Push 22:07:10 (26–28 s) → Zeiger auf jsDelivr ≤ 22:08:10 ⇒ Versatz DWD → CDN ≈ 1,5 min statt 39–43 min; Sicherheitsnetz #47 steigt in 2 min ohne Push aus (V-BW-38 behoben).** Offen: `REPACK_INDEX_URL=''` in den Warm-Crons (Jans Gate), V-BW-41 Listings im Ausstiegspfad, Produktfrage §28.5. `verify:repack` **276/276**, `typecheck` grün, Budget 980,9/1017,7 KB. **Befund V-BW-38 (Jans Hand):** die live `build.yml` im Daten-Repo ist der Stand vor BW-6b ⇒ der Batch rechnet **jede Stunde** 8–10 min neu und löst stündlich Manifest-Commit + Netlify-Build aus; eine Action darf die Datei nicht selbst pushen — einmal `scripts/repack-repo/workflow-build.yml` manuell committen. RADOLAN bleibt auf Netlify (Jans Entscheidung 2026-08-25, §26.3). Davor **BW-8 Plattform-Inventar** `audit/bandbreite.md` §27 (Restposten: ICON-EU-Sondierung 48,5 MB je Stundenschritt, Höhenwind 28,9 MB je Ebene, Modell-Umschalter, kein `_headers`, SW-Endungsfalle, FIRMS 503 in Prod; Hebel H1–H14) und **H13 umgesetzt** (Gate GH13 §27.7: Gewitter/Blitzprognose/Schnee/Rotation laden im Nur-Jetzt-Modus nur das Jetzt-Bracket wie Wind/Temp/Böen — 76 → 16 Abrufe je Aktivierung, Slider erweitert; `verify:repack` **249/249**, Nebenbefund V-BW-37 Doppel-Install beim Mount). Davor **BW-7a Regenradar — Familie `cape`** (V-BW-22: 12,65 MiB → ≈ 0,3 MiB je DE-Sitzung, `audit/bandbreite.md` §26), RADOLAN bewusst NICHT umgepackt (5-Minuten-Batch, V-BW-29, Empfehlung: nicht bauen); parallel **RL1 Regenradar-Layer-Angleich** — Regenradar zeichnet Niederschlag (DACH-Komposit), Zellbahnen (KONRAD3D) und Schnee (ICON-D2) mit denselben Modulen wie die Wetterkarte, Gate GRL1 grün, V-RL-1…4 abgearbeitet — `ScalarLayer` räumt fremde Vertex-Attribute ab, INCA hält den letzten guten Lauf 45 min, liberty-`ref_length`-Filter gepatcht, `audit/regenradar-layer-angleich.md` §7). Davor 2026-08-24 spät (Statusblock: **BW-6 alle Wetterkarten-Layer im Repack** — Böen, Gewitter, Rotation, Blitzprognose, Schnee (Decke/Neuschnee) und `tot_prec` als PNG-Familien, `verify:repack` **230/230**, Gate GBW6 offen nur am Daten-Repo-Push (Klassifizierer); **Prod-Kreis für Wind/Temp seit 19:44 UTC geschlossen**. Davor: Bandbreiten-Linie **BW-0…BW-5 vollständig umgesetzt** — Feuerwetter/`fireSpread` gelöscht, Wind + Temperatur als PNG über jsDelivr seit BW-4 **default-on**, Regenradar entschlackt (BW-5: RADOLAN-Anteil der DE-Sitzung −89,9 %); `verify:repack` 106/106, neu `verify:radar-runs` 22/22. **Verifier-Zahl 52 npm-Aliase / 55 Harnische** — die früheren „56/57/58" waren fortgeschrieben statt gezählt, s. Abschnitt Verifikation). Davor 2026-08-22 spät (BH1 Brand-Historie; davor RT1 Pfad-Routing; davor TA Thermalanomalien). Davor 2026-08-19 (Statusblock: Aktiv-Feuer AF0–AF4 umgesetzt inkl. Kalibriermodell v1 aus Archivdaten
> 2020–2026, Doku-Landkarte um `audit/aktivfeuer.md` und `docs/aktivfeuer-merkmale.md` ergänzt, Verifier-Zahl 51). Davor 2026-08-17 (Brandflächen-Panel BP0–BP4).
> Davor 2026-08-15 (Waldbrand-/Behördendaten-Lehren). Übrige Inhalte: Stand 2026-07-31.
> Dieses Dokument ist die **dauerhafte** Grundlage für alle Agenten-Sessions.
> Es ersetzt die frühere sessionspezifische Fassung („Mobile-Optimierung", abgeschlossen; Historie in Git).
> Sessionspezifische Missionen stehen in `plan.md` (aktive Phase) und `prompt.md` (Kickoff).

## Projekt

**buscosun** (Produktion: buscosun.com, SEO-Kanonicals derzeit inkonsistent auf buscosun.app — bekannter Defekt, s. `roadmap.md` §A) ist eine DACH-fokussierte Wetter-Visualisierungsplattform: reine Frontend-Web-App ohne Backend, alle Wetterdaten werden **client-seitig** geholt und dekodiert (handgeschriebener GRIB2-Decoder inkl. CCSDS-AEC).

**Mission:** buscosun zur führenden Wetterplattform im DACH-Raum ausbauen — Referenz in Qualität, Nutzererlebnis, Geschwindigkeit, Genauigkeit, Innovation und Zuverlässigkeit. Zielgruppen-Fundament: `docs/zielgruppen-dach.md`.

**Aktueller Modus (Stand 2026-08-24): Bandbreiten-Linie BW** (`audit/bandbreite.md`, Plan BW-0…BW-5) —
Netlify hat buscosun.com am 2026-08-22 mit `usage_exceeded` abgeschaltet. Der benutzerunabhängige Teil war
bereits erledigt (Warm-Crons zu Manifest-Publishern umgebaut, ~123 GB/Monat → 0, §18); diese Linie nimmt den
**benutzerabhängigen**. **BW-0** (Jans Auftrag, ausdrückliche **Ausnahme vom Funktionserhalt**): das
**Feuerwetter ist komplett gelöscht** — `src/sources/iconD2FireWeather.ts`, `src/fire/fwi/*`,
`src/fire/spread/*`, der Layer **`fireSpread` (Bit 14)** samt Pfeil und Fächer (SF1) und die Punktkurve
„Feuerwetter am Punkt"; Bit 14 bleibt `null` reserviert (Muster Bit 1/4/5/6/10/13), die Verifier
`verify:fire-spread`, `verify:fire-fwi` und `verify:fire-weather-grid` sind entfallen. **Bleibt:** der
RH-Treiber `fireWeather` (andere Quelle, `iconD2Relhum.ts`) und die EFFIS-Gefahrensichten
(`dangerViews.ts`) — die Waldbrandseite behält ihre Gefahrenanzeige vollständig. **BW-1**: der Producer
`scripts/repack-icon-d2.mjs` packt ICON-D2 Wind/Temperatur **einmal zentral** nach PNG (608 × 373 × 8 bit —
genau die Reduktion, die bisher jeder Browser einzeln gemacht hat); Producer und Client importieren
**dasselbe** DOM-freie Modul (`windFrameBuild.ts`, neu `tempFrameBuild.ts`), `TARGET_WIDTH = 700`
unverändert (hängt an `verify:layer-geometry`). **BW-2**: Ablage im eigenen öffentlichen Daten-Repo
`buscosun-data`, ausgeliefert über **jsDelivr am Commit-SHA** (`immutable`; ein Tag wäre Zeremonie),
additiver `repack`-Abschnitt in `public/latest-{grib,wind}.json` mit der Anti-Drift-Regel
`repack.run === manifest.run`. **BW-3** (Gate GBW3, §22): der Client liest die Bilder, `src/sources/repackSource.ts`
ist die EINE Stelle; benannter Fallback auf GRIB an **zwei Fristen** (3 s erster Abruf der Sitzung, danach 6 s
je Datei), ein Fehlschlag gilt für die Sitzung, ein Abbruch **durch den Aufrufer** zählt nicht. Die zwei
Familien sind **nicht symmetrisch**: das Temperatur-PNG ist Grau+Alpha, der Browser expandiert Grau auf
R=G=B — ungefiltert stünde die Temperatur auch im Grünkanal, wo der `ScalarLayer` die Bezugshöhe liest
(20 °C als 2 400 m: plausibel und falsch); `composeTempRgba` ersetzt G durch `hsurf-v1.png`. Service Worker
reicht `cdn.jsdelivr.net` **durch** (Cache-Namen `v2` → `v3`). **BW-4** (2026-08-24, Gate GBW4, §23):
**default-on** — genau eine Zeile gedreht. Gemessen an der Kaltsitzung der Wetterkarte (Startseite → Kachel,
je eigener Origin): eigener Origin **7,42 → 1,24 MiB**, Datenanteil **6,67 MiB → 0** (0,74 MiB liegen auf dem
CDN), mobil identisch, Konsole sauber. Kill-Switch `?repack=0` (Aufruf) bzw. `localStorage.repack = '0'`
(dauerhaft), die Query schlägt den Speicher in **beide** Richtungen — und `?repack=0` überlebt das
Umschreiben der Karten-URL, weil `repack` als unbekannter Key durch `parseMapSearch(…).extra` läuft.
ADR **D-31**. `verify:repack` **106/106**. **Zwei Dinge fehlen noch, beide Jans Gate:** die Warm-Crons dieses
Repos sind uncommitted (die Prod-Manifeste tragen deshalb keinen `repack`-Abschnitt) und der Producer-Batch
im Daten-Repo ist nicht dispatcht — bis dahin steht der Schalter scharf und läuft **folgenlos** ins Leere
(0 CDN-Abrufe, keine verbrauchte Frist; §23.4). **BW-5** (2026-08-24, Gate GBW5, §24) hat das **Regenradar** entschlackt — unabhängig vom Repack, weil RADOLAN kein ICON-D2 ist. Drei Maßnahmen: (1) die RV-Lauf-Zeitstempel werden **gerechnet statt gelistet** (`guessRvRuns`, `radolan.ts`) — das Verzeichnis ist ein lückenloses 5-Minuten-Raster (gemessen: 577 Läufe/48 h, keine Lücke) und kostete trotzdem 154 KiB HTML **dreimal je DE-Kaltsitzung**; der Veröffentlichungsverzug ist über zwölf Läufe zu **3,28–3,43 min** gemessen, der Rat setzt beim frühestmöglichen Slot an und lässt einen 404 korrigieren (der umgekehrte Weg zeigte still einen 5 min alten Stand), Listing bleibt benannter Fallback; (2) das **Rückblick-Archiv lädt auf Abruf** statt beim Öffnen (Abspielen · Rückwärts-Schritt · Scrubben an den Anfang) — 8 RV-Tars ≈ 2,28 MiB, die eine Sitzung ohne Rückblick nie braucht; (3) `src/sources/shareInFlight.ts` **entdoppelt gleichzeitige Ladevorgänge** für alle drei Länder — `NowcastPage` und `NowcastRadarMap` montieren gleichzeitig und luden dieselbe Quelle zweimal (DE 494 KB, AT 722 KB, CH 157 KB gemessen). **Gemessen DE nur RADOLAN: 3 531 451 → 356 287 B (−89,9 %), 13 Requests → 1** · AT 2,101 → 1,413 MiB · CH 2,290 → 2,137 MiB, mobil identisch, keine Long Tasks. **Zwei Plan-Annahmen widerlegt:** „`/regenradar` ist 100 % ICON-D2-frei" gilt für `radarFrames.ts`, nicht für die Seite — `NowcastRadarMap.tsx:32` zieht `fetchPeakCapeAtPoint`, das **vier volle ICON-D2-Raster (12,65 MiB) für EINE Zahl** lädt und damit 76 % der DE-Sitzung ausmacht (**V-BW-22**, Jans Entscheidung, drei Wege in §24.6 — außerhalb des Repack-Umfangs); und der Doppelabruf ist kein AT/CH-Thema, sondern trifft alle drei Länder. Neu im Katalog: V-BW-22…25. **BW-6** (2026-08-24 spät, `audit/bandbreite.md` §25, Jans Auftrag „alle Wetterlayer ohne Netlify-Traffic", hebt Plan-Entscheidung 2 auf): Inventar aller 19 Layer (§25.1) — **jede Punktabfrage liest schon die 8-bit-Leinwand**, der Repack verliert nirgends Präzision; sieben neue PNG-Familien in EINER Liste (`FAMILIES`, `scripts/lib/repackManifest.mjs`, Spiegel `REPACK_FAMILIES` im Client, Verifier prüft Gleichheit): `gust`, `thunder`/`rotation` (**3 GRIB → 1 Score-Bild, Faktor 56–170×**), `lightningfc`, `snowDepth`/`snowFresh`, `precip` (volle Auflösung, **sequenziell mit genannter Referenz `ref`**, alles-oder-nichts im Client). Geteilte DOM-freie Bauschleifen `src/sources/scalarFrameBuild.ts` (27/27 byte-gleich zur HEAD-Fassung). Producer 165 → 10 MiB je Lauf (16,4×) in 6,4 min. Regeln: Producer packt nur mit ALLEN Feldern (Client nähme fehlende als 0 — anderes Bild); Skalen-Drift im Manifest ⇒ Ablehnung; Niederschlag-Ecken aus `gribCorners`, nicht `subsampledCorners(f,1)` (Gleitkomma, V-BW-26). Bleibt auf Netlify aus Sachgrund: RADOLAN, amtliche Warnungen (Lizenz), Konrad3D, Shell, Fusion (Float). **Offen:** Push ins Daten-Repo (blockiert, ein Befehl in §25.12) + CDN-Browser-Beleg; BW-6e ICON-EU-Druckflächen (28,6 MB ungecacht je Umschaltung, größter Posten der Karte); `cape`-Familie (V-BW-22). **Der Prod-Kreis ist geschlossen:** Prod-Manifeste tragen seit 2026-08-24 19:44 UTC den `repack`-Abschnitt, Batch läuft stündlich. **BW-7a** (2026-08-25, §26): Familie `cape` (volle Auflösung, `capeToU8`, 0…27, kein `ref`) über `repackGridStep`/`loadGridStep`; `iconD2Cape.ts` alles-oder-nichts — das Regenradar (DE) verliert damit seinen einzigen großen Netlify-Posten; Producer 218 → 12,5 MiB je Lauf (17,5×), 206 Bilder in 9 min. **RADOLAN bleibt** (§26.3: der Takt, nicht das Format — 288 Pushes/Builds je Tag und 5–10 min Verzug für 0,36 MiB). Verifier-Falle: der Baum trägt Batch-Läufe ohne neue Familien ⇒ Lauf aus `state.json` wählen, nicht `runs[0]`.

**Modus davor (Stand 2026-08-18): Aktiv-Feuer-Linie — AF0 (Diagnose
`audit/aktivfeuer.md`, Jans Entscheidungen §10: TS-only, clientseitig, kein AF-Cron, keine Biomasse),
AF1 (Gate GAF1 grün, §11: Überflüge nach EINER Regel — 10 min je Satellit,
`src/fire/activity/overpasses.ts`, geteilt von `fireEvents`/`fireClusters` —, Intensität je Überflug
`frpLastPassMw`/`frpMaxPassMw`, FRE mit Gültigkeitsregel) und **AF2** (Gate GAF2 grün, §12:
`dynamics.ts` Tendenz nur mit FRP-Anstieg **und** Randwachstum, Ausbreitungsrichtung ab 3 Überflügen,
Windflag nur mit geladenem `fireWind` und Frame ±3 h; `observation.ts` Beobachtungsgelegenheit bei
„kein Signal" aus regionaler Aktivität ≤ 150 km, je Zelle nach Überflug indiziert — V-AF-7: 418 → 126 ms;
`src/wind/windPointSample.ts` als geteilter Wind-Sampler, V-AF-4) als additives `FireRecord.activity`
mit Zeilen-Chip und Detailkarte, sowie **AF3** (Gate GAF3 grün, §13/§14: `features.ts` = versioniertes
Merkmalsschema `FireFeatures` v1 + `featuresOf` als EINE Referenz für Client und späteren BA-Batch,
Schema-Doku `docs/aktivfeuer-merkmale.md`, Zeile „Merkmale" + „JSON kopieren" in der Detailkarte,
Persistenz-Haken benannt, nicht betrieben) sowie **AF4** (Gate GAF4 grün, §15 — Jans Entscheidung
2026-08-18: Kalibrierung **jetzt aus dem Archiv** statt Warten auf die BA-Linie): `scripts/fire/pairs-from-archive.mjs`
(EFFIS-RDA-Kartierungen 2020–2025 × FIRMS-SP-Detektionen, dieselben Module wie der Client, Schlüssel nur
lokal, Prod-Proxy unangetastet) ⇒ **531 Paare/519 zulässig**; `scripts/fire/calibrate.mjs` +
`src/fire/activity/calibration.ts` ⇒ `public/fire/af/area-estimate-v1.json` (**604 Paare 2020–2026**;
Detektionsmodell Grad 2 — das quadratische Glied ist signifikant (t = 3,5) und beseitigt die
Unterschätzung großer Brände —, σ 1,33 ln, LOO-Abdeckung 78,8 %; FRE schwächer und als Grad 2 **nicht
monoton** ⇒ dort Gerade; Prädiktor- und Gradwahl gemessen, nicht angenommen); `estimate.ts` + Panelzeile
„Schätzung" (nie ohne Intervall, keine Extrapolation, Kartierung geht vor), Kill-Switch `?afEst=0`,
Modelldatei mit `cache: 'no-store'` (SW führt `.json` als gehashtes Asset).
Neu 2026-08-19: **VB0/GVB1** (`audit/brandflaeche-vorlaeufig.md`) hat gemessen, dass eine EIGENE Brandflächen-Kontur vor der EFFIS-Kartierung die Übereinstimmung verschlechtert (618 Archivpaare, keine von vier Formregeln schlägt das Detektionsraster) — gebaut wurde daher nur die Wortwahl „Vorläufige Brandfläche (geschätzt)" mit Intervall, wortgleich in Panel und beiden Karten-Steckbriefen; die Geometrie bleibt das Raster, die Ersetzung durch EFFIS läuft unverändert über `reconcile.ts`.
**Die Aktiv-Feuer-Linie AF1–AF4 ist umgesetzt**; die BA-Linie (Sentinel-2-Batch, drei STOPP-Entscheidungen)
ist jetzt Verfeinerung (Modell v2 mit Trennbarkeit), kein Blocker. Davor: Brandflächen-Panel BP1–BP4 umgesetzt, Gate GBP1 grün; **BP5 (2026-08-19, Gate GBP5)** hat Brand- und Cluster-Liste zu EINER verschmolzen — je Brand, mit Stärke (Summe FRP), Skala, Ausdehnung und Rangfolge „Stärke“; sie steht auf beiden Größen im Readout (`Layer | Brände`), das Overlay am linken Kartenrand ist entfallen (`audit/brandflaechen-panel.md` §11)
(`audit/brandflaechen-panel.md` §9): Layer `fireFootprints` (Bit 12), Brand-Registry
(`footprint/fireRegistry.ts`, überflugstabile Kennung, Merge/Split), Panel links als Overlay bzw.
drittes Sheet-Segment, Ortsverzeichnis GeoNames (`public/fire/places-dach.json`). Uncommitted wie
alles seit 2026-07-30. Die Sentinel-2-Batch-Linie (BA-P1…P5) wartet weiter auf drei
STOPP-&-FRAGEN-Entscheidungen (Cron, Speicherweg, Toolchain) — nächste Phase ist Jans Wahl.
Neu 2026-08-18/19: **Waldbrand-Forecast-Linie WF** (`audit/waldbrand-forecast.md`) — WF0 diagnostiziert,
Jan hat entschieden (§13: Composite-Vorbehalt für publizierte Gleichungssysteme aufgehoben · Batch =
GitHub Actions + Commit-back Stations-JSON, kein R2 · Stundenregler eine Achse · **Punkt = Fusion,
Fläche = ICON-D2 nativ**), **WF1 + WF2 umgesetzt** (Gate §14: `src/fire/fwi/fwi.ts` FWI-Rechenkern gegen
cffdrs-Vektoren 43/43, `fireWeatherGrid.ts` + `src/sources/iconD2FireWeather.ts` Raster-Producer 39/39,
Browser-Smoke 14 Frames/11 s, kein UI verdrahtet) und **WF3 umgesetzt** (Gate GWF3, §15: EINE
Zeitachse mit zwei Einheiten im Brandradar — `fireTime.ts` `hourly`/`maxHour`/`HOUR_AXIS_MAX 6`/`timeUnit`
(erzwungen > gewählt > Tage)/`dayOfHour`/`hourFollow`, Permalink `h` nur auf der Stundenachse (Literal-Anker
byte-gleich), Umschalter „Tage | Stunden" sobald ein Layer Stundenframes hat (RH-Treiber, Boden, **Wind** — Jans Revision §15.5: Achse 0…6 h, damit der Wind aus jedem Lauf mitläuft; `FIRE_WEATHER_AHEAD_H` 6), Tages-Layer
als „Tageswert" des Kalendertags von jetzt + h, beide 12-UTC-Anker über `frameAtValidTime`; 111/111 + 106/106,
Prod-Preview 0 Long Tasks beim Scrubben) sowie **WF4 umgesetzt** (Gate GWF4, §16: Layer `fireForecast`
— **Bit 13**, angehängt, Z-Band 52, eigene Dock-Gruppe „Feuerwetter stündlich"; der ERSTE `hourly`-Layer,
er erzwingt die Stundenachse und klemmt die Tagesachse nicht (`sharedMaxDay` zählt nur `forecast`).
**Fläche = ICON-D2 nativ** (WF2-Producer, stündlicher ISI aus der hFFMC-Kette, Stufe 1 ohne
Vortagsgedächtnis, Start Gleichgewichtsfeuchte), **Punkt = Fusion** (Klick ⇒ dynamischer Import des
Punkt-Forecasts, DIESELBEN Gleichungen, eigener 30,4-KB-Chunk) — der Pflichtsatz „Punkt (Fusion) ≠
Fläche (ICON-D2)" steht in der Karte, weil beide am selben Ort nie identisch sind; `fwi/isiRamp.ts` mit
harten EFFIS-Klassenkanten (Doppel-Stops, Positionen `ISI/ISI_VMAX`); 123/123 + 127/127, Budget
914,1/926,1 KB, Prod-Preview 0 Long Tasks beim Scrubben/Leerlauf/Abspielen — Kaltstart-Tasks sind der
vorbestehende Hauptthread-GRIB-Dekode (V-WF-13, Kontrolle RH-Treiber allein 418 ms)).
Neu 2026-08-22 (abends): **RT1 — Pfad-Routing** (`audit/routing.md`, Jans Auftrag + Plan-Freigabe; hebt die
Leitentscheidung „Hash bleibt" aus `audit/strategie-2026-07-31/ux-designsystem.md` §4 auf): **React Router 7.18**
(`react-router`, 7. Runtime-Dependency — D-06-Ausnahme, Jan 2026-08-22), `createBrowserRouter` mit `lazy`-Routen je
Seite (auch die Startseite — eager 121,2 → **101,5 KB gz**), Routen `/wetterkarte/:layer?` · `/warnungen` ·
`/regenradar` · `/vorhersage` · `/tourenplanung` · `/eventplanung` · `/wetterarchiv` · `/atmosphaere/:lens?` ·
`/globus` · `/waldbrand/:view?` · `/feedback` · `/validierung` · `/mobiletest` · `*` (Command-Deck-404). EINE Tabelle
`src/router/routes.ts` speist Router, Client-Meta (`RouteMeta.tsx`: Title/Description/Canonical ohne Query/OG/`WebPage`-
JSON-LD), Build-Generator (13 flache Route-Shells `dist/<route>.html` + Sitemap 189 URLs) und Verifier. Wetterkarte:
Pfad = Hauptlayer, Query `lat lon z t l modell mode radar ort olat olon land` (`src/router/urlState.ts`, feste Ordnung,
Defaults still, Unbekanntes durchgereicht); Layerwechsel = pushState, Kamera/Stunde/Modell = `replaceState` debounced
300 ms; **kein Remount** der Karte über `wetterkarte/:layer?` (EINE Route mit optionalem Param, belegt: Canvas-Identität).
Andere Features behalten ihre Hash-Codecs unter dem neuen Pfad (`/waldbrand#wb=…`), Alt-Links migriert
`src/router/legacyHash.ts` (`/#m=` → Pfad+Query, übrige → Pfad+Hash). `netlify.toml`: 22 Alias-301 + 16 explizite
200-Rewrites auf die Shells, `/* → /404.html 404` bleibt LETZTE Regel (V-101). Vier Lehren: (1) **Netlify matcht
`from` ohne End-Slash** — `/x/ → /x 301` ist eine Endlosschleife, Normalisierung clientseitig; (2) Pretty URLs erzwingt
flache `dist/<route>.html`, nicht `<route>/index.html`; (3) eine `manualChunks`-Liste zieht Rollups CommonJS-Helfer in
den maplibre-Chunk ⇒ der Start-Chunk importiert maplibre statisch (eager 380 KB gemessen) — Funktion mit eigenem
`cjs-helpers`-Chunk; (4) React Router meldet den **Erstaufruf als `POP`** — ein „nur auf POP"-Spiegel läuft beim Mount.
Verifier `verify:routing` **70/70**, Verifier-Zahl **57**, Budget geratscht (totalJs 975,7/1024,5 KB).
Neu 2026-08-22 (spät): **BH — Brand-Historie** (`audit/brand-historie.md`, Konzept
`history_feature/konzept-brand-historie-v2.md`; Jans Plan-Freigabe mit allen Defaults + Q6 = Ja: Saisonverlauf-Chart
als BH5). BH0 hat gemessen, dass ~50 % des Konzepts Bestand ist (Archiv, Baseline, Klassifizierer, Clustering,
Merkmalsatz) und drei Vorgaben dem Repo widersprechen (kein R2 ⇒ Commit-back `public/fire/bh/`; kein Workflow ohne
Freigabe ⇒ Batch lokal; Live-Pfad 24 h/7 d **bleibt**, Artefakte nur Monat/Saison). **BH1 umgesetzt** (Gate GBH1 grün):
`src/fire/history/historyEvents.ts` (Ereignis = `spatialClusters` 2 000 m × `splitByTimeGap` 48 h, je Ereignis DIESELBE
Registry + `featuresOf`; `linkPrevious` für Kennungen über Läufe), Batch `scripts/fire/bh/events-from-archive.mjs`
(SP-Cache + NRT-Rand ab Cutover, NOAA-21 ausgeschlossen) ⇒ **64 545 Ereignisse 2020–2026** in
`data/fire/bh/events.jsonl` (188 MB, gitignored; Report committet). Gemessen: Radius ±50 % ändert < 3,5 % (Zeitlücke ist
der Hebel), Einzeldetektionen 57–61 %, `site-deviating` ~1 % ⇒ keine Klasse „Unklar"; Parität mit der Brände-Liste
220/220 — **Lehre V-BH-1: die Brände-Liste (BC1) kennt keine 48-h-Lücke, das Ereignis (F2) schon**. Verifier
`verify:fire-history` 26/26, Verifier-Zahl **58**. **BH2 umgesetzt** (2026-08-23, Gate GBH2 grün):
`src/fire/history/historyArtifacts.ts` (Fenster Monat/Saison — nie leer —, positionale Index-Zeilen `INDEX_FIELDS`,
Shard-Pfad 1°-Zelle × Monat, nur DE/AT/CH mit gezählten Weggelassenen) + `scripts/fire/bh/build-index.mjs`
(`npm run fire:history-index`) ⇒ `public/fire/bh/index-{month,season}-v1.json` (54 KB / **257 KB gz** — Saison über dem
100-KB-Ziel, zwei Wege dokumentiert) + 434 Shards (1,67 MB gz, größter 47 KB); Verifier jetzt **58/58**. **STOPP &
FRAGEN vor dem Commit:** `public/fire/bh/` = 14,8 MB je Saison — Vorschlag: nur laufende Saison im Repo. **BH3 umgesetzt**
(2026-08-23, Gate GBH3 grün): Fenster **`Monat | Saison`** im Zeit-Deck (Kill-Switch `?bh=0`, Permalink `bh`, Live-Links
byte-gleich), `history/historyLoad.ts` (no-store, Fehler ≠ leer), Punkt-Layer `fire-history` in `FireMap.tsx` (kein Bit, kein
Dock-Layer — Anzeigemodus; Live-Daten im Modus leer, Rauten bleiben), `FireHistoryPanel.tsx` (Stand, Deckel, Grenzen).
**V-BH-2:** über eine Saison kippt ein Stahlwerk im TA3-Vergleich auf `site-deviating` und bekommt eine AF4-Fläche —
Index-Regel `SITE_PERSIST_DAYS 7` (> 7 Signaltage auf Standort = `site`) und keine Schätzung auf Standort-Ereignissen;
ob der Klassifizierer selbst die Regel bekommt, ist Jans Entscheidung. Prod-Preview: Long Tasks 74/53 ms. **BH4 umgesetzt**
(2026-08-23, Gate GBH4 grün): `history/historyDetail.ts` (Shard je 1°-Zelle; **Wetterlage am Brandtag** aus `src/history/*`
per dynamischem Import — Tag Meteostat/DWD gemessen, Stunde ERA5 Reanalyse, Trockenphase; ICON/Fusion haben kein Archiv,
die Karte sagt es), Detailkarte mit Landbedeckung/Natura und Evidenz (Gründe, frühere Kennung, Merkmalsatz + JSON).
**Drei Altfehler der Wetterhistorie dabei gefunden und behoben:** Meteostat-Parser las **feste Spaltenindizes**, der
Spaltensatz ist aber je Station verschieden ⇒ Luftdruck stand als „Wind max 1.018 km/h" (jetzt header-basiert);
der Stations-Cache vergiftete sich mit dem AbortError des ersten Aufrufers (Lehre GBP1 (3)); Meteostat füllt Lücken mit
`metno_forecast` ⇒ neues optionales `DailyRecord.modelFilled`, die Brandkarte markiert es mit `*` (Historie-Seite noch
nicht: V-BH-3). V-BH-4: `fetchDailyRange` lädt immer das ganze Stationsinventar (~15 s). **BH5 umgesetzt** (2026-08-23, Gate GBH5 grün): `history/historySeries.ts` (kumulierte Ereignisse je
Saisontag, DIESELBE Zählregel wie die Liste — Chart-Endwert = Listenzahl ohne Anlagen, Verifier 4 686/4 686), Datei
`public/fire/bh/season-series-v1.json` (11 KB gz), `FireHistoryChart.tsx` reines SVG im Saison-Readout mit Band/Mittel der
Vorjahre 2020–2025 und Vergleich am selben Saisontag (2026: 4 686 gegen Mittel 2 476, Vorbehalte NRT-Rand + sechs Saisons
in der Bildunterschrift). Die BH-Linie BH1–BH5 ist umgesetzt. **Jans Entscheidungen 2026-08-23** (`audit/brand-historie.md` §6a): Repo = nur die
laufende Saison (`ev/` wird immer neu gebaut); V-BH-2 bleibt Index-Regel (im 7-Tage-Live-Fenster nie erreichbar); V-BH-3
umgesetzt (Historie-Seite nennt die Modellwert-Quote je Quelle); V-BH-4 umgesetzt (Meteostat-Cache je Station **und** Jahr,
Detailkarte 1,3 s statt 15 s); BH6 offen („weiß nicht" ⇒ kein Workflow, Stand manuell). Verifier **113/113**, Verifier-Zahl **58**.
Davor 2026-08-22 (nachmittags): **drei Layer zurückgezogen** (Jans Auftrag, Ausnahme vom Funktionserhalt) —
`fireWind` (Bit 10, Windpartikel; die Winddaten laufen weiter für `fireSpread`/AF2-Windflag) sowie die
EDO-blockierten `fireDrought` (Bit 5) und `fireVegetation` (Bit 6); alle drei Bits bleiben `null` (Muster
Bit 1/4/13), Belege in `audit/waldbrand-wind.md` und `audit/waldbrand-ausbau.md` (jeweils Abschnitt „Rückzug 2026-08-22").
Neu 2026-08-22: **TA — Thermalanomalien** (`audit/thermalanomalien.md`, Jans Auftrag + Plan-Freigabe): Hotspots auf
persistenten Anlagen-Signaturen werden über eine **statische Standortliste aus dem FIRMS-SP-Archiv 2020–2026**
(`public/fire/ta/thermal-sites-v1.json`, 218 Standorte, Geodaten-Join E-PRTR/MaStR/BFE) plus einen
**Laufzeit-Signaturvergleich** (`src/fire/anomaly/classify.ts`) eingeordnet — `FireRecord.anomaly` `site` (grau,
eigener Reiter **„Thermalanomalien"** neben Layer und Brände, mobil Segment in „Brände", Permalink `ta`) bzw.
`site-deviating` (weicht ab ⇒ bleibt Brand, Abzeichen ABWEICHUNG in beiden Listen). Layer `fireAnomalies`
**Bit 15**, Z 79 (Rauten). F2-Ortsfest-Heuristik und EFFIS-Override unverändert, Kill-Switch `?ta=0`.
Verifier `verify:fire-anomalies` 56/56, Gate GTA1–5 grün; **kein Workflow, kein R2, kein OSM** (Entscheidungen
§6 dort). Verifier-Zahl jetzt **56**.
Davor 2026-08-19/20: **SF1 — Ausbreitungsrichtung aktiver Brände** (`audit/waldbrand-ausbreitung.md`,
Jans Auftrag „weniger Karte, mehr Aussage je Brand"): Layer **`fireSpread` (Bit 14, Z-Band 82)** —
je aktivem Brand ein Pfeil (Symbol-Layer, `icon-rotate`, Fuß am Brand) plus Unsicherheitsfächer,
gerechnet mit dem **kanadischen FBP-System** (Forestry Canada 1992 ST-X-3 / Wotton u. a. 2009
GLC-X-10, abgeschrieben aus `cffdrs`) aus ICON-D2-Wind, stündlichem ISI und Hangneigung aus dem
Höhenmodell; Reichweite als **Spanne über vier Brennstofftypen** (D-1/C-2/C-3/O-1b), weil es für
frische Hotspots keine Vegetationskarte gibt. **Die Rasterfläche `fireForecast` ist zurückgezogen**
(Jans Entscheidung, ausdrückliche Ausnahme vom Funktionserhalt) — Bit 13 bleibt `null`, die
DATENQUELLE `iconD2FireWeather.ts` bleibt in Betrieb und schreibt jetzt ISZ in den freien G-Kanal;
die Punktkurve auf Klick hängt an `fireSpread`. Vier Lehren: (1) die **Stundenachse ist eine
Absicht, der geladene Lauf die Wirklichkeit** — der Warm-Cron-Windlauf war live 12,7 h alt und
endete bei „jetzt", während das Feuerwetter 3,7 h alt war ⇒ `horizonHour` + `horizonNote()` sagen
den nutzbaren Horizont, sonst entsteht stille Leere; (2) die SPECS-Einfügeschleife
(`FireMap.tsx:1704`) **überschrieb das `layout`** — für jeden Symbol-Layer tödlich, gefixt;
(3) die MapView-Import-Sperre las `src/fire/` **nicht rekursiv** (52 statt ~20 Dateien, gefixt);
(4) der **Service Worker liefert nach einem Rebuild das alte Bundle** — vor jeder
Prod-Preview-Verifikation Worker abmelden und Caches leeren. Verifier `verify:fire-spread`
**203/203**, Budget 918,2/926,1 KB. Gate GSF1: Konsole und Funktionserhalt belegt, Mobile/Trace/
Desktop-Diff offen (Browser-Automatisierung brach ab).
Davor: Nächste Phase **WF5** (Tages-Codes DMC/DC/BUI per Actions-Batch + Commit-back ⇒ ISI wird FWI; neue
Workflow-Datei = STOPP & FRAGEN, Prod-Dispatch bleibt Jans Gate).**
Was wann umgesetzt wurde, steht **nicht mehr hier**, sondern in `plan.md` (Stand + nächste
Phasen), `checklist.md` (Gates mit Belegen) und `context.md` (Chronik).
> ⚠ **Befund 2026-08-17:** `plan.md`, `checklist.md`, `context.md` und `mobile-design-guidelines.md`
> fehlen im Arbeitsverzeichnis (gegenüber HEAD 2026-07-30 gelöscht), `improvements.md`/`roadmap.md`
> gibt es weder im Baum noch in Git, und `src/fire/` ist seit 2026-07-30 uncommitted. Bis Jan
> entscheidet (wiederherstellen vs. neu anlegen), gelten die `audit/*.md`-Dateien als Gate-Belege;
> V-Einträge werden dort gesammelt (`audit/brandflaechen-panel.md` §8) und später nachgetragen.
Kurz: die 2D-Layer-
Erweiterung ist geplant (GL0) und für **L5 + L6** spezifiziert (GLS, `docs/zuglinien-radar-spec.md`);
daraus vorgezogen umgesetzt sind Zellbahnen (`cells`, Z1/Z2), Hagel (`hail`), amtliche Warnungen
DE + CH (`warnings`, W1/W2); dazu die Windpartikel-Phasen (WG1/WS1/WZ1/Z3), Startseite (SA1) und
die komplette **Waldbrand-Linie** (WB0–WB5, WBU1, F0–F2, E0–E3, GWBA1, GWT1, GWW1). Offen als
Länderlücke: **AT-Warnlayer (W3)**.

> **Sonderregel für den Warn-Layer:** `warnings` ist der einzige Layer, der ein **amtliches
> Warnprodukt IST**; alle anderen verweisen in ihren Texten darauf. Dort ist Warnsprache
> korrekt — aber **ausschließlich als Zitat**: amtliche Texte werden wortwörtlich übernommen,
> nie zusammengefasst, umformuliert, verschärft oder abgeschwächt. Zusätzlich gelten die
> Lizenzauflagen in `docs/API.md` §7 (kein Durable-Cache, Datenalter sichtbar, bei Ausfall
> ausdrücklich als Ausfall kennzeichnen statt als Leerstand). Dieselbe Zitatregel gilt für
> GeoSphere-Warntexte im Waldbrand-Kontext (`geosphereWarnContext.ts`).

**Repo-weite Lehren aus der Waldbrand-Linie (gelten überall):**
1. **WFS-`maxfeatures` gegen den EFFIS/GWIS-MapServer schneidet die jüngsten Datensätze ab** —
   auf `/effis` vor dem BBox-Filter (V-224), auf `/gwis` danach, aber älteste zuerst (GWBA1) ⇒
   **kein serverseitiger Deckel**; wenn nötig im Client nach dem BBox-Filter deckeln, jüngste zuerst.
2. **Der MapServer spiegelt die BBox-Achsenreihenfolge in die Ausgabe-Geometrie** — WFS 1.1.0 +
   EPSG:4326 verlangt lat,lon; Anker prüfen die **zurückgegebenen Koordinaten**, nie Zählstände
   (`src/fire/sources/wfsAxis.ts`).
3. **`setData` auf `idle` ist eine Endlosschleife** — nur bei geänderter Referenz setzen (V-220).
4. **„bestätigt"** fällt in der Waldbrand-Ansicht nur mit Quelle im selben Satz: EFFIS-Kartierung,
   Copernicus-EMS-Aktivierung (amtliche Warnung nur nach Freigabe — MoWaS wird **nicht** ausgewertet,
   Jans Entscheidung 2026-08-15, nur Deep-Link). „Unbestätigt" ist der Normalfall und wird so gesagt.
5. **Keine unklare Lizenz, keine NC-Klausel, kein Scraping** — Quellen ohne Lizenz (NINA/MoWaS,
   AT-Einsatzübersichten) oder mit NC (Alertswiss, ORF) sind Deep-Links, nichts sonst.

## Stack (verifiziert am Code, Stand 2026-07-31)

- React 19 + Vite 6 + TypeScript 5.7, MapLibre GL 5.6. Runtime-Dependencies nur: `maplibre-gl`, `react`, `react-dom`, `bz2`, `bzip2-wasm`, `jsfive`. Kein Router, keine State-, HTTP-, Chart-Bibliothek.
- **Nicht (mehr) im Code, auch wenn Alt-Doku es behauptet:** kein Three.js, kein WebLLM/KI-Meteorologe (`src/assistant` existiert nicht), kein Cloudflare R2/PMTiles, kein „AdaptiveQualityController" (real: `FrameGovernor` in `src/wind/perfGovernor.ts`).
- Hosting: Netlify (statisch + 3 Edge Functions: `/_dwd_wind`, `/_dwd_grib` als gehärtete Cache-Proxys, `/_firms` als Schlüssel-Proxy für NASA FIRMS — `FIRMS_MAP_KEY` nur in der Umgebung), GitHub-Actions-Warm-Crons pflegen `public/latest-{grib,wind}.json`.
- Vollständige Architektur: `architecture.md`. Entscheidungs-Log: `decisions.md`.

## Dokumenten-Landkarte

| Datei | Rolle |
|---|---|
| `CLAUDE.md` | Diese Verfassung: Regeln, Konventionen, Doku-Landkarte |
| `README.md` | Repo-Einstieg: Was ist buscosun, Funktionsumfang, Schnellstart, Doku-Index |
| `architecture.md` | Repo-weite Architektur (App-Shell, Layer-System, Quellen, Fusion, Transport, Deployment) |
| `decisions.md` | ADR-Log: getroffene Grundsatzentscheidungen + Status |
| `roadmap.md` | Strategische Handlungsfelder, bekannte Defekte, Wettbewerb, Priorisierung |
| `improvements.md` | **Verbesserungskatalog (Pflicht, D-28):** jede gefundene Verbesserung als V-Eintrag mit Mehrwert + Umsetzungsskizze |
| `agents.md` | Agent-Teams-Betriebsmodell: Rollen, Zuständigkeiten, Arbeitsabläufe, Definition of Done |
| `CONTRIBUTING.md` | Arbeitsweise, Gates, Ehrlichkeitsregeln, Definition of Done (für Menschen **und** Agenten) |
| `DEVELOPMENT.md` | Entwicklungsumgebung, Skripte, Verifier-Harness, Transport, Fallstricke |
| `plan.md` | Aktive Phase (oben) + historischer Phasenplan (Archiv, unten) |
| `context.md` | Projektstand (oben, korrigiert) + Session-Log (Archiv, unten) |
| `checklist.md` | Gates; oben das Gate der aktiven Phase |
| `tests.md` | Verifikationsprotokolle V-* |
| `prompt.md` | Kickoff-Prompt für die nächste Session |
| `prompt-zellbahnen-v2.md` | Kickoff-Prompt der Phase **Z2** (Zellbahnen: Lesbarkeit & Standortbezug) — eigenständig **neben** `prompt.md`, Muster wie das frühere `prompt-2d-zuglinien.md` |
| `prompt-waldbrand-dach.md` | Kickoff-Prompt der Phasen **WB0–WB5** (Waldbrand DACH) — umgesetzt 2026-08-14, GWB4 teilblockiert |
| `prompt-waldbrand-ui.md` | Kickoff-Prompt der Phase **WBU1** (Waldbrand-Deck in Wetterkarten-Optik) — reine Darstellungsphase nach Z2-Muster, umgesetzt 2026-08-14 (Gate GWBU1) |
| `prompt-warnungen-ch.md` | Kickoff-Prompt der Phase **W2** (amtliche Warnungen Schweiz) — umgesetzt 2026-08-08 |
| `prompt-brandflaechen-echtzeit.md` · `konzept-brandflaechen-modul.md` · `prompt-waldbrand-brandflaeche.md` | Konzept-Dokumente der Brandflächen-Linie: Live-Footprints (BF0–BF5, Diagnose `audit/brandflaechen-echtzeit.md`), Sentinel-2-dNBR-Kartierungsmodul (Batch, noch nicht gestartet) und die Quellenrecherche „echte Brandflächen statt Punkte" |
| `prompt-brandflaechen-panel.md` | Kickoff-Prompt der Phasen **BP1–BP4** (Brandflächen-Panel links + Polygone, Gate **GBP1**) — umgesetzt 2026-08-17; Diagnose + Gate `audit/brandflaechen-panel.md` |
| `audit/waldbrand-behoerden.md` | Diagnose + Entscheidungsprotokoll **GWBA1** (Behördendaten DACH: Achsen-Anker, MoWaS-Befund und -Entscheidung, EMS, GeoSphere, CORINE-Maske, V-222) |
| `audit/waldbrand-boden.md` | Diagnose der Phase **WT1** (Gate **GWT1**, 2026-08-15): Bodentrockenheit aus ICON-D2 `smi`, zwei Tiefen. Enthält drei gemessene Fallen für jeden künftigen ICON-Bodenlayer — (1) `smi` verlässt 0..1 in **beide** Richtungen, ein `clamp01` auf dem Rohwert schneidet die Aussage ab; (2) die NaN-Maske deckt nur den Modellrand, **212 735 Wasserzellen tragen Werte** → `soiltyp` mitladen; (3) die Ebenen 243 und 729 sind **wertgleich**. Außerdem: die Whitelist von `dwd-grib.ts` filtert nach **Pfad-Präfix, nicht nach Variable** — neue ICON-D2-Variablen brauchen keinen Edge-Function-Eingriff und sind damit kein STOPP & FRAGEN |
| `audit/waldbrand-wind.md` | Diagnose der Phase **WW1** (Gate **GWW1**, 2026-08-15): der Windlayer der Wetterkarte 1:1 im Brandradar. Enthält die beiden Regeln, die für die Folge-Layer **Temperatur** und **Niederschlag** genauso gelten — (1) 1:1 heißt `src/wind/*` **importieren**, nicht kopieren (nur `../MapView` ist gesperrt); (2) Layer, deren Horizont kürzer ist als ein Reglerschritt, sind `instant` und werden mit `Date.now()` gefüttert — als `forecast` mit `maxDay: 0` würden sie über `sharedMaxDay()` den gemeinsamen Tagesregler der anderen Layer abschalten |
| `audit/waldbrand-firms.md` · `audit/waldbrand-effis.md` | Diagnosen der Waldbrand-Folgephasen **F0–F2** (FIRMS als Primärquelle, Gate GWBF1) und **E0–E3** (EFFIS/GWIS als Sekundär- und Kontextquelle, Gate GWBE1) — inkl. der Sonden `scripts/l0/probe-waldbrand-effis.mjs` und der Belege unter `audit/l0/` |
| `audit/waldbrand-cluster.md` | Diagnose der Phase **BC1** (Gate **GBC1**, 2026-08-16): Brand-Cluster als Liste rechts + konvexe Hülle auf der Karte. Enthält drei Lehren, die über die Phase hinausgelten — (1) es gibt **ein** Clustering im Projekt (`spatialClusters` in `fireEvents.ts`, seit BC1 mit Radius-Parameter); wer dort die Vorgabe ändert, verschiebt die Ortsfest-Einstufung F2 mit; (2) eine Liste, die „Brände" heißt, muss den **Ortsfest-Vorbehalt der Karte** tragen — sonst steht ein Stahlwerk als siebtstärkster Brand darin (live passiert); (3) **lange Listen im Readout brauchen einen ausgesprochenen Deckel**: 1 111 Zeilen kosten am Prod-Build 253 ms bis in den DOM, `content-visibility` macht es schlechter, und still zu kürzen wäre eine Falschaussage über den Bestand (V-246) |
| `audit/radar-datenrepo.md` | **§14 RD3 (2026-09-03):** Spiegel deriviert je Slot fertige Dateien nach `radar/img/v1/` (RV/INCA/rzc als precipToU8-PNGs byte-verlustfrei, KONRAD als cells.json), Client liest Bild-CDN zuerst (Gate-Leiter, `?radarimg`, alles-oder-nichts), M0-Messungen, V-RD-12 Zweig `rd3-producer`, `verify:radar-repack`. Davor **§11 RD1 Produktivsetzung (2026-08-29):** Radar-Spiegel auf `main` des Daten-Repos, Sync-Publish gegen den Force-Push-Publisher (lokal mit simuliertem Force-Push belegt), Cron+Warteschlange+Selbst-Dispatch für den Dauerbetrieb, Erststart-Nachholen, `status.json`-Telemetrie; **§10 RD-T1 Messlauf** (Orphan-Zweig, jede Stufe gestempelt, V-RD-10: Workflows nur vom Default-Zweig startbar); **§8/§9** Nachmessung des ICON-Versatzes und die Job-Kette. Davor Diagnose **RD0** (2026-08-29, Jans Frage „Radar- und KONRAD-Daten auch in buscosun-data speichern, um Netlify-Bandbreite zu sparen?" — **kein Code**): was RADOLAN-RV/KONRAD3D heute über Netlify kostet (nur DE; RV-Tar heute **Median 1,36 MB, max 2,4 MB** statt der 0,36 MiB aus BW-5 — die war ein trockener Tag, V-RD-1; ≈ 1,7 MB je 5 min offener Radar-Tab; der Tar hängt auch an **jeder DE-Punktvorhersage**, V-RD-2), die Publish-Mechanik des Daten-Repos (ein Publisher, Force-Push der ganzen Historie) und acht Befunde R1–R8: **Actions-Jitter am eigenen Batch gemessen p50 +17 min, nur 12 % ≤ 5 min** (ein `*/5`-Cron träfe einen von acht Slots), Frische-Kette 3,3 → 6–25 min (jsDelivr hält angefragte 404 fest ⇒ konservativer Rat, +5 min), **Zwei-Schreiber-Rennen** mit `publish-repack.mjs` (⇒ wenn, dann eigenes Repo), jsDelivr 150 MB/Paket ⇒ Retention ≤ 1 h, Repo-Wachstum (49 MiB bei 21-MiB-Baum, V-RD-4), **GitHub-ToS „don't use Actions as a content delivery network"**, Lizenz kein Blocker, roh vs. Repack; Wegevergleich A Daten-Repo / **B Cloudflare Worker als CORS-Proxy** (0 Netlify-Bytes, Frische unverändert, 100 000 Anfragen/Tag frei, neues Konto = Jans Entscheidung) / C Client-only; **§8 Nachmessung des ICON-Versatzes** (29 Läufe: 59 % ±1 min, 14 % drei Stunden ohne vollständigen Stand; warum der Median gut ist und das Prinzip „vorher starten, warten" auf ein 5-Minuten-Produkt nicht übertragbar ist; V-RD-7…9 als Beifang an der laufenden Kette); **§9 Dauer-Kette** (selbst-nachstartende Jobs + HEAD-Polling: möglich, 6,5–11,5 min im Browser, ToS-Fall bleibt); Empfehlung: V-BW-29 bestätigt, Radar nicht ins Daten-Repo; Entscheidungen E1–E4, V-RD-1…9 |
| `audit/route-3d.md` | **§26 R3D-8 — das Ergebnis öffnet mit dem Gelände (2026-08-29):** warum die flache Karte bleibt (vier Funktionen, die die Geländekarte nicht hat), die Tabelle der Logik, die an EINE Stelle wanderte (Palette nach `model.ts`, Schalter/Chips/Bodenspuren nach `routeSection.ts`, geteilter Chip-Knopf), der Umschalter über statt in der Karte, und **§26.3 zwei Befunde aus dem laufenden Bild** (Chips schoben die Karte unter den Falz ⇒ je Ansicht eine andere Reihenfolge, beide im Verifier; die liberty-Warnung kam aus der flachen Karte, die die geteilte Korrektur nie kannte) sowie **§26.4** die Mess-Lehre, dass ein „erfolgreich" meldendes Werkzeug nicht gewirkt haben muss (`resize_page` gegen `innerWidth`/`matchMedia`); Gate **GR3D-9**. Davor **§24/§25 R3D-7 — Zeitplan in Schriftform (2026-08-29):** warum die dritte Form fehlte (Zustand ≠ Bewertung ≠ Ereignis, F1), die Tabelle der elf ableitbaren Ereignisse mit ihrer Regel und Quelle, die Interpolation der Schwellen (F2), der Vollständigkeits-Vorbehalt (F3), die Hysterese gegen Flattern (F4), der ausgesprochene Deckel, Plan S1–S5, Entscheidungen K1–K3 (Pausen bleiben draußen, Grenzwerte nur mit dem Wort „dein", 5-°C-Marken), Umsetzung §25 mit Gate **GR3D-8**, dem Plan an echten Daten und der Messgrenze beim Druckbild. Davor **§23 R3D-6 — die Gelände-Ansicht zeigt den Boden (2026-08-29):** die drei Ursachen, warum die Karte Atmosphäre erzählte (die Wand war der einzige Träger, sie reichte bis zur Decke obwohl `windAtAGL` bei 1 500 m über Grund sättigt, und die Startschalter verstärkten es), die vier neuen Bodenspuren an der Strecke, die bodenfolgende Bahn (`bandAglM` — ausdrücklich keine Pipeline-Änderung), die feinere Abtastung, die Chip-Gruppen „Am Boden"/„In der Luft" und **§23.3 vier Befunde aus dem laufenden Bild** (Positionsmarke erst nach dem Scrubben, Pfeile hinter der Wand, `icon-rotation-alignment` zieht `icon-pitch-alignment` mit ⇒ flacher Pfeil bei 64° Neigung fast unsichtbar, und „keine Pfeile" braucht das Wort „zu schwach"); Gate **GR3D-7**. Davor **§21/§22 R3D-5 — die Route komplett in 3D (2026-08-29):** Bestandsprüfung des vorhandenen MapLibre-Wegs, Befunde **E1/E2** (Vorhang und Rechenkern sind schon allgemein — die Tour speist den bestehenden Schnitt, kein zweiter Rechenweg), **E3** (die Anker einer Tour gelten nicht zur selben Zeit ⇒ additive Option `inversion`, sonst wird ein Zeitunterschied als Kaltluftsee gezeichnet), **E4** (kein Shader wird angefasst), **E5** (was die Textur tragen kann — Regen/Warnung/Schneefallgrenze **nicht**), **E6** (eine Überhöhung an zwei Stellen), **E7** WebGL-Rückfall, **E8** geteilte Position, **E9** zwei weitere unverdrahtete Selbstverifikationen, **E10** Bildauflösung ≠ Wetterauflösung; Tabelle „was in der Wand steht und was daneben", Plan T1–T5, Entscheidungen J1–J3, Umsetzung §22 mit Gate **GR3D-6**, **§22.3 vier Befunden aus dem laufenden Bild** (liberty-Korrektur als lokale Funktion + `style.load` statt `load`, richtig gerechnete und trotzdem unbrauchbare Kamera, 3,7-km-Wand über einem gesättigten Windprofil, Umschalter der wie ein Ausschalter aussah) und **§22.4** der Mess-Lehre zum WebGL-Screenshot. Davor **§19/§20 R3D-4 — Gelände exakt, Wetter exakt auf der Ebene (2026-08-29):** Messskript `audit/route-3d/gelaende-messung.mjs` (M1 Profil-Auflösung, M1b mit 25-m-Track und echten DEM-Höhen, M2 Datei-Höhe gegen DEM, M3 Sample → Abfragepunkt, M4 Kachelkosten des Reliefs), Befunde **D1** (die Szene zeichnete ihr Gelände durch die Wetter-Samples — max 39,7 m daneben, im Flachland 77 % des Anstiegs weg), **D2** (die Höhe der Szene war die Behauptung der Datei; sie steckt über `correctForElevation` auch in Temperatur, Wind und der 1c-Entscheidung), **D3** (die Rückwand war eine Extrusion; echtes Relief kostet 4–5 → 9–11 Kacheln), **D4** (die Windwand endete neben dem Berg — D1 an zweiter Stelle), **D5** (Hagel ist in diesem Projekt ein Jetzt-Produkt, belegt an `MapView.tsx:1609`; AT hat keine Quelle), **D6** (Schnee war im Modell, nicht im Bild), **D7** (der MapLibre-Weg liegt fertig im Repo — `TerrainMap` + `CurtainLayer` —, ist aber ein anderes Produkt und WebGL; Empfehlung samt Wegevergleich), **D8** (der Ortsbezug bleibt der Cluster: flach EIN Cluster für die ganze Strecke); Plan G1–G5, Entscheidungen H1–H3 mit Defaults, Umsetzung §20 mit Gate **GR3D-5**, den Browser-Tabellen und **§20.4 drei Befunden aus dem laufenden Bild** (ein Flag für zwei Vorgänge, das Relief kommt später als die Szene, und die Messfalle „alle Segmente statt des untersten je Spalte" = §18.5 in anderer Gestalt). Davor **§17/§18 R3D-3 — Phase 1c „Go / No-Go" (2026-08-29):** Elementtabelle der Vorlage je Breakpoint, Datenlage je Grenzwert, Befunde **C1** (keine Sichtweite im ganzen Repo), **C2** (das bestehende `src/threed/goNoGo.ts` beantwortet eine andere Frage — ein Anker/eine Zeitachse/ein Grenzwert/zwei Zustände gegen Strecke/wandernde Position/mehrere Grenzwerte/vier Zustände), **C3** (der vierte Status ist ein seit 1a gegebenes Versprechen; Rangfolge `no-go > unklar > knapp > go`), **C4** (die Startsuche darf die unkorrigierte Böe nicht benutzen — `correctForElevation` im Kandidaten; Warnungen bleiben ungeprüft und werden benannt), **C5** (Grenzwerte in `localStorage`, Startwerte nicht je Bewegungsart), **C6/C7** (Druck statt PDF-Paket, Text statt Link), **C8** (1b-Aufmerksamkeitsmarken ≠ 1c-Entscheidungsgrenzen), **C9** (entschieden wird auf der Höhe, auf der der Nutzer steht — sonst zwei Zahlen für denselben Ort), **C10** (die Kantenregel wird mit 1b geteilt: `segmentEdges`); Plan 1c-1…5, Gate **GR3D-4** mit den Browser-Belegen (alle vier Zustände an echten Daten, C9 end-to-end nachgemessen), **§18.4 vier Befunde aus dem laufenden Bild** (zwei Karten aufeinander, Start-Beschriftung durch die Legende — auch in 1a, Fahne über „Ziel", „unklar" zeigte einen Messwert statt des Grundes samt der Grammatik-Nennform) und **§18.5** die zwei Mess-Lehren (`getBBox()` ignoriert `transform`; Negativ-Sonden brauchen Wortgrenzen). Davor **§15/§16 R3D-2 — die vier Restposten vor 1c (2026-08-28):** Messaufbau und Long-Task-Tabelle am Prod-Build (ungedrosselt und CPU 4×), die Sechs-Konfigurationen-Gegenprobe, die V-R3D-2 schließt, die Bild-für-Bild-Messung des 2D-Rückfalls (`.r3-root` weg nach 20 ms, MapLibre bei +51 ms), die Speicherwege-Tabelle (localStorage/sessionStorage/IndexedDB an `MAX_TRACKPOINTS` gemessen) mit den drei Auflagen (Notiz + „verwerfen", erst ab der Planung, Startzeit über `horizonState`), Plan R1–R4, Gate **GR3D-3** und **§16.4 die fünf Funde der nachgeholten Sichtprüfung** (Kantenlabel je Modus statt je Breite, belegte Kästen für „abgeleitet", `layoutCards` klemmte nach dem Vergleich, „ohne Daten" gegen „trocken"/„keine Warnung" auch in 1a, 44-px-Anfassfläche ohne Bildänderung). Davor **§13/§14 R3D-1b — Zeitkorridor (2026-08-28):** Diagnose der Vorlage 1b (Elementtabelle je Breakpoint, Datenlage je neuem Element), Befunde **B8** (keine Niederschlagswahrscheinlichkeit in der Punkt-Kette), **B9** (Radar-Vorlauf je Land, CH = null, nicht durchgereicht), **B10** (Zuggeschwindigkeit nicht ableitbar, das Regenfenster schon), **B11** (Startzeit-Vergleich kostet nichts — aber nur innerhalb der Anreicherung), **B12** (Startzeit ändert die Fahrzeiten), **B13** (die Kopplung dreht sich nur um); Plan 1b-1…5, Gate **GR3D-2** mit den Browser-Belegen, **§14.4 sieben Befunde, die erst das laufende Bild zeigte** (Windwand ohne Schalter, km ≠ px bei Kartenabständen, Zeichen-Auflage ≠ fachliche Regel, Doppelspalte, Fenster mit Ausdehnung null, ein fehlender Wert kippt die Empfehlung, „ohne Daten" ≠ „trocken") und **§14.5** die Verifier-Lehre `codeOnly`. Davor **R3D — 3D-Ansicht „Wetter entlang der Route“ (2026-08-28):** Diagnose R3D-0 (was die drei Vorlagen 1a/1b/1c wirklich zeigen — **kein** Kartenbild; Bestandsmatrix `src/threed` × `src/route`; Datenlage je Layer mit den vier Lücken; die Befunde B1 Cluster-Auflösung 6/10/14 km statt „2 km“, B2 Quellen-Stack je Land, B3 keine Persistenz in `src/route/`, B4 Pfadregeln, B5 „Depth-Test“ ist ein Ergebnis, B6 kein Verifier für Schnitt/Tour, B7 der 2D-Rückfall braucht einen Anlass), Wegevergleich SVG-Axonometrie ↔ MapLibre-Terrain, Entscheidungen E1–E7, Umsetzung §12 mit Gate **GR3D-1** und den zwei Bau-Fallen (Label in der Verdeckungsmaske, Lazy verschiebt geteilte CSS) |
| `audit/layer-erstbild.md` | **§9 LE2 — H3 + H7 umgesetzt (2026-08-28)**: Parser herausgelöst (`incaParse`/`rzcParse`), `hdf5Worker` + `hdf5OffMain` (Rückfall, Kill-Switch), Prioritätstabelle je Abruf, A/B-Tabellen ungedrosselt/CPU 4×/Fast 4G/Slow 4G (Regenradar DE/AT, Wetterkarte Niederschlag), der Befund „`priority` verteilt keine Bandbreite über Hosts" mit Zahlen, Gate GLE2, V-LE-12…17 (H7b Sequenzierung = Jans Entscheidung). Davor **§8 LE1 — H2 umgesetzt (2026-08-28)**: Frühstart der Manifeste und des RV-Tars im Router (`prefetch.ts`, `liveManifest.ts`, `radolanRuns.ts`, `takeWarm…` in drei Verbrauchern), Shell-Preloads aus Vites Abhängigkeitsliste + `preconnect`, Vorher/Nachher-Tabellen (Wind −0,6…−1,2 s, Temperatur −1,9 s, Regenradar −1,8 s, Niederschlag −0,3…−0,5 s lokal), CSS-Preload-Falle (`crossorigin`), V-LE-11 (Kern-Kontention), Gate GLE1. Davor Diagnose **LE0** (2026-08-28, Jans Auftrag „Layer-Darstellung nochmals beschleunigen — Regenradar und Wetterkarte, Verbesserungen recherchieren"; **kein Code**): Zeitleisten in Prod je Seite und Layer (Wetterkarte Wind 4,2 s · Temperatur 18,5 s · Niederschlag 11–12 s; Regenradar DE 8–12 s mit warmem Tar), CPU-Kosten je Baustein (WASM-bz2 1,3–1,9 s · pure-JS 33 s · Tar-Dekode 0,5–0,8 s · INCA-Parse 1,0–2,5 s · DEM-Bau 2,9–3,4 s · Index-Map 0,18 s), vier Ursachen (serielle Ketten mit spätem Start, Arbeit vor dem Erstbild, Hauptthread-Blockaden, Netz), Hebel H1–H10 mit Wirkung/Aufwand/Gate (H1 vorgerechnetes DEM-Bild, H2 Frühstart + modulepreload, H3 HDF5 im Worker, H4 eigenes Land zuerst, H5 RV-Kette, H6 V-WF-10, H7 Prioritäten), V-LE-1…10, Entscheidungen §7. Messfallen: Resource-Timing-Puffer 250, initScript greift auf Prod nicht, zweiter Tab hält den RV-Tar warm, vergessene Dev-Server mit 100 % CPU |
| `audit/bandbreite.md` | **§30 BW-11 Warum die BW-10-Korrektur die Bestandsbrowser nicht erreichte (2026-08-26)**: Jans „immer noch der alte Lauf“ am Beleg zerlegt — Manifest UND Daten-Repo standen auf 00z, ein frischer Browser zeigte 00z, ein Bestandsbrowser 21z; gemessen: der v4-Worker stand als `installed` (waiting), der ALTE bediente weiter und hielt `/latest-{grib,wind}.json` in `bsc-assets-v2`. **Lehre V-BW-44:** `skipWaiting()` im `install` ist eine Bitte, kein Vollzug — eine Korrektur IM Service Worker ist nicht ausgeliefert, wenn sie deployt ist, sondern wenn der Worker **aktiviert** ist. Umgesetzt: `SKIP_WAITING`-Nachricht + `controllerchange`-Reload (einmalig, an `hadController`), und `liveManifestUrl()` hängt einen Minutenstempel an den ABRUF-URL der Live-Manifeste (Pfad unverändert ⇒ `LIVE_RE` greift weiter) — damit hängt die Frische an keinem Worker mehr. Am Nachbau des alten Workers belegt: ohne Stempel Lauf 2026082609 bei Serverstand 2026082612, mit Stempel 2026082612. V-BW-45: `activate` löscht auch `icon-d2-grib-decompressed-v1`. Davor **§29 BW-10 Erstbild schneller (2026-08-26)**: warum der Repack nicht schneller war (GBW3/4-Messung, Decode war nie kritisch, jsDelivr kalt, BW-9-Index vor dem ersten Bild), die SW-Endungsfalle mit Live-Beleg (Manifest der vorigen Sitzung, 9 h), Concurrency-Messung 6/12/13 und die Fristen-Wechselwirkung (Bytes in der Luft ÷ Bandbreite), Hebel S/1/2/3 umgesetzt, Gate GBW10, V-BW-42/43. Davor **§28 BW-9 Repack-Kette schneller (2026-08-25)**: DWD-Veröffentlichungszeiten je Lauf gemessen (Schritt 000 bei + 44 min, komplett + 81 min), Kette Lauf + 105…135 min zerlegt (Cron-Jitter, Producer, Warm-Cron + Netlify-Build), jsDelivr-Messungen (404 ungecacht, neuer Pfad ≈ 35–57 s nach Push, Purge-API), Hebel B1/V1/S1 umgesetzt, Befund V-BW-38 (live `build.yml` im Daten-Repo veraltet ⇒ stündliches Neurechnen). Davor **§27 BW-8 Plattform-Inventar (2026-08-25)**: alle Routen auf Netlify-Restposten geprüft (Sondierung 48,5 MB je Stundenschritt, Höhenwind 28,9 MB je Ebene, Modell-Umschalter, kein `_headers`, SW-Endungsfalle, FIRMS 503), Hebel H1–H14, **H13 umgesetzt** (Gate GH13 §27.7: Gewitter/Blitz/Schnee/Rotation laden das Jetzt-Bracket, 76 → 16 Abrufe je Aktivierung, `verify:repack` 249/249). Davor: Diagnose der **Bandbreiten-Linie** + Gates **GBW0…GBW4** (2026-08-22 bis 2026-08-24, Anlass: Netlify `usage_exceeded`): Messung je Ansicht und Posten (§2), Rückzug des Cache-Wärmens (§18, ~123 GB/Monat → 0), BW-P/BW-0 (§19: Browser-Rundlauf verlustfrei belegt, **bevor** gebaut wurde — und der Feuerwetter-Rückzug), BW-1 (§20: Producer + Byte-Identität), BW-2 (§21: Ablage, jsDelivr, Commit-SHA statt Tag, drei Betriebsfallen — Force-Push ohne `ls-remote`-Wächter, „liegt schon" ≠ „liegt vollständig", `raw.githubusercontent.com` zeitweise unlesbar), BW-3 (§22: der Client, die Asymmetrie Grau+Alpha ↔ RGB, die zwei Fristen, „kein Abschnitt" als Normalfall), **BW-4** (§23: default-on, Messung 7,42 → 1,24 MiB eigener Origin, Kill-Switch am laufenden Kartenzustand) und **BW-5** (§24: Regenradar — Messung je Land vor dem Bau, die widerlegte Annahme „ICON-D2-frei", gerechnete RV-Zeitstempel mit gemessenem Verzug, Rückblick auf Abruf, Entdopplung mit der Abbruch-Falle, Gate GBW5). Enthält den V-Katalog **V-BW-1…28** (§12) und **BW-6** (§25: Inventar aller Layer mit Feld→Textur-Spalte, gemessene PNG-Größen je Familie, vier Fallen, Plan 6a–6e, Umsetzung, Gate GBW6). Verifier `verify:repack` und `verify:radar-runs`, ADR **D-31** |
| `audit/event-zone.md` | Diagnose **EZ0** + Umsetzung **EZ1–EZ3** + Gate **GEZ1** (2026-08-25, Jans Auftrag „neben dem Standort eine Zone aufziehen, um eine bessere Aussage treffen zu können“): Event-**Fläche** als aufgezogenes Rechteck auf einer Lazy-MapLibre-Karte in Schritt 1 des Wizards (`EventZoneMap.tsx`, kein Draw-Plugin — D-06), Modell `src/event/eventZone.ts` (DOM-frei), Abtastung der Ecken am besten Tag über dieselbe Punktforecast-Pipeline (`eventZoneScan.ts`, Muster `eventAltLocation`). **Vor dem Bau gemessen (V-EZ-1):** die Zone trägt nur im Gelände — Spanne über 8 km Flachland **0,16 K**, über 6 km alpin **3,82 K**; deshalb ergänzt die Zone eine **Spanne** und ersetzt den Punktwert nicht (E3), und bei zu kleiner Spanne sagt die App ausdrücklich, dass die Quellen die Fläche nicht auflösen. Lehren: **V-EZ-3** vier Ecken parallel = HTTP 429 bei GeoSphere (jetzt nacheinander mit Pause), **V-EZ-4** `touchend` trägt keinen Finger mehr — übernommen wird das zuletzt gezeichnete Rechteck, nicht ein aus dem Ereignis gerechnetes, **V-EZ-5** Long Tasks beim Ziehen sind im Automations-Browser nicht entscheidbar (rAF-Takt **1004 ms** gemessen) — Vorschau trotzdem auf ein Bild koalesziert. Verifier `verify:event-zone` 41/41 |
| `audit/event-terrain.md` | Diagnose **ET0** + Umsetzung **ET1–ET5** + Gate **GET1** (2026-09-01, Jans Auftrag „Terrain-Ansicht der ausgewählten Fläche + mehrere wetterabhängige Informationen übersichtlich; sei ein professioneller Eventplaner"): Bedarfsanalyse N1–N12 mit Machbarkeits-Urteil je Bedarf (§3 — die Terrain-Ansicht löst Verortungs-/Erklärungs-Needs; neuer Informationsgewinn sind Gelände-Kennzahlen und Sonne/Horizont), vier tragende Befunde (§2: der Zonen-Scan verwarf das bezahlte `DaySummary` je Ecke; `terrainPhysics.ts` rechnet Kaltluftsee/Hangexposition schon in die Scores; die Terrain-Technik lag fertig im Repo; die Pflicht-Fläche war im Ergebnis unsichtbar und jenseits 28 h gab es gar keine Karte), Ehrlichkeitsgrenzen (§4: Hillshade = Beleuchtung ≠ Schattenwurf, Modellwind ≠ Umströmung, DEM ~30 m ohne Bebauung, Uniform-Zone bleibt uniform), Wegevergleich (§5: Ergebnis-Bühne statt CurtainLayer — eine Fläche hat keine Schnittlinie). Umsetzung §9: `eventTerrain.ts` (pur: Wind-Helfer, Zonen-Raster ≤ 64, Horizont 64×24 mit Erdkrümmungsabzug, `sunBehindRidge` mit Grat-Schwelle 1°), `eventTerrainLoad.ts` (je Aufgabe EIN `sampleElevations`-Batch, Kacheldeckel 16/32), lazy `EventTerrainMap.tsx` (BEARING 0 — die Ecken-Namen verlangen Nord oben), Ecken-Readout + Sonnen-Karten + Wizard-Umschalter (Zeichenkarte bleibt montiert, `map.resize()` beim Zurückschalten). Lehren: Windpfeil und Chip teilen die Koordinate (Chip-Offset, sonst verdeckt der HTML-Marker den Symbol-Layer); `resize_page` endet bei ~500 px `innerWidth` — Mobil nur per Viewport-Emulation mit Zusicherung; JSX bricht Knopftexte um (Whitespace-tolerante Textsonden). `verify:event-zone` **102/102** |
| `audit/brandradar-satellitenbilder.md` | **§12.10 SAT2h** (2026-09-03, Gate GSAT2h): V-SAT-18 umgesetzt — aber anders als skizziert. Drei Prüfstände zerlegen das Komposit (`dnbr-composite-bench.mjs` Bausteine, `dnbr-loop-bisect.mjs` Bisektion mit der alten Schleife als Gleichheits-Orakel und Modus `ONE=<Label>` für ein Isolat je Variante, Browser-Sonde zwischen `Uint8ClampedArray(1048576)` und `createImageBitmap`); Befund: `createImageBitmap` kostet 1,1 ms, die 28 ms sind die `number | null`-Union plus Array-Destrukturierung je Pixel ⇒ **kein Worker, sondern `NaN` statt `null` und typisierte Klassenspalten aus `DNBR_CLASSES`**; die NaN-Falle `!(d >= min0)` gegen `d < min0`; Negativ-Kontrolle (8 Sonden fallen) als Beweis, dass die Byte-Gleichheit Zähne hat; Mess-Lehren zu polymorphen Isolaten, Maschinenlast und der PowerShell-Mojibake-Falle; neu V-SAT-19 (Sampler-Phase ist der größte Rest). Davor **§12.9 SAT2g** (2026-09-03, Gate GSAT2g): V-SAT-17 umgesetzt — die Messung zuerst (Sonde über die `Float64Array(262144)`-Allokation: EIN Task = SECHS Sampler, NULL Komposite; Kontrolle `?wc=0` verteilt die Komposite von selbst), die eigene Skizze „ein Komposit je rAF“ dadurch widerlegt, Frame-Budget `WC_STARTS_PER_FRAME` nur beim gedämpften dNBR, 237 → 60–69 ms bei gleicher Fertigzeit, rAF-Bildlücken-Zähler als nötige zweite Sonde, V-SAT-18. Davor **§12.8 SAT2f** (2026-09-03, Gate GSAT2f): V-SAT-16 behoben — Ganzzahl-Kachelcode statt Namens-Strings in der Pixel-Schleife, Gleichheits-Orakel `wc-sampler-bench.mjs`, Bisektion der Schleifenkosten, zwei geprüfte Irrwege (Gitter-Verschmelzung LANGSAMER, Kehrwert-Multiplikation wirkungslos), V-SAT-17. Davor **§12 SAT2d** (2026-09-02, Gate GSAT2d): WorldCover-Landbedeckungs-Dämpfung — Sonden (COG = Leser-Zwilling, AWS-Bucket CORS-los ⇒ Planetary-Computer-Token-Weg, Börde-Kreuztabelle 13,4 % Acker-Fehlsignal, Narbe teils Klasse „Gras" ⇒ nie „nur Wald" dämpfen, utmInv 51,5 ms ⇒ Stützgitter), asymmetrieerhaltender Bau (halbe Deckkraft nur auf 40/50/60/70/80, Ausfall nimmt nur die Dämpfung), Canvas-Diff-Beleg (7,1 % blasser, Narbenkern 0 Pixel), V-SAT-15. Davor **§11 SAT2c** (2026-09-02, Gate GSAT2c): SCL-Maske am dNBR — Sonden (SCL = Leser-Zwilling; binäre Nachher-Maske löscht am Ereignis 100 % des Signals; Brandfläche selbst wird Klasse 5 „unbewachsen" ⇒ Landbedeckungs-Dämpfung widerlegt), asymmetrische Maske (Vorher hart, Nachher blasser, Zirrus läuft durch), fehlertoleranter Bau, V-SAT-14. Davor **§10 SAT2b** (2026-09-02, Gate GSAT2b): SWIR + dNBR — Sonden (Band-COGs uint16/Predictor-2-auf-Werten, Skala je Szene aus raster:bands, dNBR-Feld real gerechnet: Narbe 0,27–0,60 vs. Hintergrund ~0,05, Kachelgitter decken sich nur ebenenweise), Umsetzung, Gate, Befunde (Brandbeginn-Anker, rAF-ID-Reset, ehrliche SWIR/Ernte-Sätze, Mobil-Kollision), V-SAT-12/13. Davor **§9 SAT2a** (2026-09-02, Gate GSAT2a): eigener COG-Leser + 10-m-Viewer — Sonden (visual-Asset ⇒ CORS-offener `sentinel-cogs`-Bucket inkl. Range-Preflight; STAC trägt die Georeferenz; Earth Search projiziert Asset-Felder nicht ⇒ zweite Tages-Suche; 20-m-Kacheln real ~0,5 MB ⇒ ehrliche Knopf-Spanne), Umsetzung, Long-Tasks-0-Messung, V-SAT-8 zu / V-SAT-9…11 neu. Davor Diagnose **SAT0** (2026-09-01, Jans Auftrag „Satellitenbilder vor und nach den Bränden zur Begutachtung“ — **kein Code**): Kandidaten an den Endpunkten gemessen (GIBS-HLS-WMTS 30 m mit CORS `*` und Aufnahmetage-404-Falle, Worldview-Snapshot-API mit `Data-Present`-Header, Earth-Search-STAC für Aufnahmetage + Wolken-%, AWS-COGs 10 m per Range-Request, Copernicus-Browser-Deep-Link; verworfen CDSE/EOX/VIIRS/Esri), Augenschein-Belege Hürtgenwald (Narbe sichtbar, Wolkenproblem im Bild), Sichtbarkeitsgrenze kleiner Brände, Empfehlung SAT1 (STAC + 2 Snapshots + Deep-Link, 0 Netlify-Bytes, 0 Dependencies), Entscheidungen E1–E3, V-SAT-1…5; **§7 SAT1 umgesetzt** (Gate GSAT1: Modul + Komponente + beide Dossiers, Belege Hürtgenwald/Live/Data-Present, V-SAT-6/7) |
| `audit/brandradar-detail-mitte.md` | Diagnose **BD2-0** + Umsetzung **BD2** + Gate **GBD2** (2026-08-29): die Mitte des Brandradars umschaltbar „Karte \| Dossier" — Befunde B1 (SVG-Text ohne `font-family`), B2 (keine zweite `FireMap`: leichte Minikarte aus dem vorhandenen `footprintFc`), B3 (Hauptkarte bleibt montiert), B4 (`ds` im Permalink), B5/B6 (Mobil-Seite, Tablet-Rail mit ausgesprochenem Deckel); Entscheidungen D1–D6; §4.1 drei Befunde aus dem laufenden Bild (Warnungen kamen von der Hauptkarte, viewBox-Breite je Spalte, entzerrte Beschriftungen) |
| `audit/brand-detail.md` | Diagnose **BD0** + Umsetzung **BD1** + Gate **GBD1** (2026-08-25, Jans Auftrag „alle wichtigen Informationen in der Kachel, beim Klick alles übersichtlich — Zeitverlauf, Ursache, Wetterlage"): Kachel mit vier Kennzahlen (Fläche · Detektionen · Stärke · Tendenz, immer sichtbar), Detailkarte gegliedert (Kennzahlen · Verlauf · Wetterlage · Einordnung & Bestätigung · Merkmale), **FRP-Verlauf je Überflug** als SVG (`FirePassChart.tsx` auf `detail/passTimeline.ts`: log-Achse, Lücken > 6 h schraffiert, kein interpolierender Linienzug), **Wetterlage am Brandort** (`detail/fireWeatherAtPoint.ts`: DWD ICON über die Open-Meteo-Vorhersage-API mit `past_days` — der Brandradar hat selbst keine Vergangenheit; Stunde der Erstdetektion, letzte Detektion, jetzt, Brandtag, 24 h davor, Tage seit Regen; Modellwerte, so beschriftet; nur auf Klick, kein Netlify-Traffic) und die ehrliche Zeile **„Ursache: keine Quelle"** (kein Anbieter liefert sie je Brand; Einordnungshilfen mit Herkunft). Verifier `verify:fire-detail` 44/44 (Verifier-Zahl 53) |
| `audit/regenradar-layer-angleich.md` | Diagnose **RL0** + Umsetzung **RL1** + Gate **GRL1** (2026-08-25, Jans Auftrag „Regenradar: dieselben Niederschlags-, Zellbahnen- und Schnee-Layer wie die Wetterkarte", Ausnahme vom Funktionserhalt für Eigenverfolgung/Radar-Phasen-Schnee/Einzelland-Raster): Niederschlag = `PrecipCompositor` mit dem Landes-Stack als Ein-Frame-Quelle (Rückblick + Morph bleiben, Nachbarn nur ab „jetzt"), Zellbahnen = DWD KONRAD3D über das **neue geteilte Modul `src/radar/cellLayers.ts`** (+ `cellPopup.css`, aus MapView herausgelöst — 1:1 heißt importieren), Schnee = ICON-D2 `ScalarLayer` mit Decke\|Neuschnee im Deck. Lehren: Nachbarquellen nie für Vergangenheitsframes (sonst still falsche Zeit); `setData`/Lerp in Effekten, die je rAF laufen, nur bei Änderung; INCA liefert zeitweise 0 Frames (V-RL-2); der liberty-Stil wirft `found null` (V-RL-3); einmalige WebGL-Attribut-Warnung im `ScalarLayer` (V-RL-1). **§7 (Jans Freigabe):** alle vier V-RL abgearbeitet, Long Tasks beim Abspielen 0/24 s, Steckbrief belegt |
| `audit/routing.md` | Diagnose **RT0** + Umsetzung **RT1** + Gate **GRT1** (2026-08-22): pfadbasiertes Client-Routing mit React Router 7.18 statt Hash-Fragmenten — Routen-Tabelle, Query-Schema der Wetterkarte, Legacy-Migration, netlify.toml-Regeln, Route-Shells, Bundle-Zahlen vorher/nachher, Browser-Belege (kein Remount, pushState nur bei Layer-/Feature-Wechsel, Back/Forward), die Spec-Abweichungen (Trailing-Slash nur clientseitig, flache Shells, kein `/atmosphaere/himmel`) und der Prerender-Vorschlag |
| `audit/brand-historie.md` | Diagnose **BH0** + Plan **BH1–BH6** + Gate **GBH1** (2026-08-22): Brand-Historie über vier Zeitfenster (24 h · 7 d · Monat · Saison 1.3.–31.10.) aus dem FIRMS-Archiv. Bestandsmatrix Konzept ↔ Code (§2), zehn Widersprüche W1–W10 (§3: kein R2, kein Workflow ohne Freigabe, Live-Pfad bleibt, „Wetterlage am Brandtag" nur über Meteostat/DWD + ERA5, ID-Stabilität bei SP-Nachlieferung, Listen-Deckel V-246), sieben Entscheidungen mit Defaults (§4, von Jan freigegeben), Phasen (§5), Messwerte 2020–2026 und Parität mit der Brände-Liste (§6). Lehre V-BH-1: Brände-Liste ohne Zeitlücke vs. Ereignis mit 48 h |
| `audit/thermalanomalien.md` | Diagnose **TA0** + Umsetzung **TA1–TA5** + Gate (2026-08-22, Jans Auftrag „Hotspots in Vegetationsbrände vs. persistente Anlagen-Signaturen trennen + Reiter „Thermalanomalien“"): **73 %** aller Archivdetektionen sind NASA `type 2` (statisch) in nur 362 Zellen, aber 197 Dauerzellen tragen `type 0` ⇒ eigene Persistenzregel **≥ 2 Kalenderjahre mit je ≥ 5 Detektionstagen** + EFFIS-Veto (469 Zellen, Jüterbog 2022/2023 erfüllt sie NICHT) ⇒ **218 Standorte** (A benannt 145 · B unbenannt 8 · C Tagessignal 65) mit Geodaten-Join ≤ 1,5 km gegen **E-PRTR v16 (EEA, CC-BY 4.0, DE/AT/CH)**, MaStR (DL-DE/BY-2.0), BFE (OPEN BY); **EOG Nightfire blockiert** (Kommerz-Verbot), OSM nicht (ODbL, Entscheidung 2026-08-15), R2 nicht (Entscheidung 2026-08-19), kein Workflow (STOPP). Laufzeit-Signaturvergleich (`src/fire/anomaly/classify.ts`: Raster ± 1 Zelle, `grewFromPasses`, FRP ≤ 1,5 × Archivmax, keine EFFIS/EMS) ⇒ `FireRecord.anomaly` `site` (grau, Reiter) / `site-deviating` (bleibt Brand, Abzeichen „Abweichung", in beiden Listen). Layer `fireAnomalies` **Bit 15**, Z 79, Rauten-Sprites; Permalink `ta`; Kill-Switch `?ta=0`; Batch `scripts/fire/ta/*.mjs` (lokal, Archiv 2020–2026 ganzjährig nachgeholt), Datei `public/fire/ta/thermal-sites-v1.json` (36 KB gzip). Lehren: (1) die nächste Anlage ist oft die Nebeneinheit — Rang braucht Größe; (2) Werksart = schwerste Installation, nicht die zuletzt gemeldete; (3) Tagessignal bekommt keine Anlage; (4) E-PRTR-CSV führt keinen Betreiber; (5) FIRMS-API liefert transient HTTP 400 (Wiederholung hilft); (6) der Leuna-Brand 2023 neben der Raffinerie wird ohne EFFIS-Wissen über Raster + Wachstum gehalten. Verifier `verify:fire-anomalies` 56/56 (Verifier-Zahl 56), Budget 929,9/934,0 KB |
| `audit/aktivfeuer.md` | Diagnose **AF0** + Entscheidungsprotokoll §10 + Gates **GAF1** §11 / **GAF2** §12 / **GAF3** §13–14 / **GAF4** §15 (2026-08-18): das Konzept `konzept-aktivfeuer-modul.md` (Python, Objektspeicher, stündlicher Cron, Alpha-Shape) am Code widerlegt — AF ist eine reine TS-Erweiterung der BP1-Registry, clientseitig; ~70 % existierte schon (Überflüge, Rechteck-Union, Hülle, `statusOf`, `grew`). Lehren: (1) **eine** Überflug-Regel im Projekt (10 min je Satellit, `src/fire/activity/overpasses.ts`) — wer sie ändert, ändert `overpasses`/`eventLabel` sichtbar, F2 nicht; (2) Namenskollisionen benennen statt umdeuten (`frpSumMw` = Fenstersumme bleibt, Überfluggrößen heißen `frpLastPassMw`/`frpMaxPassMw`); (3) FRE nur mit Gültigkeitsregel, `null` heißt „nicht bestimmbar", nie 0; (4, GAF2) „wachsend" braucht FRP-Anstieg **und** Randwachstum (reiner FRP-Anstieg kann Blickwinkel sein), „kein Signal" trägt immer eine Beobachtungsqualifikation (Proxy: regionale Sicht ≤ 150 km, der eigene letzte Überflug zählt nie als „späterer"), das Windflag ist ein Flag mit `null`-Grund, nie eine Korrektur; (5, V-AF-7) eine „nur bei kein Signal"-Abfrage ist im 7-Tage-Fenster die Regel (≈ 90 % der Einträge) — Long Tasks am Prod-Build messen, nicht schätzen; (6, GAF3) das Merkmalsschema wird VOR der Zielgröße fixiert (Detektionen fallen nach 7 Tagen aus dem Fenster), `coverageHa` ist fensterabhängig (Baelen: 2,1× EFFIS über 7 Tage, BF0 sah 0,5–0,6× über 24 h), Perf-Anker als „bester von 3 Läufen" (V-AF-10); (7, GAF4) das Archiv liefert die Labelpaare **heute** (EFFIS-RDA seit 2020/21 Sentinel-2-gestützt bis 0–2 ha; FIRMS-SP mit demselben Schlüssel, lokal, 5-Tage-Chunks) — Detektionen schlagen FRE als Prädiktor, Modellwahl nach gemessener LOO-Streuung statt Konzept-Vorannahme; Residuen **nach Prädiktorklasse** prüfen (nach Zielgröße klassiert erzeugt der Regressionseffekt ein Scheinbild), Grad per t-Test statt LOO-Heuristik, **Monotonie als fachliche Nebenbedingung** (mehr Feuer ⇒ nie weniger Fläche), Ausschlüsse größenabhängig (0–2 ha 48 %, > 200 ha 25 %) und die Batch-Zuordnung bleibt exakt die des Clients (sonst trainiert man auf Paaren, die im Betrieb nie entstehen); Bash-Heredocs verdoppeln Backslashes nicht sauber (Patch-Skripte per Write-Tool), `npm run … --` reicht unter PowerShell keine Flags weiter (Skripte direkt mit `node`). Enthält Mapping-Tabelle Konzept→`FireRecord`, Phasenplan AF1–AF4 mit Aufwand, offene Fragen mit Defaults, 15 Konzept-Irrtümer mit Beleg |
| `audit/brandflaechen-panel.md` | Diagnose **BP0** + Gate-Protokoll **GBP1** (2026-08-17, §9): Brandflächen als Polygone + linkes, ausklappbares Panel im Brandradar. Drei Lehren aus dem Bau: (1) mehrere Cluster in EINER Kartierung sind EIN Brand — die Registry verschmilzt sie (Hohes Venn, 3 × „2 825 ha" live gesehen); (2) Raster und Registry-Fläche gleichen gegen verschiedene Flächenmengen ab (gezeichnete vs. geladene) — die Registry-Fläche **vertritt** die Zone auf der Karte (`mapZones`), sonst zwei Formen; (3) ein geteilter Lade-Cache darf nicht am Abbruch-Signal des ersten Aufrufers hängen (Reacts doppelte Dev-Effekte). Enthält Einhängepunkte mit Datei:Zeile, den Datenweg-Entscheid (Live + EFFIS clientseitig; eigene Sentinel-2-Detektion **nur** als GitHub-Actions-Batch mit Commit-back nach `public/` — **im Repo gibt es kein R2/PMTiles**), das `FireRecord`-Schema, den Vorschlag einer überflugstabilen Brand-ID (Anker = älteste Detektion), den Lizenzcheck (FIRMS + Nominatim blockiert nach den neuen Constraints) und den Plan BP1–BP4 / BA-P1–P5. Kickoff: `prompt-brandflaechen-panel.md` |
| `audit/brandflaeche-vorlaeufig.md` | Diagnose **VB0** + Gate **GVB1** (2026-08-19): Kann die App eine eigene Brandfläche zeichnen, solange EFFIS noch nicht kartiert hat? **Gemessen: nein.** 618 Archivpaare (EFFIS-Kartierung × FIRMS-Detektionen 2020–2026, netzfrei aus dem AF4-Cache, Paarzahl reproduziert AF4 exakt), vier Formregeln gegen die Nullhypothese „Detektionsraster": keine schlägt sie (IoU-Median 0,095 gegen 0,092/0,088/0,090). Schrumpft man das Raster auf die AF4-Schätzfläche, wird die **Größe** richtig (Verhältnis 1,03 statt 6,10) und die **Lage** falsch — der Anteil Formen ohne jede Überlappung steigt von 1 % auf 24–36 %. Drei Ursachen: 85,4 % aller Fälle sind **Sub-Pixel** (Schätzung kleiner als ein VIIRS-Pixel), der Schwerpunktversatz Raster ↔ Kartierung beträgt **rund ein Pixel** (Median 261 m: VIIRS sieht die Front, EFFIS kartiert die Narbe), und der AF4-**Punktwert regressiert zur Mitte** (7,45× bei 0–2 ha, 0,17× bei > 200 ha — das Intervall trägt, der Punktwert allein nicht). Umgesetzt wurde deshalb nur die Sprache an der vorhandenen Form: **„Vorläufige Brandfläche (geschätzt)"** mit Intervall und Einschluss-Satz, wortgleich in Panel-Detailkarte und **beiden** Karten-Steckbriefen (vorher stand die Schätzung nur im Panel). Lehren: eine Formregel wird an der Zielgröße gemessen, bevor sie gezeichnet wird — ein Abbruchkriterium vorab schützt vor der hübschen falschen Form; und eine Auskunft, die es nur an einer von zwei Stellen gibt, ist ein Widerspruch, kein Detail. Skript `scripts/fire/geometry-eval.mjs`, Ergebnisse `data/fire/vb/geometry-eval.*` |
| `audit/radar-punktverortung.md` | Diagnose **RP0** + Gates **GRP1** (DE, 2026-08-19) und **GRP2** (AT + CH, 2026-08-20): Karte und Punktabfrage des Regenradars beschrieben verschiedene Orte. Alle drei Landesgitter sind projiziert, keines ist in lon/lat regulär — die Punktabfrage interpolierte aber linear zwischen den vier Geo-Ecken und griff dadurch **DE 13–36 km** (Median 24, systematisch nach Norden), **AT 6,7 km** (Median, max 11,0) und **CH 10,3 km** (Median, max 11,1) daneben; an echten Daten kippte das bei 21,7 % von 700 DE-Städten die Aussage „regnet es hier“ (Slider „trocken“, während die Karte Regen zeigt — Jans Beobachtung). Behoben: je Quelle EIN Geo-Modul (`radolanGeo.ts` polar-stereografisch · `geosphereIncaGeo.ts` Lambert EPSG:31287 auf WGS84 · `meteoSwissGeo.ts` LV95/somerc), Punktabfrage über die Fassade `src/pointForecast/radarSample.ts` (`projectionFor` + `anchorFor`), Warp-Meshes für AT/CH im RainLayer und `GridKind` statt `ps: boolean` im DACH-Komposit der Wetterkarte. Ergebnis: DE 700/700, AT 51/51, CH 133/133 Orte wertgleich mit der Karte; Verifier `verify:radar-sampling` 25/25. Lehren: (1) Rendering und Punktabfrage desselben Feldes müssen **denselben** Verortungscode benutzen, sonst beschreibt der Text einen anderen Ort als das Bild; (2) ein Genauigkeits-Kommentar („Sub-Pixel“) ist eine Behauptung, bis sie an der Zielgröße gemessen ist — die Widerlegung („bis ~40 km“) stand zwei Dateien weiter im selben Repo; (3) die Projektion muss man nicht raten: **rzc nennt sie selbst** (`/where.projdef`), **INCA liefert die Zellkoordinaten**, an denen jeder Kandidat prüfbar ist; (4) die zweite Fehlerhälfte ist die Frage, **was die vier Ecken bezeichnen** — Zellmitten oder Außenkanten (eine halbe Zelle); (5) `regular-lat-lon` (ICON-D2) ist der einzige Fall, in dem die 4-Eck-Inverse exakt ist |
| `audit/karten-layer-verortung.md` | **§15 KL9 „±0 km" (2026-08-26):** Fehlerformel e = Δφ²·tan φ/8·R, Fehlerkurven je Mesh und Unterteilung (§15.3: lat/lon nur Zeilen, projiziert N²), Baukosten-Falle 211 ms und Lösung 64² + bikubisch (§15.4), Plan KL9a–g, Gate GKL9 (§15.6: alle Meshes ≤ 1 m, 46/46) und **Befund B11:** die GPU rechnet `log(tan())` im Vertex-Shader mit bis 280 m Fehler — der Rest ist nicht mehr das Mesh, sondern die Shader-Transzendente; **§15.7 V-KL-3 umgesetzt** (Jans Go 2026-08-27): `a_merc` aus `mercatorOf()` in sechs Vertex-Shadern, `highp`, Browser ≤ 6 m auf fünf Breiten; **§15.8 KL10:** Wind-Partikel über `mercYTable`/`mercYOf` (Tabelle statt log/tan, GPU-Messung max 2,6 m); **§15.9 KL11:** B5 behoben — `src/ml/coarsen.ts` flächengewichtete Blockung (Dehnung 3,96 km → 0), 76/76; **§15.10 Große Verifikation:** 759 Orte headless + 14-Punkte-Raster im Browser (≤ 10 m alle Zeichenpfade), INCA/rzc nur headless (im UI unerreichbar), Tabellen je Layer (V-KL-4 Real-Device, B2, V-KL-5 offen). Belege `karten-layer-verortung/kl9-mercsim.mjs`, `kl9-bicubic.mjs`. Davor **§14 KL8 Mercator-Platzierung (2026-08-26):** Befund B8 — der Niederschlag lag 29 km zu weit nördlich, die Zellbahnen stimmten; Ursache 4-Eck-Quad linear in Mercator vs. Textur breiten-linear; B9 Wolken rechnerisch 66 km; B10 Mesh-Layer ≤ 2,4 km (offen, V-KL-1); Lehre: ein Warp-Mesh hat ZWEI Aufgaben (Projektion aufheben UND unterteilen) — ein regulärer lat/lon-Frame braucht die zweite trotzdem; Verifier prüften nur den uv-Raum (V-KL-2, jetzt Mercator-Anker). Davor: Diagnose **KL0** (2026-08-21, Jans Frage „kommt derselbe Fehler auch in den Wetterkarten-Layern vor?“): alle 19 Layer auf **Projektion** und **Eck-Konvention** geprüft, Versatz an 759 DACH-Orten gemessen. Die Projektionshälfte ist repo-weit abgesichert — der GRIB-Decoder nimmt nur GDT 0 und GDT 101 an (`gribDecode.ts:230`), und alle Switcher-Modelle liefern Punktlisten in die Fusion. Die **Eck-Konvention** ist es nicht: ICON-D2 liefert Außenkanten, die Fusion Zellmitten, und beide gehen durch denselben Shader. Sechs Befunde: **B1 (schwer)** `confidence` im PoP-Modus gibt die polar-stereografischen DE1200-Ecken als achsparalleles lon/lat-**Rechteck aus nur NW und SE** an den `ConfidenceLayer` ⇒ **Median 76,7 km, max 93,7 km** (DE) — schwerer als der Radar-Fehler, der die Linie ausgelöst hat; Kontrolle mit demselben Rechenweg auf `poprob` (mit `de1200WarpMesh`) 0,02 km. **B2** der IDW-Rasterer (nur bei explizit gewaehltem EXTERNEM Modell; das hauseigene „Buscosun Fusion“ ist am 2026-08-22 aus dem Katalog geloescht, der Rasterer selbst nicht) legt seine Zellen auf `i/(cols−1)`, der Shader liest sie als Außenkanten ⇒ Karte 3,1–9,2 km daneben (Punktabfrage richtig). **B3** ICON-D2-Subsampling nimmt den ersten Punkt jedes 2×2-Blocks, gezeichnet wird die Blockmitte ⇒ **konstant 1,11 km zu weit nördlich**; zusätzlich Karte ↔ Punktabfrage bis 1,42 km (betrifft die Stadt-Temperatur-Labels, deren Kommentar „matches the colour of the underlying heatmap pixel“ damit falsch ist, und die Schneefallgrenze). **B4** DACH-Komposit 0,5 km. **B5** `coarsenFrameU8` deckelt mit `floor(w/8)` und lässt 4 der 1100 RADOLAN-Spalten fallen, gezeichnet wird über die vollen Ecken ⇒ `flownowcast`/`poprob`/`confidence` nach Osten gedehnt, bis 4,0 km. **B6** das DEM des Temp-Layers tastet auf Zellmitten ab, der Shader liest es mit derselben `uv` wie die Werte-Textur (Außenkanten) ⇒ die Höhenkorrektur je Pixel greift bis 1,2 km daneben — der einzige Befund, der nicht die Position, sondern den **Wert** ändert (Wirkung = Geländerelief über diese Strecke, im Hochgebirge bestimmend, nicht gemessen). Lehre: der Radar-Fehler war nicht „RADOLAN ist projiziert“, sondern **es gibt keine Stelle, die weiß, was die vier Zahlen bedeuten** — `radarSample.ts`/`GridKind` sind diese Stelle nur für die Radar-Kette; für die übrigen Layer ist `lngToEquiX` in zwölf Dateien kopiert und die Konvention nirgends geschrieben. Umgesetzt am 2026-08-22 (Gate **GKL1**, §12): EINE benannte Stelle für die zweite Frage — `subsampledCorners(f, ss)` neben `gribCorners` in `gribDecode.ts` („wo liegt das Gitter“ vs. „wo liegen die Werte, die im Bild stehen“) plus `texelCoord(uv, n)` als die eine GPU-Konvention; neun Raster-Quellen, drei Wind-Pfade und fünf Punktabfragen darauf gezogen, DEM auf Zellmitten, `GRID_GEO.lonlat` auf `edge: true`, `coarsenFrameU8` auf `ceil`, und der PoP-Schleier per `buildIndexMap(…, 'radolan')` auf ein reguläres lon/lat-Gitter umgetastet — **ohne Shader-Änderung**, damit die STOPP-&-FRAGEN-Regel nicht fällt. Verifier `verify:layer-geometry` 15/15 (u. a. Rundlauf Karte→Punktabfrage 8,5e-14 Texel, Schleier max 5,42 km statt 76,7 km Median). **Nachmessung je Layer §13** (2026-08-22, Jans Nachfrage; `audit/karten-layer-verortung/nachmessung.mjs` importiert die echten Module): 17 der 19 Layer zeichnen jeden Wert exakt auf seinem Abtastpunkt (≈ 0 km, vorher 1,11 km N bzw. 4,60 km ICON-EU), Karte ↔ Punktabfrage überall 0,00 km. **B5 ist NICHT behoben** — `ceil` statt `floor` beseitigt zwar den Datenverlust (4 von 1100 RADOLAN-Spalten), aber nicht die Dehnung: `1100/8 = 137,5` kachelt die Domäne mit ganzzahligen Blöcken nie glatt, das Vorzeichen dreht sich nur (vorher +3,99 km, jetzt −3,96 km; an Orten Median 1,64 km) — betrifft `flownowcast`, `poprob` und den PoP-Schleier. Zwei exakte Wege: flächengewichtete Blöcke in `coarsenFrameU8` (empfohlen) oder `FLOW_FACTOR` 8 → 10 (1100/10 und 1200/10 gehen beide glatt auf). Zwei Nicht-Befunde sind dokumentiert, damit sie nicht wieder als Fehler gelesen werden: „3/759 falsche Zelle“ sind Zellgrenzen-Gleichstände (max Abstand 1,31 km < halbe Zelldiagonale 1,32 km), und die 2,99/5,92 km des Schleiers sind die erwartete Nächster-Nachbar-Umtastung 8 km → 8 km. Offen: **B5**, B2 (nur bei externem Raster-Modell) und die Browser-Verifikation |
| `audit/waldbrand-ausbreitung.md` | Diagnose **SF0** + Umsetzung + Gate **GSF1** (2026-08-19/20): Ausbreitungsrichtung je aktivem Brand (Pfeil + Fächer) statt Rasterfläche. Enthält die FBP-Gleichungstabelle mit Nummern und Quelle, den Brennstoffsatz mit ST-X-3-Koeffizienten (und **warum M-1/M-2 nicht gehen**: a = b = c = 0, PC wäre erfunden), die gemessene Datenlage (65 aktive Brände, 12 Pfeile, 12 DEM-Zellen, ISZ für alle 7 Stunden, Wind nur für Stunde 0) und acht Lehren — darunter: **Gl. 71/72 (Beschleunigung) gehören NICHT hierher** (sie beschreiben eine Punktzündung, unsere Brände brennen bereits), der Kurungsfaktor ist am Knick 58,8 % **nicht exakt stetig** (Sprung 4·10⁻⁴ — so veröffentlicht), **Textsonden am Quelltext scheitern an Zeilenumbrüchen im String** (auf den Werten prüfen), und **Leistungsanker messen die Maschine mit** (drei Anker fielen unter Last, im Leerlauf sofort grün) |
| `audit/waldbrand-forecast.md` | Diagnose **WF0** (2026-08-18) + Entscheidungsprotokoll **§13** + Gate **GWF1/GWF2** §14 + **WF3/GWF3** §15 (2026-08-19; Lehren WF3: die Stundenachse braucht eine **dreistufige** Ehrlichkeit — stündlich / Tageswert / gar nicht (`hourFollow`) —, ein Tages-Layer zeigt auf ihr den Kalendertag von jetzt + h (`dayOfHour`, Mitternacht = morgen), und ein Permalink-Feld, das „Achse aktiv" bedeutet, muss auch die 0 schreiben (`h` vorhanden ⇔ Stunden); CDP-`Page.navigate` auf denselben Pfad mit anderem Hash lädt die App NICHT neu — über `about:blank` gehen. Lehren WF1/2: cffdrs rechnet mit der exakten FF-Skalen-Konstante 250·59,5/101, nicht mit 147,2 — mit 147,2 verfehlt man die Referenzvektoren; die Stundenachse ist „jetzt + h", also `stepsForNowWindow` statt starrer Schritte 0…12; ein voller 608×373-Kettenschritt kostet ~90 ms ⇒ in Scheiben rechnen; **V-WF-10**: `bz2Worker.ts` degradiert nach dem 4-s-WASM-Timeout dauerhaft auf pure-JS — 28 min statt 11 s im Smoke): **WF4/GWF4** §16 (Layer `fireForecast`: Bit 13, Fläche ICON-D2 / Punkt Fusion, `isiRamp.ts`, Punktkurve per dynamischem Import; Lehren: ein neuer Custom-GL-Layer braucht DREI Einträge — `CUSTOM_GL_LAYERS` (sonst blockiert der Platzhalter aus `installLayers` den echten Layer), einen Lizenzträger (Custom-Layer tragen keine Source-Attribution) und `stateRef` **plus** `applyState`-Deps; Stundenlabels gegen `Date.now()` gerundet erzeugen zwei „jetzt" — Bezug ist der Beginn der laufenden Stunde; Long Tasks nur mit Kontrolllauf zuordnen, sonst schreibt man Bestand der neuen Phase zu — V-WF-13): Waldbrand-Forecast 0…+12 h **auf der Fusion** (Jans Vorgabe: Punkt-Forecast als Basis, fehlende Parameter in die Fusion, GitHub-Actions-Batch + R2). Kernbefunde: (1) der Punkt-Forecast trägt T/RH/Wind/Böe/Niederschlag stündlich, DE 24 h / AT+CH 60 h — für den Stundenanteil eines FWI **kein neuer Parameter**, aber **kein einziges ICON-D2-Feld**; (2) die Raster-Fusion (100×80, ~10 km) **verwirft RH und Böe** der Obs-Adapter (`fusionEngine.ts` greift sie nie ab; AROME-Grid fragt `rh2m` nicht ab) — RH ist der eine harte Ingest-Punkt; (3) die Brandradar-Treiber `fireWeather`/`fireSoilDryness`/`fireWind` sind **native ICON-D2 an der Fusion vorbei** — der Index aus der Fusion wäre eine zweite Meteo-Basis (Jans Entscheidung); (4) DMC/DC/FFMC₁₂ brauchen Vortagsgedächtnis — der Batch hebt die §W.5-Grenze auf, DWD-CDC `hourly/*/recent` (~600 Stationen, ~500 Tage) erlaubt Kaltstart ohne Einschwingphase in DE; (5) stündlicher Index ist fachlich tragfähig (Van Wagner 1977 hFFMC, `cffdrs::hffmc`, Rodell et al. 2024 hFWI aus NWP; **der DWD rechnet den WBI selbst stündlich**, publiziert nur das Tagesmax) — kein eigener Index, FWI-Gleichungen unverändert, nie „WBI"/„Gefahrenstufe"; (6) der Composite-Vorbehalt (`iconD2Relhum.ts:27-34`, `fireAssessment.ts:19-21`) trifft ein FWI nicht, steht aber im Code — nur Jan hebt ihn auf; (7) R2 existiert im Repo nicht (Transport-Zone). Enthält Parameter-Matrix (PF/RF/neu), ICON-D2-Katalog (148 Var., EPS-Abgleich), vier Wege A–D mit Schwächen, Empfehlung **B gestuft** (WF1 Rechenkern → WF2 RH in RF → WF3 Stundenregler → WF4 Layer ohne Batch → WF5 Batch+R2 → WF6 Kalibrierung an DWD-WBI), 11 Fragen mit Defaults, V-WF-1…8 |
| `mobile-design-guidelines.md` | Verbindliche Mobile-UI-Patterns (⚠ Datei fehlt derzeit im Arbeitsverzeichnis, s. Statusblock) |
| `docs/` | Fachspezifikationen (s. Tabelle unten) |
| `audit/` | Diagnose-/Phasen-Befunde (historisch, mit Belegen/Screenshots) |

**Fachspezifikationen unter `docs/`**

| Datei | Rolle |
|---|---|
| `docs/MAP.md` | 2D-Karte: Komponenten, Renderpipeline, Datenfluss, State, Konfiguration, Caching, Fehlerbehandlung, Performance |
| `docs/LAYER_SYSTEM.md` | Layer-Vertrag: die zwei Mechanismen, `LayerKey`-Verdrahtung, Z-Ordnung, Zielbild „Layer-Registry" |
| `docs/WEATHER.md` | Meteorologischer Layer-Katalog: 16 bestehende + 9 geplante Layer, Paletten-Ordnung, Länder-Abdeckungsmatrix |
| `docs/DATA_SOURCES.md` | Quellenbewertung DACH (DWD, GeoSphere, MeteoSchweiz, EUMETSAT, EUMETNET/OPERA, Copernicus) mit 24-Kriterien-Matrix je Quelle |
| `docs/API.md` | Externe Endpunkt-Kontrakte: URLs, Formate, Projektionen, Lizenz- und CORS-Lage |
| `docs/2d-layer-erweiterung.md` | **Integrationskonzept + Umsetzungsplan** für die neuen 2D-Layer (Phasen L0–L15) |
| `docs/zuglinien-radar-spec.md` | **Umsetzungsreife Spezifikation der Phasen L5 + L6:** Zeitmodell (`layerTime.ts`), Playback (`TimelinePlayer`), Frame-Budget, Prefetch, Regenradar-Rückblick, Niederschlagszuglinien (E1/E2), E3-Spec für L11, Verifier-Verträge, Byte-Identitäts-Kontrakte |
| `docs/niederschlag-architektur.md` | Niederschlags-Ansicht „jetzt–2 h" im Detail (D-14) |
| `docs/high-end-radar-feature-catalogue.md` | Funktionskatalog Radar (Referenzspezifikation) |
| `docs/fusion-*.md` | Fusions-Engine (Spec, Paper, v2-Plan, 2D-Integration) |
| `docs/zielgruppen-dach.md` | Zielgruppen-Fundament |
| `docs/model-switcher-gate0.md` | Per-Land-Modell-Switcher |
| `docs/aktivfeuer-merkmale.md` | **Merkmalsschema `FireFeatures` v1** (AF3) + **Kalibrierung** (AF4, §7): Felder mit Herkunft und `null`-Bedeutung, Regeln (kein `undefined`, keine Fläche ohne Art, deterministisch), Labelpaar mit zwei Quellen (`effis-rda` heute, `ba-dnbr` später) + `isEligiblePair`, log-log-Modelle `fre`/`det` mit 80-%-Prädiktionsintervall, Kalibrierbereich, Kill-Switch `?afEst=0` — Referenz `src/fire/activity/{features,calibration,estimate}.ts`, Skripte `scripts/fire/{pairs-from-archive,calibrate}.mjs` |

**Achtung Alt-Doku:** `docs/reports/*`, `docs/seo-geo/*`, `buscosun-atmosphaere-*.md`, `buscosun_seo_geo_*.md`, `prompt-loading.md` sind abgeschlossene Session-Artefakte — als Historie wertvoll, nicht als Ist-Beschreibung. Bei Widerspruch gilt: **Code > `architecture.md`/`decisions.md` > Alt-Doku.**

## Harte Regeln (gelten für jede Session)

- **Oberste Direktive: Funktionserhalt.** Keine bestehende Funktion wird entfernt, versteckt oder „vereinfacht". Umgruppieren ja, Weglassen nein — Ausnahmen nur mit expliziter Freigabe durch Jan.
- **Diagnose-First:** Diagnose → Plan → Implement → Verify → Gate. Kein Code vor schriftlicher Diagnose (`audit/<thema>.md`). Gates werden nur mit Beleg (Screenshot-Pfad, Trace, Konsolen-Auszug, Verifier-Output) abgehakt.
- **Ein Thema = eine Phase = ein Gate.** Keine zwei Features parallel in einer Session anfassen. (Für parallele Agent-Teams gelten die Zuständigkeits- und Konfliktregeln in `agents.md`.)
- **Desktop-Regression = Phase fehlgeschlagen.** Mobile-Änderungen nur per Media Query isoliert. Breakpoints: 767 px (mobil) / 1439 px (Desktop-Groß) — keine Ad-hoc-Breakpoints. Safe-Area via `env(safe-area-inset-*)`.
- **STOPP & FRAGEN (Jan) bei:** Shader-/WebGL-Pipeline-Änderungen, Fusion-Engine-Änderungen, Löschen von Komponenten, Dependency-Upgrades, Änderungen an Edge Functions/Warm-Crons/Manifest-Mechanik, allem Irreversiblen. Prod-Dispatch der Crons ist Jans Gate.
- **Mobile-GPU-Fallen:** kein Verlass auf `EXT_color_buffer_float`; explizite `highp`-Deklarationen; RGBA8-Packing-Pfad nicht anrühren. Performance-Regelung ausschließlich über den `FrameGovernor` (FPS-Leiter zuerst, Trail-0,5× als letzter Hebel, Partikelzahl ist **kein** Hebel) — keine Sonderpfade.
- **Ehrlichkeit ist Produktprinzip:** Unsicherheiten, Datenlücken und Länder-Asymmetrien (z. B. UV/Pollen/Warnungen DE-only) werden ausgewiesen, nie kaschiert. Experten-Layer (z. B. Rotation) tragen konservative Formulierungen — nie „Tornado"-Sprache.
- **Flag-Gating („Rule 2"):** Neue Rechenpfade ersetzen alte nie direkt; sie kommen default-off hinter Flags mit benanntem Fallback (Muster: Fusion v2).
- **Design-Standard Command-Deck (D-27):** Alle neue UI entsteht im Command-Deck-System (hell, Sand/Ink, League Spartan, Topbar+Rail+Dock, Feature-Token-Namespaces); Alt-Themes werden migriert, nie erweitert.
- **Verbesserungs-Pflicht (D-28):** Jede gefundene Verbesserung wird als `V-NN`-Eintrag in `improvements.md` festgehalten — immer mit Mehrwert (für Jan verständlich) und Umsetzungsskizze.

## Verifikation

- **Kein Test-Framework** (bewusst, s. `decisions.md`): stattdessen Headless-Verifier `npm run verify:*`. **Am 2026-08-23 (BW-1) nachgezählt statt fortgeschrieben: 51 npm-Aliase** (49 `scripts/verify-*.mjs` + `verify:seo` + `verify:repack`), dazu 1 nicht verdrahtetes Skript (`scripts/verify-layer-matrix.mjs`) und 2 manuell gebündelte Browser-Harnische (`src/ml/_verify.ts`, `src/radar/_verify.ts`) = **54 Harnische**. **Am 2026-08-28 (R3D) nachgezählt: 56 npm-Aliase / 57 Harnische** (54 Dateien `scripts/verify-*.mjs` — davon 53 verdrahtet, `verify-layer-matrix.mjs` weiterhin nicht — plus `verify:seo` und 2 Browser-Harnische; die Aliaszahl liegt über den verdrahteten Skripten, weil `verify-ifs.mjs` an drei Aliasen hängt). Neu `verify:route-3d` **564/564** (82 mit 1a, +84 mit dem Zeitkorridor 1b, +53 mit den Restposten, +97 mit 1c, +62 mit R3D-4, +80 mit R3D-5, +29 mit R3D-6, +57 mit R3D-7, **+11 mit R3D-8**: dass Bühne und Ergebnis dieselbe Funktion für die Bodenspuren, dieselbe Chip-Liste, denselben Schalter-Speicher und denselben Chip-Knopf nehmen, dass die Palette im puren Modul liegt und nicht mehr in der Komponente, dass das Ergebnis mit dem Gelände startet und die flache Karte erreichbar bleibt, dass der Umschalter erst mit berechnetem Wetter erscheint und sein CSS in `routeDeck.css` steht (vor dem Lazy-Chunk sichtbar), und dass die Reihenfolge Karte/Chips je Ansicht verschieden **bleibt**; R3D-7-Anteil: jede der elf Ereignisregeln einzeln, die Interpolation der Schwellen an nachgerechneten Zahlen, `steppedBand` in beide Richtungen, **dass ein pendelnder Wert KEINE Zeile erzeugt** (Wind wie Temperatur), ein Sprung über zwei Marken zwei Zeilen, die Ordnung bei gleicher Zeit (Start zuerst, Ankunft zuletzt), der ausgesprochene Deckel, der Text ohne Link samt Vorbehalt und Lücken, und die Verdrahtung — der Plan hängt an keinem `dmode`- oder `stage`-Zweig; R3D-6-Anteil: die bodenfolgende Bahn (Oberkante je Spalte über IHREM Gelände, `v` an der echten Oberkante, Decke bleibt Grenze — und **ohne** Bahn byte-gleiche Stützpunkte wie bisher), die Streckenstücke (Mitten-Regel, lückenlos, keine Farbe = kein Stück), `windPicks`, die Reihenfolge der Spuren, dass die Pfeile NACH dem Vorhang gezeichnet werden und mit der Karte drehen, die Gruppentrennung „Am Boden" zuerst, die Startschalter, und dass ein leerer Pfeil-Layer „zu schwach" sagt; R3D-5-Anteil: die Tour als Vertikalschnitt (Anker nur aus belegten Samples, fehlende Bewölkung gemeldet statt zu 0 % erklärt, **keine** Inversion und die Lapse-Rate im Temperaturfeld, jede Zelle gegen `windAtAGL` nachgerechnet, der Wand-Deckel und der **unveränderte** Standard der Atmosphären-Ansicht), die Geometrie-Helfer (Resampling, `interpTrack` auf 50 000 Punkten, Ausdünnung mit beiden Enden, exakte Fensterkanten), die Sätze über den Inhalt der Wand, und die Verdrahtung samt der Sonde, dass die Datei **keine Shader-Quelle** enthält — plus `verifyCurtainMesh()` und `verifySectionGeometry()`, die bis hier an keinem Verifier hingen; R3D-4-Anteil: das Geländeprofil als Hüllkurve (Gipfel und Kerbe überleben die Ausdünnung, Knoten an jeder Wetterspalte, Deckel, aufsteigende Distanzen, NaN-Höhen fallen heraus) samt der tragenden Zahl — an einem 20-km-Gelände liegt die Spaltenkette **223,9 m** daneben, das Profil **0,1 m** (Faktor 2 587) —, `terrainAt` und `minTerrainBetween`, Windwand und Regensäule auf dem Boden **ohne** neu gerechnete Höhe über Grund, `reliefPath` gegen die Tiefe, die vier Herkunftstexte der Höhen (geprüft / ungeprüft / ergänzt / ersetzt, und dass keiner die Wetter-Auflösung nennt), der Hagel-Satz samt der beiden Belege im Code (kein Hagelfeld in `SampleWeather`, `forecastHour === 0` in `MapView`), und die Verdrahtung von Profil, Relief, Gegenprobe und Vorschau-Chip; 1c-Anteil: Bewertung je Spalte in beide Richtungen der Rangfolge (Überschreitung schlägt Unsicherheit, Unsicherheit schlägt „knapp" und wird nie zu „Go" aufgelöst), fehlender Wert und fehlende Quelle getrennt, Abschnittsbildung lückenlos und mit der geteilten Mitten-Regel, `lead`/`leadIdx`, die Begründungssätze samt deutscher Grammatik in jeder Kombination, die Chip-Zusammenfassung, die Startsuche gegen die Grenzwerte (frühester Treffer, unvollständige Kandidaten, ein fehlender Wert gilt nicht als bestanden, die ungeprüfte Warnung wird benannt), die Höhenkorrektur IM Kandidaten als Textsonde, die Grenzwert-Persistenz mit Klemmung und Fremdschlüssel-Filter, der Bericht ohne Link, die Verdrahtung des dritten Modus samt Druckbild und 44-px-Zielen — und `verifyGoNoGo()` aus `src/threed/`, die bis hier an keinem Verifier hing; 1a/1b/Restposten-Anteil: Pack-/Entpack-Rundlauf des Tour-Speichers samt Sample-Identität und den fünf Ablehnungsfällen, Frische, Startzeit-Regel gegen `horizonState`, Kill-Switch, die Verdrahtung in `TourView`/`RoutePage`, die Trennung von Rahmen und Szene, das Wartefeld ohne Wetterwerte, seine CSS-Heimat außerhalb des Lazy-Chunks, die Decke-vor-Vergleich-Regel und die belegten Kästen in `layoutCards` sowie „leerer Layer ≠ fehlende Quelle" in beiden Chip-Sätzen; 1b-Anteil: Regenfenster inkl. Mitten-Ausdehnung und Einzel-Sample, Perlen mit Abstands- und Doppel-Regel, `layoutCards` in Bildkoordinaten, Zeitfenster-Prioritäten, Startempfehlung mit Schwelle/Gleichstand/Abdeckung, Radar-Vorlauf je Land, und die Sonden auf das, was NICHT behauptet wird — gelesen über `codeOnly`, weil sie sonst auf ihren eigenen Begründungskommentaren anschlagen) — Parallelprojektion (affine Tiefe, Parallelen, gedeckelte Überhöhung), Verdeckung (Gelände- und Himmelspolygon teilen exakt dieselbe Profilkante), Windwand (lückenlose Segmente, Bandmonotonie, `windAtAGL` aus demselben Kern), Kopplung Position ↔ Zeit (Rundlauf < 1 m / < 1 s, Klemmen statt Extrapolieren), die Ehrlichkeitstexte gegen `clustering.ts` statt gegen Literale und Textsonden auf die Ansicht; **der Lauf zieht die bis dahin unverdrahtete `verifyCrossSection()` mit** (B6 des Audits). Davor **am 2026-08-28 (LE2) ausgezählt: 55 npm-Aliase / 56 Harnische** (53 Dateien `scripts/verify-*.mjs` + `verify:seo` + 2 Browser-Harnische; neu `verify:layer-erstbild` **37/37** — Byte-Gleichheit der herausgelösten INCA/rzc-Parser gegen die Referenzschleife an echten Dateien (`LE_FIXTURES=<dir>` mit `inca.nc`/`rzc.h5`, sonst Live-Abruf, ohne Netz ⊘), Kill-Switch, Rückfall, Prioritäten mit gestubbtem `fetch`). Davor **am 2026-08-25 (EZ): 54 npm-Aliase / 55 Harnische** — 52 Dateien `scripts/verify-*.mjs` (51 verdrahtet, `verify-layer-matrix.mjs` weiterhin nicht) + `verify:seo` + 2 Browser-Harnische; die 54 Aliase sind mehr als die verdrahteten Skripte, weil `verify-ifs.mjs` an **drei** Aliasen hängt. Neu: `verify:event-zone` (**41/41**, netz- und DOM-frei — Zonen-Geometrie, 60-km-Deckel, Messpunkte, die gemessene Spannen-Schwelle und der additive `z`-Permalink; `audit/event-zone.md`). Davor **seit BW-5 (2026-08-24): 52 Aliase / 55 Harnische** (BW-6: unverändert — `verify:repack` jetzt **230/230**, Sonden `scripts/l0/probe-bw6*.mjs`) — dazugekommen ist `verify:radar-runs` (**22/22**: die gerechneten RV-Zeitstempel gegen das ECHTE Verzeichnis — trifft der Rat den jüngsten vorhandenen Lauf, ist das Raster lückenlos, existieren die 9 Rückblick-Kandidaten —, dazu netzfrei die Entdopplung samt der Abbruch-Falle aus GBP1 (3)/BH4: der abbrechende Aufrufer bekommt `AbortError`, der zweite **wird nicht vergiftet**; ohne Netz werden die Live-Zeilen als ⊘ übersprungen, nicht als Fehler gezählt). `fusion:*` gibt es in `package.json` NICHT mehr — die frühere Angabe „58 inkl. `fusion:*`" war bereits vor dieser Phase zu hoch. Zuletzt dazugekommen: `verify:repack` (**106/106** — BW-1 Byte-Identität über vier echte ICON-D2-Läufe, BW-2 Ablage/Manifest-Abschnitt inkl. Force-Push-Wächter, `skipDecision` und der Kette bis zum CDN, BW-3 der **Client** gegen dieselbe Wahrheit: URL-Spiegel, Anti-Drift, acht Ablehnungsfälle, Zusammensetzen des Temperaturbilds aus Browser-Bytes **mit Gegenprobe**, `uvBounds` == GRIB-Weg auf 0, Fristen und der SW-Durchreicher, BW-4 der gedrehte Schalter samt Überleben von `?repack=0` in der umgeschriebenen Karten-URL; braucht vorher `npm run repack` + `npm run repack:publish`, die CDN-Zeile erst mit `REPACK_CHECK_CDN=1` — sie prüft den **Live-Index** des Daten-Repos, nicht den lokalen Baum); entfallen mit BW-0: `verify:fire-spread`, `verify:fire-fwi`, `verify:fire-weather-grid`. Weitere Zahlen: `verify:routing` 70/70, `verify:fire-anomalies` 56/56, `verify:layer-geometry` 15/15, `verify:radar-sampling` 25/25, `verify:fire-time` 114/114, `verify:fire-model` 112/112 (Node `--experimental-strip-types` importiert echte App-Module; teils gegen Live-Server → Netzabhängigkeit einkalkulieren). `npm run typecheck` muss grün sein.
- **UI-Verifikation:** Chrome DevTools MCP (Desktop 1440×900, iPhone 12 Pro 390×844 DPR 3). Emulation ist für WebGL **nicht** repräsentativ — GPU-kritische Aussagen brauchen Real-Device (scrcpy/ADB), Jan informieren. Hinweis: In-App-Browser pausiert rAF → WebGL-Karten nur im Vordergrund-Browser verifizieren.
- Vor jedem Gate: die fünf Selbstverifikations-Fragen schriftlich mit Beleg beantworten (1 Funktionserhalt einzeln, 2 Desktop pixelgleich, 3 Touch-Targets ≥ 44 px, 4 Konsole sauber, 5 keine Long Tasks > 200 ms).

## Sprache & Konventionen

- Dokumentation auf **Deutsch**, Prompts an Claude Code auf **Englisch**, Code/Kommentare/Commits auf Englisch (Bestand ist gemischt — bei Neuanlage Englisch).
- Commits: Conventional Commits, Scope = Feature-/Themenname. Keine Commits ohne Auftrag.
- Nach jeder Phase: `checklist.md` aktualisieren, 3–5-Satz-Fazit in `context.md` §Session-Log anhängen.
