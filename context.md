# context.md — Session-Kontext: Mobile-Optimierung buscosun

## Projekt
buscosun.com ist eine DACH-fokussierte Wetter-Visualisierungsplattform.

**Stack:**
- React + Vite + TypeScript
- MapLibre GL JS (2D-Karten)
- Three.js, WebGL2 mit WebGPU-Pfad (3D Globus, volumetrische Wolken via Ray Marching)
- GPU-Ping-Pong-Windpartikel (RGBA8-packed Positions-Encoding, webgl-wind-Technik)
- Adaptive Wind-Performance-Governance: **`FrameGovernor`** (`src/wind/perfGovernor.ts`) — EMA-Smoothing, Warmup-Gating, asymmetrische Hysterese, Cooldown. (Hinweis: die früher hier genannten „RepaintScheduler-Architektur / AdaptiveQualityController / FixedTimestepLoop" existieren **nicht** im Code — bestätigt in `audit/performance-2d.md` §2; der reale Hebel ist `FrameGovernor`.)
- Cloudflare R2 + PMTiles Tile-Pipeline, Netlify Functions
- Fusion-Forecast-Engine (Minimum-Variance/OI über DWD MOSMIX, GeoSphere AROME, INCA) mit Fusion⇄Native-Toggle
- Per-Country-Model-Switcher (DE/AT/CH)
- Browser-lokaler LLM-Meteorologe (WebLLM, Qwen2.5-3B, Web Worker)

## Die acht Features dieser Session

| # | Feature | Kern-Funktionalität | Mobile-Risiko |
|---|---------|--------------------|---------------|
| 1 | Wetterkarte | MapLibre-Hauptkarte, Layer-Steuerung, Model-Switcher DE/AT/CH, Fusion-Toggle, Windpartikel-Overlay | Hoch: Bedienelemente konkurrieren mit Karten-Gesten; GPU-Last |
| 2 | Regenradar | 6h-DACH-Nowcast (RADOLAN/SINFONY-Blend), Zeit-Scrubber/Timeline | Mittel: Timeline-Scrubbing mit dem Daumen; Frame-Preloading |
| 3 | Vorhersage | Punkt-Vorhersage, Modellvergleich, Konfidenz-Anzeige | Mittel: dichte Datenpanels, Tabellen/Charts auf 390 px |
| 4 | Tourenplanung | Route/Tour-Forecast, GPX-Upload, Abfahrtszeit-Optimierer | Hoch: mehrspaltiges Layout, Datei-Upload auf iOS, Karteninteraktion + Panel |
| 5 | Event-Planung | Best-Day-Planer für ortsfeste Anlässe | Niedrig–Mittel: Formulare, Datumswahl, Ergebnisliste |
| 6 | Historie | Historischer Klima-Explorer | Mittel: Charts, lange Zeitreihen, Zoom/Pan in Diagrammen |
| 7 | Atmosphäre | Linsen "Fliegen" / "Berg & Weg" / "Himmel", drei Progressive-Disclosure-Tiefen | Mittel: Disclosure-Ebenen brauchen mobiles Interaktionsmuster |
| 8 | 3D Globus | Three.js-Weltkugel, volumetrische Wolken | Sehr hoch: GPU/Thermik, Touch-Orbit-Controls, Speicher |

## Bekannte technische Randbedingungen (aus früheren Debugging-Sessions)
- Mobile GPUs: `EXT_color_buffer_float` häufig nicht verfügbar → Float-Render-Targets tabu, RGBA8-Packing ist der stabile Pfad. **Nicht anfassen.**
- `mediump` kollabiert auf realer Hardware (Präzisionsverlust bis hin zu invertierter Windrichtung auf iOS) → alle relevanten Shader tragen explizite `highp`-Deklarationen. Bei neuen/geänderten Shadern gleiches Muster.
- Software-Rendering (SwiftShader/Lavapipe) im Emulator ist für WebGL-Aussagen unbrauchbar → Chrome-DevTools-Emulation nur für Layout/Interaktion/Netz verlässlich, GPU-Verhalten braucht echtes Gerät.
- `FrameGovernor` (nicht „AdaptiveQualityController" — der Name existiert nur in Altdoku) ist der zentrale Mobile-Performance-Hebel — Probleme zuerst über ihn lösen bzw. ihn erweitern, keine neuen Sonderpfade/Bypässe bauen.

## Ziel dieser Session
Jede der acht Feature-Seiten soll auf dem iPhone 12 Pro:
1. **vollständig bedienbar** sein (alle Desktop-Funktionen erreichbar, Touch-Targets ≥ 44 px, keine Hover-Only-Interaktionen),
2. **visuell aufgeräumt** wirken (kein abgeschnittener Content, kein horizontales Scrollen, Safe-Area respektiert),
3. **flüssig laufen** (keine Long Tasks > 200 ms bei Standardinteraktionen, GPU-Features im angemessenen Quality-Tier).

Explizites Nicht-Ziel: neue Features, Redesign der Desktop-Ansicht, Änderungen an Datenpipelines oder der Fusion-Engine.

## Verifikations-Werkzeuge
- **Chrome DevTools MCP**: Device-Emulation (iPhone 12 Pro: 390×844, DPR 3, Touch), Screenshots, Console, Performance-Traces, Network.
- **scrcpy + ADB** (Android-Realgerät) für GPU-kritische Stichproben; iOS-Realgeräte-Check durch Jan manuell (Safari, iPhone).
- **Context7 MCP** für aktuelle MapLibre/Three.js-API-Referenzen bei Bedarf.

## Aktueller Stand (Stand 2026-07-08)

**Abgeschlossen:**
- **Phase 0 — Fundament & Baseline (G0 bestanden):** Emulation (iPhone 12 Pro 390×844 DPR 3) verifiziert, Breakpoint-Konvention 767/1024 bestätigt, `viewport-fit=cover` ergänzt, Baseline-Screenshots + Konsolen-Baseline aller 8 Features unter `audit/screenshots/baseline/`, geteilte Primitives unter `src/mobile/`. Details: `audit/phase0-fundament.md`.
- **Phase 1 — Wetterkarte (G1 bestanden):** Touch-Target-Fixes, half/full-Snap-Sheet, Landscape-Bug 844×390 behoben. Follow-up (§8): zwei getrennte Layer-/Modell-FABs + Karte auf obere 2/3 begrenzt. Details: `audit/wetterkarte.md`.

- **Phase 1-C — Wetterkarte Redesign „Variante C" (G1-C bestanden, 2026-07-08):** Von Jan aus drei Mockup-Varianten gewählt (`audit/mockups/`). Ein persistentes Bottom-Sheet mit Segment-Umschalter Layer·Modell·Vorhersage, drei Snap-Zustände (collapsed 64px / half 46vh / full 88vh), Karte vollflächig, transform-basierte Motion (CLS ≈ 0), Punkt-Vorhersage als drittes Segment integriert (Wrapper-Umzug, `useMediaQuery`-Weiche), CSS auf einen Mobile-Block konsolidiert. Führt die zwei FABs des §8-Follow-ups bewusst wieder zusammen — von Jan freigegeben, keine Regression. Details: `audit/wetterkarte.md` §9.

**Offene Phasen 2–8:** Regenradar, Vorhersage, Tourenplanung, Event-Planung (von Jan bewusst vorgezogen — getrennter Formular-Flow; Diagnose + Maßnahmenliste liegen vor, EventPage-Änderungen im Working Tree), Historie, Atmosphäre, 3D Globus. Reihenfolge ansonsten wie in `CLAUDE.md`.

**Arbeitsmodus:** Jan lässt zwischen grünen Gates ohne Rücksprache fortfahren; Commits erfolgen gesammelt durch Jan selbst.

## Session-Log
_(wird von Claude Code nach jeder Phase ergänzt — Datum, Phase, 3–5 Sätze Fazit, Link auf Audit-Datei und Screenshots. Historie der abgeschlossenen Phasen 0/1 steht in den jeweiligen `audit/`-Dateien und in `checklist.md`.)_

**2026-07-19 · Querschnitt-Phase P3 — Repaint-Disziplin / Hebel 5 UMGESETZT (Gate GP3 grün).** Der letzte der 5 Fachmann-Hebel ist implementiert (auf `main`, kein Branch, reine Event-/Scheduling-Logik). **P3-1** — `visibilitychange`-Listener in `onAdd`/`onRemove`; `document.hidden` → `paused`. **P3-2** — `IntersectionObserver(threshold 0)` auf `map.getCanvas()`; `ratio===0` → `paused`; `disconnect()` in `onRemove`. **P3-3** — ein `paused`-Flag (hidden ∨ offscreen) über `updatePausedState()` (reagiert nur auf Übergänge) gated die selbst-perpetuierenden Repaint-Pfade; `render()` bleibt für Fremd-Repaints (Kamera) korrekt. **Zusatz zur Spec:** neben `scheduleParticleRepaint` wurde auch der zweite Dauerloop — der „warte auf Wind-Daten"-Spinner in `render()` — unter denselben Flag gestellt (ohne das würde ein Hintergrund-Tab ohne geladene Daten weiterdrehen; im Emulator direkt beobachtet). **P3-4** — `clearOnNextFrame` + genau ein `triggerRepaint` beim Resume; `repaintCapTimer` beim Pausieren gecleart. Live-Verifikation (Chrome DevTools MCP, iPhone 12 Pro emuliert, :5180): sichtbar **61** `triggerRepaint`/s → `hidden` **0/s** (`paused=true`) → `visible` zurück auf **61/s**; Offscreen via `translateY(-200vh)` schaltet `paused` false→true→false; Baseline == Resumed (sichtbar unverändert); Konsole (error+warn) leer, `tsc -b` grün. In dieser Session lud die Wind-*Daten*quelle nicht (`hasWindData=false`) → aktiver Loop war der Pre-Data-Spinner; da er denselben `paused`-Guard nutzt wie `scheduleParticleRepaint`, ist die gesamte Kette (Event→Handler→`updatePausedState`→`paused`→Loop-Stopp→Resume) end-to-end belegt, der Partikel-Loop-Gate ist der identische Einzeiler. P3-eigener Diff = **nur `WindLayer.ts`** (alle P3-`+`-Zeilen reine Event/Scheduling, kein GL/Float/RGBA per Diff-Scan); `perfGovernor.ts`/`verify-governor.mjs` von P3 nicht angefasst (Harness 35/35). Kein Shader-/RGBA8-/Trail-Ladder-/Fusion-Eingriff. Hinweis: da P/P2 noch nicht committed sind, zeigt `git diff` gegen HEAD zusätzlich deren Diffs — nicht Teil von P3. 🔴 Real-Device Akku-/Thermik-Gewinn nur nice-to-have (nicht gate-blockierend) an Jan notiert. **Damit sind alle 5 Fachmann-Hebel abgeschlossen** (1+3 vorbestehend, 4=P, 2=P2, 5=P3). Details/Selbstverifikation 1–5: `audit/webgl-cross-device.md` §12.4. — _(Spec-Eintrag desselben Vorhabens siehe unten.)_

**2026-07-24 · Konsolidierungs-Phase N1 — Eine Niederschlags-Ansicht (Radar↔Modell, 0–12 h) (Spec + Doku, Umsetzung offen).** Jan will das bestehende Niederschlagsradar und SIM-Radar (F3) zu **einer** Ansicht „Niederschlag" (jetzt … +12 h, nahtlos, UI ohne Radar/Modell-Kenntnis) zusammenführen. **Diagnose-Kernbefund: der Radar→Modell-Blend existiert bereits** als `nowcast`-Layer — 0–2 h gemessenes Radar per Land (DE RADOLAN-RV / AT GeoSphere INCA ≤3 h / CH MeteoSchweiz rzc, `RainLayer` via `precipFrameReady()` [MapView:724](src/MapView.tsx#L724)), >2 h Fusion-Modell-Niederschlag (`precip-forecast`-`ScalarLayer` via `fusionActiveFor('nowcast')`), gemeinsame `precipRainRamp`; die Umschaltung ist nur über `precipFrameReady` + Sichtbarkeits-Booleans **verstreut**. Aufgabe = konsolidieren, nicht neu bauen. **STOPP-&-FRAGEN ausgelöst** (Radar-Pipeline + Fusion-Verdrahtung + Komponenten-Löschung); Jan per AskUserQuestion entschieden: **2–12-h-Hälfte bleibt der Fusion-/Modell-Niederschlag (mm/h), NICHT dbz_cmax-Reflektivität** → **SIM-Radar wird stillgelegt** (lowest-risk, keine Fusion-Logik-Änderung, AT/CH schon „beste Quelle" über den Model-Switcher). Ziel-Architektur: neues Modul `src/nowcast/precipSource.ts` (`resolvePrecipSource(hour, country, avail)` als **einzige** Quellenwahl-Stelle, zieht `precipFrameReady` + Radar/Modell-Visibility zusammen; Seam-Crossfade ~2 h, Lücken-Fallback nie leer; Renderer `RainLayer`/`ScalarLayer` bleiben, nur Sichtbarkeit koordiniert; UI kennt kein Radar/Modell mehr). Maßnahmen N1-1…N1-7 (Spec §6): Abstraktion + `verify-precip-source.mjs`, MapView-Visibility auf die Abstraktion, Seam/Lücken/0–12-h, **SIM-Radar restlos entfernen** (~15 Seams + `iconD2Dbz.ts`; F3-Doku als „stillgelegt" markieren), Label „Niederschlag · jetzt–12 h", Doku (README + `docs/niederschlag-architektur.md`). Harte Regeln: **Fusion-Engine-Logik (`src/fusion/*`) unangetastet** (nur Verdrahtung), `flownowcast`/`poprob`/Model-Switcher/Fusion⇄Native/`radarModel.ts`/Regenradar erhalten, Output-Identität je Slider-Stunde. SIM-Radar-Löschung + Radar/Fusion-Verdrahtung sind durch Jans Auftrag autorisierte STOPP-Punkte. Spec `audit/niederschlag-vereinheitlichung.md`, Plan/Checklist Konsolidierungs-Phase N1/Gate GN1, Protokoll V-NIEDERSCHLAG. Umsetzung via CLI.

**2026-07-24 · Feature-Phase F5 — Superzellen-/Rotationspotenzial-Layer (Spec + Doku, Umsetzung offen).** Aus der Layer-Potenzial-Analyse der Vorschlag „Superzellen-/Rotationspotenzial" (Mehrwert 8, Aufwand Komplex): ein **eigenständiger**, standardmäßig **inaktiver** **Experten**-Layer, der aus ICON-D2-`uh_max`(+`uh_max_low`)+`sdi_2` (Updraft-Helicity + Supercell-Detection-Index) **Verdachtsflächen für rotierende Gewitter** (Großhagel/Tornado-Potenzial) flächig über DACH rendert (0–12 h) — Storm-Chaser-Nische, kaum ein Consumer-Dienst. **⚠️ Heikelster der Layer — Ehrlichkeits-Leitplanken (Spec §0) sind gate-blockierend:** zeigt Modell-**Verdacht**, **kein** Warnprodukt → Tooltip/Legende müssen „kein amtliches Warnprodukt" (Verweis DWD-Warnungen), „Verdacht ≠ Ereignis", „hohe Fehlalarmrate" und „Experten-Layer" benennen; Sprache **nie** „Tornado"; Darstellung **geglättet** (Nachbarschafts-Aggregation gegen Einzelpixel-Alarmismus). Falls keine seriöse Darstellung möglich → STOPP & FRAGEN (lieber kein Layer als ein irreführender). Diagnose-Kern: `uh_max`/`sdi_2` sind reguläre ICON-D2-2,2-km-Gitter (kein EPS) → gleicher Decode wie Temp/Böen; `uh_max`/`uh_max_low` sind Intervall-**Maxima** → t+0 strukturell 0 → **`minStepHours=1`**; `sdi_2`-Vorzeichen/Wertebereich im Decode verifizieren. „Komplex" liegt an Schwellen-**Kalibrierung** + **Glättung** des rauschigen UH-Felds + **Fusion** (UH-Stärke + SDI-Korroboration) + **Ehrlichkeits-Labeling** — nicht am Laden. Jans harte Vorgabe unverändert: **Lazy-Load** (`active.has('rotation') && !ref → installRotationRef`, Muster Clouds/Gust), kein Eager-Fetch, nicht im `initialActive`-Default. Maßnahmen F5-1…F5-7 (Spec §5): neue Quelle `iconD2Rotation.ts`, reine Fusion+Glättung `rotationPotential.ts` + Harness, `rotationRamp` (dezent, eigene Palette), 7 additive `MapView.tsx`-Seams inkl. Lazy-Effekt, Legende/Tooltip mit §0-Leitplanken, `scripts/verify-rotation.mjs`. Additiv; **`dwdAlerts`/`convectiveIndex.ts` nur gelesen/verwiesen**, kein Eingriff in Wind-Shader/RGBA8/Fusion/EPS/Radar; `warm-grib`-Vorwärmung + `uh_max_med`/`w_ctmax` optional/vertagt. Spec `audit/rotationspotenzial.md`, Plan/Checklist Feature-Phase F5/Gate GF5, Protokoll V-ROTATION. Umsetzung via CLI.

**2026-07-24 · Feature-Phase F4 — Schneehöhe-&-Neuschnee-Layer (Spec + Doku, Umsetzung offen).** Aus der Layer-Potenzial-Analyse der Vorschlag „Schneehöhe & Neuschnee" (Mehrwert 8, Aufwand Einfach): ein **eigenständiger**, standardmäßig **inaktiver** Layer „Schnee" mit zwei Modi (Umschalter analog Satelliten-Produkt) — **Schneedecke** (`h_snow`, cm) + **Neuschnee** (cm) als Flächen-Raster, statt nur der Schneegrenzen-Linie. **Abgrenzung (Spec §0):** der bestehende `snowline`-Layer (ML-**Linie**) bleibt **unverändert**; F4 zeigt die **Menge als Raster**; `snowlmt`-Linie ist durch `snowline` abgedeckt → nicht dupliziert. **⚠️ Kritischer Diagnose-Fund (§2.2): `freshsnw` ist NICHT „Neuschnee in cm"**, sondern der ICON-Schnee-**Frische-/Albedo-Alterungsfaktor** (0..1) — die im Vorschlag angenommene cm-Quelle ist falsch; Neuschneemenge korrekt aus **akkumuliertem Schneefall `snow_gsp`(+`snow_con`)** → mm SWE → cm (bzw. `h_snow`-Δ). Vor Code die `freshsnw`-Semantik im Decode verifizieren. Diagnose-Kern: `h_snow`/`snow_gsp` sind reguläre ICON-D2-2,2-km-Gitter (kein EPS) → gleicher Decode wie Temp/Böen; `h_snow` instantan → t+0 gültig ohne `minStepHours`, Neuschnee ist Akkumulation → `minStepHours=1` (wie `tot_prec`). **Reuse vorhanden:** `src/nowcast/alpineSplit.ts` (SWE→cm ~10:1, `freshSnowCm`) + `src/radar/precipPhase.ts` `snowRamp`. Jans harte Vorgabe unverändert: **Lazy-Load** (`active.has('snow') && !ref → installSnowRef`, Muster Clouds/Gust), kein Eager-Fetch, nicht im `initialActive`-Default, Modus-Wechsel lädt lazy nach. Maßnahmen F4-1…F4-6 (Spec §5): neue Quelle `iconD2Snow.ts`, Neuschnee-Ableitung via `alpineSplit.ts` (`rho_snow` bevorzugt), Rendering über `snowRamp`, additive `MapView.tsx`-Seams inkl. Modus-Umschalter (analog `SAT_PRODUCT`) + Lazy-Effekt, Legende/Tooltip (cm/Modus/Domäne/Horizont/Verhältnis-Näherung). Additiv; **`snowline`/`climaField` unberührt**, `alpineSplit.ts`/`precipPhase.ts` nur gelesen/wiederverwendet; kein Eingriff in Wind-Shader/RGBA8/Fusion/EPS/Radar; `warm-grib`-Vorwärmung + `freshsnw`-Optik optional/vertagt. Spec `audit/schnee.md`, Plan/Checklist Feature-Phase F4/Gate GF4, Protokoll V-SCHNEE. Umsetzung via CLI. STOPP & FRAGEN, falls `freshsnw` doch amount-artig kodiert ist, `h_snow`/`snow_gsp` nicht regulär-gegittert sind oder die SWE→cm-Wiederverwendung `alpineSplit.ts` verändern würde.

**2026-07-24 · Feature-Phase F3 — Simuliertes-Radar-Layer / dbz_cmax (Spec + Doku, Umsetzung offen).** Aus der Layer-Potenzial-Analyse der Vorschlag „Simuliertes Radar" (Mehrwert 9, Aufwand Mittel): ein **eigenständiger**, standardmäßig **inaktiver** Layer, der die ICON-D2-Composite-Reflektivität `dbz_cmax` flächig **in der gewohnten Radar-dBZ-Optik** über DACH rendert und über den Zeit-Slider 0–12 h in die Zukunft läuft — verlängert das Regenradar **über den 2-h-Nowcast-Horizont hinaus**, wo es sonst kein Radarbild gibt. **Abgrenzung (Spec §0):** F3 ist **Modell**-Reflektivität, keine Messung; `nowcast`/Regenradar (RADOLAN-RV/INCA/MeteoSchweiz) sind in 0–2 h präziser und bleiben **unverändert** — F3 ergänzt die Vorwärtsschau und wird klar als „simuliert/Modell" gelabelt. Diagnose-Kern: `dbz_cmax` ist reguläres ICON-D2-2,2-km-Gitter (kein EPS) → gleicher Decode wie Temp/Böen; **instantan pro Step → gültig bei t+0, kein `minStepHours` nötig** (anders als `lpi_max`/Böen). **Schlüssel-Reuse: `src/radar/radarModel.ts`** enthält die dBZ↔mm/h-Umrechnung + die Radar-Paletten schon → `dbz_cmax` → mm/h → **dieselbe `precipRainRamp` wie das Regenradar** → optisch konsistent, keine neue Palette (daher Aufwand Mittel, nicht Komplex). Jans harte Vorgabe unverändert: **Lazy-Load** (`active.has('simradar') && !ref → installSimRadarRef`, Muster Clouds/Gust), kein Eager-Fetch, nicht im `initialActive`-Default. Maßnahmen F3-1…F3-6 (Spec §5): neue Quelle `iconD2Dbz.ts` (dBZ→mm/h via `radarModel.ts`, nicht dupliziert), Rendering an die bestehende Radar-Palette, 7 additive `MapView.tsx`-Seams inkl. Lazy-Effekt, Legende/Tooltip mit „simuliert"+Horizont+Verweis auf echtes Radar, optional `echotop`-Punkt-Readout (Gewitter-Schwere, vertagt). Additiv; **`nowcast`/RainLayer/RADOLAN-Pfad + Regenradar-Feature unberührt**, `radarModel.ts` nur gelesen/wiederverwendet; kein Eingriff in Wind-Shader/RGBA8/Fusion/EPS; `warm-grib`-Vorwärmung optional/vertagt. Spec `audit/simuliertes-radar.md`, Plan/Checklist Feature-Phase F3/Gate GF3, Protokoll V-SIM-RADAR. Umsetzung via CLI. STOPP & FRAGEN, falls die dBZ→mm/h-Wiederverwendung eine Verhaltensänderung an `radarModel.ts` erzwingt oder `dbz_cmax` nicht regulär-gegittert ist.

**2026-07-24 · Feature-Phase F2 — Blitz-Vorhersage-Layer / LPI (Spec + Doku, Umsetzung offen).** Aus der Layer-Potenzial-Analyse der zweitbewertete Vorschlag (Mehrwert 9, Aufwand Einfach): ein **eigenständiger**, standardmäßig **inaktiver** Layer „Blitz-Vorhersage", der den ICON-D2 **Lightning Potential Index** (`lpi_max`) flächig als Blitzrisiko-Raster über DACH rendert und über den Zeit-Slider 0–12 h in die Zukunft läuft — echte Prognose statt nur gemessener Sferics (USP: kaum ein Consumer-Dienst zeigt einen Blitz-*Prognose*-Layer). **Wichtige Abgrenzung (Spec §0): drei getrennte Blitz-Dinge** — `lightning` (bestehend) = *gemessene* Blitze letzte 60 Min (DWD-WMS `Accumulated_Flash_Area`, bleibt unverändert); **`lightningfc` (F2)** = *prognostiziertes* Risiko (LPI-Grid); `thunder` (F1) = fusionierter Gewitterindex, in dem LPI nur interne Zutat ist. F2 macht LPI **solo sichtbar** und ist **unabhängig von F1** umsetzbar. Diagnose-Kern: `lpi`/`lpi_max` sind reguläre ICON-D2-2,2-km-Gitter (kein EPS/icosahedral) → **Einfeld-ScalarLayer**, gleicher Decode wie Temp/Böen; alle Bausteine vorhanden (`fetchIconD2Grid`, `frameAtValidTime` inkl. `minStepHours`, `ScalarLayer`, Lazy-Muster). Feld-Wahl **`lpi_max`** (Peak-Risiko/Stunde) mit **`minStepHours=1`** (Intervall-Max ist bei t+0 strukturell 0, wie `vmax_10m` — sonst „jetzt" leer); `lpi` instantan als Fallback. Jans harte Vorgabe unverändert: **Lazy-Load** (`active.has('lightningfc') && !ref → installLightningFcRef`, Muster Clouds/Gust), kein Eager-Fetch, nicht im `initialActive`-Default. Maßnahmen F2-1…F2-6 (Spec §5): neue Quelle `iconD2Lpi.ts`, `lpiRamp`/`visRange` (Palette klar getrennt von der Blitzortung), 7 additive `MapView.tsx`-Seams inkl. Lazy-Effekt, Legende/Tooltip mit Ehrlichkeits-Hinweisen + Abgrenzung zum Beobachtungs-Layer, optional leichtgewichtiger `verify-lpi.mjs`. Additiv; **`dwdLightning.ts` unberührt**, kein Eingriff in Wind-Shader/RGBA8/Fusion/EPS/Radar/Decode; `warm-grib`-Vorwärmung optional/vertagt. Spec `audit/blitz-vorhersage.md`, Plan/Checklist Feature-Phase F2/Gate GF2, Protokoll V-BLITZ-VORHERSAGE. Umsetzung via CLI. STOPP & FRAGEN, falls `lpi_max` nicht regulär-gegittert ist oder Decode/`frameAtValidTime` über `minStepHours` hinaus angefasst werden müsste.

**2026-07-24 · Feature-Phase F1 — Gewitterpotenzial-Layer (Analyse + Spec + Doku, Umsetzung offen).** Jan will nach einer Layer-Potenzial-Analyse der 2D-Wetterkarte (Bestand: Wind/Böen/Niederschlag/Temp/Wolken/Sat/Blitze/Stationen + ML-Layer Sicherheit/Schneegrenze/Flow-Nowcast/Regen-Chance) den höchstbewerteten Vorschlag umsetzen: einen neuen, **standardmäßig inaktiven** Layer **„Gewitterpotenzial"**, der aus drei ICON-D2-Feldern einen 0–100-Index je Zelle bildet — `cape_ml` (Energie) × `cin_ml` (Deckel) × `lpi` (Blitzbereitschaft) — und flächig über DACH rendert (Vorwarnung 0–12 h **vor** dem ersten Radarecho; USP ggü. Windy/Kachelmann/WetterOnline, die die Felder nur einzeln/roh zeigen). Diagnose-Kern: alle drei Felder sind **reguläre** ICON-D2-2,2-km-Gitter (kein icosahedraler EPS-Pfad) → derselbe Decode wie Temp/Böen; `cape_ml` wird bereits punktweise geladen (`iconD2Cape.ts`), `convectiveIndex.ts` liefert wiederverwendbare Rampen/Schwellen und die Ehrlichkeits-Logik (Potenzial ≠ Auslösung); der Edge-`ALLOWED_PREFIX` deckt `icon-d2/grib/` schon ab → kein Transport-Umbau. **Jans harte Vorgabe: Lazy-Load wie alle anderen Layer** — `active.has('thunder') && !ref → installThunderRef` (Muster Clouds/Gust), **kein** Eager-Fetch am Kartenstart, nicht im `initialActive`-Default. Maßnahmen F1-1…F1-7 (Spec §5): neue Quelle `iconD2Thunder.ts`, reine Fusion `thunderPotential.ts` + Harness, `thunderRamp`/`visRange`, 7 additive `MapView.tsx`-Seams inkl. Lazy-Effekt, Legende/Tooltip mit Ehrlichkeits-Hinweisen, `scripts/verify-thunder.mjs`, Mobile-Sichtprüfung. Additiv, kein Eingriff in Wind-Shader/RGBA8/Fusion-Engine/EPS/Radar/Decode; `warm-grib`-Vorwärmung der drei Params optional/vertagt. Spec `audit/gewitterpotenzial.md`, Plan/Checklist Feature-Phase F1/Gate GF1, Protokoll V-GEWITTER. Umsetzung separat über die CLI. **STOPP & FRAGEN**, falls die Diagnose zeigt, dass `lpi`/`cin_ml` nicht regulär-gegittert vorliegen oder der Decode angefasst werden müsste.

**2026-07-23 · Infrastruktur-Phase T2c — Prod-Manifest-Advance-Fix UMGESETZT (Gate GT2c, bis auf 🔴 Prod grün).** Die Actions-Logs (4 warm-grib-Runs, alle success) widerlegen die Push-Race-Prime-Hypothese als beobachtete Ursache und belegen eine dreiteilige Kette: (1) Die Push-Kette funktioniert — Run 20:31Z advancte auf 18z und landete als Commit 7bb272d. (2) Jans lokaler Commit „improve Layer" (1b334bd, enthielt die Seed-Arbeitskopie von latest-grib.json) + Merge e4e888c setzten den Bot-Advance ~20 min später auf den localhost-Seed zurück — deshalb sah das Live-Audit den Seed. (3) Die Selbstheilung scheiterte danach dreimal in Folge an 2–5 transienten undici-Fetch-Fehlern unter ~130 Warm-Fetches (kein Retry; jedes Mal ein Near-Horizon-Step betroffen → Fail-Safe blockt den 2D-Advance; Wind mit ~10 Dateien trifft nie einen Fehler — die Asymmetrie wirkte wie ein Push-Race). Fix dreiteilig: `warmUrl`-Retry in warm-grib.mjs (2×, 1s/3s, nur transiente Fehler/5xx — durch den Log-Beweis autorisierte Ausnahme; Advance-Semantik unverändert), race-sicherer Commit-back in BEIDEN Workflows (Sichern→fetch→reset auf Remote-Tip→Drüberlegen→Push ×3 — shallow-sicheres Rebase-Äquivalent, da depth-1-Checkout keine Merge-Base hat; disjunkte Manifest-Dateien → konfliktfrei; heilt auch Merge-Regressionen automatisch), Wind-Cron auf 2,17,32,47 versetzt. Sandbox-Trockenlauf: alter Push im Race `! [rejected]`, neuer Loop landet (Versuch 1), Merge-Regression heilt am Folge-Tick, fremde Commits bleiben erhalten. Diff exakt 3 Dateien (kein Client/Decode/Fusion), typecheck grün, uncommitted. Verify-Log §J.4. 🔴 Jans Gate: committen/pushen, `workflow_dispatch` warm-grib, Prod-Manifest = aktueller Lauf + Prod-URL, 2D-Kaltload `hit`; Ops-Hinweis: latest-*.json nicht mehr von Hand mitcommitten.

**2026-07-22 · Diagnose-Phase T-AUDIT — Live-Netzwerk-Audit pro Layer DURCHGEFÜHRT (Gate GT-AUDIT grün).** Prod komplett vermessen (frischer isolierter Browser-Context, 1320 Requests / ~354 MB Session-Traffic; Ergebnis in `audit/live-network-audit.md` §3/§4). Kernbefunde: (1) **T2b ist deployt und wirksam** — EPS läuft über `/_dwd_grib` mit Edge-HITs (13,5 MB in 1,5 s frei; die alte 4–15-s-Origin-Baseline ist weg). (2) **Die zentrale Betriebslücke ist der Manifest-Advance:** Der warm-grib-Cron lief (~20:54Z) und wärmte 18z (HIT-Sonden 2D/EPS/Wind), aber Prod serviert weiter den committeten localhost-Seed (15z) → alle 117 2D-GRIB-Dateien der Session waren fwd=stale (Live-Proxy) und der Datenstand hängt einen Zyklus zurück (Wind nutzt parallel korrekt 18z). (3) Der Bare-Load startet mit 0 aktiven Layern, lädt aber t_2m 0–24 (24,5 MB) eager; der **Temperatur-Toggle triggert die volle EPS-Fusion (191,98 MB, ~17 s bandbreitenlimitiert)** plus 288-Request-brightsky-Sweep (der sich je Fusion-Toggle wiederholt). (4) Redundanzen: rv-Tar+Listing ×2 je Refresh, Sicherheits-Layer lädt 5 gecachte t_2m-Dateien erneut, 158 MeteoSwiss-Einzel-CSVs bei Stationen. §4 priorisiert 9 Maßnahmen (Nr. 1: Manifest-Advance fixen = Jans Betriebs-Gate; Nr. 7: T2b-4 Vor-Resampling als einziger echter Fusion-Lastbrecher); Fusion-Lade-Timing nur als STOPP-Option benannt. Read-only — kein Code, kein Deploy; neue gemeldete Quellen: S3-Terrain-DEM, maps.dwd.de WMS, GeoSphere (Rate-Limit 240/h), data.geo.admin.ch, open-meteo.

**2026-07-22 · Infrastruktur-Phase T2c — Prod-Manifest-Advance-Fix (Diagnose + Spec + Doku, Umsetzung offen).** Top-Hebel aus dem Live-Audit (Finding 1): der `warm-grib`-Cron wärmt in Prod den aktuellen Lauf (18z-Edge-HITs belegt), aber Prod liefert weiter den einkommitteten **localhost-Seed** `latest-grib.json` (2D-Lauf 15z, `warmedThroughProxy: localhost:5196`) → alle Clients laden die **ungewärmten** 15z-Dateien (117/117 `fwd=stale`, ~100 MB/Session, Daten 1 Zyklus alt). Wind advanced korrekt. Ursachen-Analyse: die beiden Warm-Workflows (`warm-wind.yml`/`warm-grib.yml`) sind identisch, beide `git push` **ohne** `pull --rebase`, **gleicher `*/15`-Zeitplan** → **Push-Race**: das langsamere Grib pusht nach Wind in ein bewegtes `main` → non-fast-forward-Reject, kein Retry → landet nie (`warmedThroughProxy: localhost` beweist: nie committet). Fix-Plan (§J): **T2c-1** Ursache aus Actions-Logs bestätigen; **T2c-2** Commit-Back beider Workflows race-sicher (`fetch`+`rebase origin/main`+Retry vor `push`; disjunkte Dateien → konfliktfrei) + optional Zeitpläne entzerren; **T2c-3** optional Netlify-Blobs statt Repo-Push; **T2c-0** interim `workflow_dispatch` erzwingt die erste Landung. Rein Ops/Transport, output-identisch, Client-Code unberührt. Prod-Verifikation (Branch-Protection, Live-Cron, `latest-grib.json`=aktueller Lauf + Prod-URL, 2D-Kaltload=Edge-HITs) = **Jans Gate**. Plan/Checklist GT2c, Protokoll V-TRANSPORT-2c.

**2026-07-22 · Diagnose-Phase T-AUDIT — Live-Netzwerk-Audit pro Layer (Auftrag + Doku, Ausführung durch CLI offen).** Jan will nach der T2b-Änderung die **deployte Prod-Site** systematisch vermessen lassen: Ziel-URL `https://buscosun.com/#m={"l":[50.2,10.5,"Deutschland · Österreich · Schweiz","DE"],"b":0,"h":0}` (Karten-Ansicht mit Ort → Punkt-Vorhersage + eager Fusion inkl. EPS). Auftrag: Bare-Cold-Load + **jeden Layer einzeln** durchschalten, **kompletten Netzwerk-Traffic** erfassen (Route je Request, Bytes, Dauer, **Cache-Status-Header** = Edge-HIT vs. Origin-MISS), dann **priorisierte Verbesserungen** ableiten. Methodik + Ergebnis-Template in `audit/live-network-audit.md` (§1 Methodik, §2 Ablauf, §3 Tabellen, §4 Analyse); Plan/Checklist als Diagnose-Phase T-AUDIT/Gate GT-AUDIT, Protokoll V-AUDIT. Reine Diagnose (Chrome DevTools MCP, Netzwerk emulator-belastbar), **kein Code/Deploy**; Fusion-Lade-Timing nur benennen (STOPP). Verifiziert nebenbei den T2b-Deploy (EPS via `/_dwd_grib`?).

**2026-07-22 · Infrastruktur-Phase T2b — EPS/icosahedral-Transport (Diagnose + Spec + Doku, Umsetzung offen).** Nach umgesetztem T2 waren die Kaltladezeiten weiter zu hoch; ein Prod-Traffic-Sample (Lauf 2026072215) deckte die Wurzel auf: nicht die T2-Layer, sondern die **ICON-D2-EPS-Dateien** (icosahedral, von der Fusion-Engine über `iconD2EpsSource`) mit **4–15 s je Datei** dominieren — sie laufen über `/_dwd_opendata` **ohne** Durable-Cache, weil der Edge-`ALLOWED_PREFIX` nur `weather/nwp/icon-d2/grib/` matcht, nicht `icon-d2-**eps**/grib/`. Zusätzlich sind EPS-Dateien ~16 MB entpackt (~20 Member, ~542 k Zellen) → auch der Client-Decode ist teuer. Fix-Plan T2b (Spec §H): **T2b-1** Edge-Whitelist um den EPS-Baum erweitern (`ALLOWED_PREFIX` → Liste); **T2b-2** EPS-Byte-Fetches über `/_dwd_grib` routen (Directory-Listing bleibt auf `/_dwd_opendata`, kein Decode-Eingriff); **T2b-3** EPS-Params in `warm-grib.mjs` mitwarmen; **T2b-4** (optional, größer) icosahedrales Vor-Resampling in den Cron → kompaktes Artefakt (Output-Äquivalenz, numerischer Beweis Pflicht). Rein Transport (T2b-1…3), additiv, Fusion-Blend-Logik + `/_dwd_opendata` + `/_dwd_wind` unberührt. **Ausdrücklich außerhalb:** *wann* Fusion EPS lädt (Deferral) = Fusion-Verhaltensänderung → STOPP-&-FRAGEN-Vermerk. Plan/Checklist T2b/Gate GT2b, Protokoll V-TRANSPORT-2b. Umsetzung via CLI; Prod-Deploy = Jans Gate.

**2026-07-19 · Infrastruktur-Phase T2 — Layer-Transport / Caching (Diagnose + Spec + Doku, Umsetzung offen).** Jan will das T1-Wind-Muster (Durable-Edge-Proxy + GitHub-Action-Warm-Cron + Manifest-Gate) auf alle Kartenlayer ausrollen. Zwei Code-Analysen (gegen den 4–12 Tage alten Memory-Stand verifiziert): **passt** für Temp (`t_2m`+`hsurf`), Gust (`vmax_10m`), Precip (`tot_prec`), Clouds (`clcl/clcm/clch/clct`) — alle ICON-D2-GRIB über `fetchStepField`/`fetchStepBytes` mit Default-`base`, immutable per-(Lauf,Step); der Edge-`ALLOWED_PREFIX` ist schon generisch `weather/nwp/icon-d2/grib/`, und der Refresh-Koordinator (`refreshIconD2Layers`) fächert die Layer bereits auf → der Umbau ist klein (eigener `base` je Layer + generische Route + Multi-Param-Manifest + Warm-Cron). **Passt nicht:** Radar/Nowcast/PoP (5-Min-Kadenz, ein Tar-Bündel statt per-Step-URLs, eigener Rewrite `/_dwd_opendata` + Cache `radolan-rv-tar-v1`, Live-WMS-Blitze) und Confidence (kein eigener Transport, rechnet über Temp/Precip/Radar-Frames). Spec T2-1…T2-7 in `audit/layer-transport.md`, Plan/Checklist als Infrastruktur-Phase T2/Gate GT2, Protokoll V-TRANSPORT-2. Output-identisch, additiv (`/_dwd_opendata`+`/_dwd_wind` unberührt), kein STOPP-Gate — aber Prod-Deploy + Cron = Jans Gate (Repo-Var `SITE_URL`, Branch-Protection-Bot-Push wie bei T1). Umsetzung separat über die CLI. 🔴 Latenz/Durable-Cache-`hit` erst nach Deploy belastbar.

**2026-07-19 · Querschnitt-Phase P3 — Repaint-Disziplin / Hebel 5 (Spec + Doku, Umsetzung offen).** Schließt die 5 Fachmann-Hebel ab (1/3 bereits gelöst, 4 = Phase P, 2 = Phase P2). Ziel: den einzigen selbst-perpetuierenden Wind-Repaint-Loop (`scheduleParticleRepaint`, laut `performance-2d.md` §1a Akku/Thermik-Treiber Nr. 1) pausieren, wenn nichts sichtbar ist — weniger Hitze → weniger thermisches Throttling → Zielrate hält länger, der Governor (P/P2) steppt seltener runter. Maßnahmen (§12): **P3-1** `visibilitychange`-Pause (Haupt-Win: `document.hidden` stoppt den Loop, sichtbar → einmal `triggerRepaint`); **P3-2** Offscreen-Pause via `IntersectionObserver` aufs Karten-Canvas; **P3-3** ein `paused`-Flag (hidden ∨ offscreen) gated nur den Self-Repaint-Pfad; **P3-4** `clearOnNextFrame` beim Fortsetzen. Niedrigstes Risiko aller Hebel — reine Event-/Scheduling-Logik, **kein** Shader-/RGBA8-/Trail-Ladder-/Fusion-Eingriff, kein STOPP-Gate. Anders als P/P2 **grösstenteils emulator-belastbar** (Loop-Stopp ist JS-beobachtbar); 🔴 Real-Device nur für den Akku-/Thermik-Gewinn (nice-to-have, nicht gate-blockierend). Spec `audit/webgl-cross-device.md` §12, Plan/Checklist Phase P3/Gate GP3, Protokoll V-PARITY-3. Umsetzung separat über die CLI.

**2026-07-08 · Phase 1-C — Wetterkarte „Variante C" (G1-C bestanden).** Die Mobilansicht der 2D-Wetterkarte wurde auf ein einziges persistentes Bottom-Sheet mit Segment-Umschalter (Layer · Modell · Vorhersage) und drei Snap-Zuständen umgebaut; Karte wieder vollflächig, Timeline schwebt synchron über der Sheet-Oberkante, Snap-Motion komplett transform-basiert (CLS 0.002, kein Long Task > 200 ms, max. 103 ms). Der Punkt-Forecast ist per Wrapper-Umzug als drittes Segment integriert (Sub-Tabs Übersicht/Diagramme/Tabelle erhalten, Render-Ort per `useMediaQuery`-Weiche — nie doppelt gemountet). Nebenbefund aus der Diagnose behoben: Wind-Feinsteuerung und Satellit-Produktwahl im Sheet waren seit dem §8-Follow-up durch ein zu breites `display:none !important` unsichtbar (Preservation-Punkte 6+7) — jetzt korrekt gescopet und wieder bedienbar; zusätzlich sind die Kartenlegenden mobil erstmals als Kapsel sichtbar statt ausgeblendet. Alle 13 Preservation-Punkte einzeln verifiziert, Desktop pixelgleich, Landscape 844×390 intakt; Audit: `audit/wetterkarte.md` §9, Screenshots: `audit/screenshots/wetterkarte/c-vorher-*` / `c-nachher-*`. Real-Device-TODO für Jan: Safe-Area-Insets (collapsed-Höhe/Footer-Padding) auf echtem iPhone prüfen.

**2026-07-18 · Querschnitt-Phase P — WebGL Cross-Device-Parität UMGESETZT (Gate GP, bis auf 🔴 Real-Device grün).** Die Entscheidung „Governor regelt FPS statt Partikel" ist implementiert (auf `main`, kein Branch, reine Governance-Logik): **P-1** — `governor.quality`-Multiplikator aus `WindLayer.getEffectiveParticleCount()` entfernt, jedes Gerät rendert die volle, CSS-flächen-gekoppelte Zahl (`floor(_numParticles × frac(zoom))`); **P-2** — `FrameGovernor` um einen FPS-Ziel-Modus erweitert (Leiter `[20,24,30]`, Schwellen relativ zum aktiven FPS-Ziel, down > 1,3×/up < 0,9×), auf `maxParticleFps`/`scheduleParticleRepaint` gelegt; kritisch: Governor wird **nach** den Pässen mit der **gemessenen Render-Dauer** gefüttert (nicht dem gecappten Wall-Clock-Intervall) — beide Pitfalls vermieden; **P-3** — `verify-governor.mjs` um 8 FPS-Modus-Checks erweitert, **27/27 PASS**; **P-4** — DPR-Cap 1,5 bestätigt (unverändert). MCP-Verifikation (Dev :5180): Dichte Desktop 3683 vs. Mobile (iPhone 12 Pro emuliert) 3722 Part./MPix (Verhältnis 1,011, beide GPU-Klasse „weak" — vor Phase P hätte der Governor Mobile gedrosselt), Mobile `drivesFps=true/targetFps=30`, Desktop `drivesFps=false/maxParticleFps=0` byte-identisch, keine neuen Konsolenfehler, Typecheck grün. FPS-Stepping/Thermik sind unter Emulation nicht belastbar (`ema`=0, rAF gedrosselt) → 🔴 Pflicht-Real-Device-Check iPhone 12 Pro **und** schwaches Android an Jan übergeben. Details/Selbstverifikation 1–5: `audit/webgl-cross-device.md` §9, Screenshot `audit/screenshots/wetterkarte/parity/mobile-iphone12pro-fps-mode.png`. — _(Diagnose-Eintrag desselben Tages siehe unten.)_

**2026-07-19 · Querschnitt-Phase P2 — Trail-Res-Governance / Hebel 2 UMGESETZT (Gate GP2, bis auf 🔴 Real-Device grün).** Der Governor-Letzthebel ist implementiert (auf `main`, kein Branch); das STOPP-Gate am RGBA8-Trail-Color-Pfad wurde streng respektiert. **P2-1** — `FrameGovernor` (FPS-Modus) um eine parallele `trailLadder` erweitert; ein einziger monotoner Index über die jetzt 4-stufige Leiter `[20/0,5, 20/1,0, 24/1,0, 30/1,0]` gibt die Wiederherstellungs-Reihenfolge automatisch her (FPS wird vor der Trail-Res verbraucht, Schärfe kommt bei Erholung zuerst zurück). **P2-2** — `allocScreenTextures` sized die Trail-Buffer mit `round(drawingBuffer × trailScale)`, realloziert nur bei Änderung von `(drawingBuffer, trailScale)`; `_epr` bleibt die **volle** `drawingBufferWidth/clientWidth`-Ratio (kein Doppel-Cap), `trailScale` als separater Faktor. **P2-3** — Trail-Texturen auf `gl.LINEAR` (weicher Upscale; bei 1,0 bit-exakter 1:1-Blit). **P2-4** — Point-Size × `trailScale` (CSS-Dicke nach Upscale konstant). **P2-5** — Viewport folgt bereits `screenWidth/Height`. Harness `verify-governor.mjs` +6 Trail-Checks → **35/35 PASS** (Abstieg …→trail0,5, Aufstieg trail0,5→… zuerst belegt). Abgrenzung bewiesen: Diff = 3 Files, `+`-Zeilen-Scan nach Float-/Shader-Tokens leer, `particleStateTexture` weiter `NEAREST`/voll, Trail-Targets RGBA8; Desktop byte-identisch (`governorDrivesFps=false` → `trailScale` immer 1,0). Typecheck grün. Live-`perfState`-Readout diesmal nicht erhältlich (Dev :5180 mountete den Wind-Layer nicht rechtzeitig) — unkritisch, da Trail-Stepping ohnehin 🔴-Real-Device-only. FPS-/Trail-Stepping unter Last, Partikel-Weichheit auf der Sprosse und Thermik: 🔴 Pflicht-Real-Device **mit visuellem Sign-off** (iPhone 12 Pro darf die Sprosse **nie** erreichen + schwaches Android) an Jan übergeben. Details/Selbstverifikation 1–5: `audit/webgl-cross-device.md` §11. — _(Spec-Eintrag desselben Vorhabens siehe unten.)_

**2026-07-18 · Querschnitt-Phase P2 — Trail-Res-Governance / Hebel 2 (Spec + Doku, Umsetzung offen).** Nach umgesetzter Phase P hat Jan Hebel 2 (Trail-Buffer 0,5×) freigegeben — der stärkste verbleibende Fillrate-Win, aber ein bewusst geöffnetes STOPP-Gate am RGBA8-Trail-*Color*-Pfad. Entscheidung (Jan): **Governor-Letzthebel** — die Trail-Auflösung fällt erst auf 0,5×, wenn der FPS-Abbau aus Phase P ausgereizt ist (Governor am FPS-Floor) und das Gerät weiter reißt; ein monotoner Ladder `[fps30]>[fps24]>[fps20]>[fps20,trail0,5]`, Erholung zieht die Schärfe zuerst zurück. Capable Mobiles (iPhone) erreichen die Sprosse nie → scharf; Desktop nie im FPS-Modus → byte-identisch. Wichtige Abgrenzung (Spec §10): berührt werden nur Auflösung + Filter der Trail-Color-Buffer sowie ein Point-Size-Skalar — **nicht** das Partikel-State-Positions-Packing (bleibt voll/`NEAREST`/byte-exakt) und **kein** GLSL-Edit; Trail-Targets bleiben RGBA8. Spec P2-1…P2-5 in `audit/webgl-cross-device.md` §10, Plan/Checklist als Phase P2/Gate GP2, Protokoll V-PARITY-2. Umsetzung separat über die CLI. 🔴 Real-Device Pflicht **mit visuellem Sign-off** (Weichheit der Partikel auf schwachem Android).

**2026-07-18 · Querschnitt-Phase P — WebGL Cross-Device-Parität (Diagnose + Doku, Umsetzung offen).** Jan legte eine externe Fachmann-Einschätzung zur Cross-Device-Performance vor; verifiziert gegen den Ist-Code (`audit/webgl-cross-device.md` §1): 5 Hebel — DPR-Cap 1,5 (bereits umgesetzt) und CSS-flächen-gekoppelte Dichte (besser als vorgeschlagen) erledigt, Adaptiv-Loop als `FrameGovernor` vorhanden; die Einschätzung nennt aber einen nicht existierenden Stack (Three.js/WebGPU/RepaintScheduler/WebGL2-Pfad — real ist MapLibre WebGL1). Eigene Fillrate-Bilanz (§2): die zwei Full-Screen-Trail-Pässe (~2,2 MPix/Frame) sind partikel-unabhängig, die Advektion (~0,005 MPix) skaliert als einzige mit der Partikelzahl → **Partikelzahl ist der schwächste Perf-Hebel**, den der Governor heute aber ausgerechnet drosselt. **Entscheidung Jan:** Governor regelt künftig **FPS statt Partikel** — volle, geräteübergreifend gleiche Partikeldichte, Performance über FPS-Abbau (30→24→20) + DPR-Cap; Desktop ungedeckelt = Referenz. Spec P-1…P-4 + kritische Umsetzungsfallen (Governor mit echter Render-Dauer füttern, Schwellen relativ zum FPS-Ziel) in `audit/webgl-cross-device.md` §4; Plan-/Checklist-/Test-Verzahnung als Phase P/Gate GP. **Umsetzung erfolgt separat über die Claude-Code-CLI** (dieser Durchlauf war nur Diagnose + Doku). Governor-bezogene Altdoku oben korrigiert (`FrameGovernor` statt Phantom-Komponenten); Three.js/WebGPU-Angaben im Stack-Abschnitt bleiben für Jan zur Klärung markiert (betreffen 3D-Globus, nicht dieses Vorhaben). 🔴 Real-Device-Pflichtcheck iPhone 12 Pro + schwaches Android vor Gate GP.

**2026-07-10 · Phase 1-C — Nachbesserung nach Jans Review (Gate G1-C bleibt bestanden).** Timeline auf den Mockup-Stil umgebaut (runder Ink-Button, Terracotta-Füllstand mit weißem Knob, Tick-Beschriftung, fetter Zeitstempel — Desktop-Slider unverändert), Vorhersage-Segment in den Z5-Look gebracht (redundanter Panel-Kopf im Sheet ausgeblendet, Sub-Tabs vergrößert, PFC-Interna unangetastet). Die gemeldete Überdeckung behoben: Legenden-Stapel wird im half-Zustand über der Timeline gedeckelt, und die seit der Vollflächen-Karte hinter dem Sheet verschwundene Karten-Attribution (+Maßstab) wieder sichtbar über die collapsed-Sheet-Kante gehoben (Preservation-Punkt 12). Details: `audit/wetterkarte.md` §9.8, Screenshots `c-nachher2-*`.

**2026-07-21 · Eventplaner „Command-Deck" (hell) — Vollumsetzung Desktop/Tablet/Mobile.** Der Event-Planer („Welcher Tag passt am besten?") wurde nach der verbindlichen Vorlage (`audit/screenshots/kartenseite/eventplaner.dc.html` + 12 PNGs) komplett auf das helle Command-Deck umgestellt — analog Kartenseite/Regenradar (Sand/Ink · League Spartan gescoped · Terracotta/Sage/Amber). Die Konfiguration ist jetzt ein **3-Schritt-Wizard vor der Resultatseite** (Ort&Anlass · Zeitfenster&Phasen · Plan B) für alle Breakpoints; das Resultat ist das bester-Tag-Deck (Vorhaben-Dock links · Hero/Ablauf/Karte/Rangliste Mitte · Risiko-Readout rechts; mobil vertikal + Bottom-Nav). **Funktionserhalt vollständig** (alle 9 Anlässe + additiv „Drohne", Geocode, Zeitraum/Einzeltermine, Phasen/Hochzeit, TuningPanel als eingeklapptes „Faktoren feinjustieren", Plan-B, Permalink, ICS, Share, findBetterLocation, Foto/Astro/Hochzeit-Sektionen). Neue Dateien/Änderungen: `EventPage.tsx` (Rewrite), `EventResult.tsx` (Recommendation+Wrapper neu, tote Leaf-Komponenten entfernt), `eventDeck.css` (neu), `eventModel.ts`/`eventScoring.ts` (+drone), `eventIcons.tsx` (+Deck-Icons), `designTokens.css` (+`--evd-*`). `scoreColor` auf Deck-Palette (Sage=gut). MCP-verifiziert in 3 Viewports gegen alle 12 Referenzen, `tsc -b` + `vite build` grün, Konsole fehlerfrei. Schrift-Entscheidung Jan: League Spartan (scoped). Uncommitted; Dev lief auf :5187. Details siehe Memory `eventplaner-command-deck.md`.

**2026-07-22 · Infrastruktur-Phase T2 — Layer-Transport / Caching für alle ICON-D2-Layer UMGESETZT (Gate GT2, bis auf 🔴 Prod-Deploy grün).** Das T1-Wind-Muster (Durable-Edge-Proxy + Warm-Cron + Manifest-Gate) ist auf Temp/Gust/Precip/Clouds ausgerollt — output-identisch, rein additiv: neue Edge Function `netlify/edge-functions/dwd-grib.ts` (`/_dwd_grib/*`, durable+immutable, Whitelist), Quellen `iconD2Temp/Gust/Clouds/Precip` per `base`-Seam auf den Proxy (inkl. `hsurf`; kein Decode-/Norm-/Shader-Eingriff), kombiniertes Multi-Param-Manifest `public/latest-grib.json` + geteilter Resolver `src/sources/gribManifest.ts` (24h-Staleness-Guard, Scan-Fallback) als Manifest-First in `resolveLatestRun` — damit greift das Gate automatisch auch für den 30-min-Refresh-Koordinator. Warm-Cron `scripts/warm-grib.mjs` + `.github/workflows/warm-grib.yml` (Caps t_2m/vmax 24, tot_prec 27, Clouds 12; Near-Horizon-Fail-Safe; Early-Exit vergleicht ggü. T1 zusätzlich die Step-Listen wegen progressiver ICON-D2-Publikation). Verifiziert (V-TRANSPORT-2, Audit §G): Verifier 43/43 (SHA-256-Byte-Identität je Param + hsurf, Header, no-store), Kaltload je Layer 0 Directory-Listings / 0 `/_dwd_opendata`-GRIB-Requests / Manifest 4 ms (vorher 1 Listing 83–157 ms je Layer), Screenshots vorher/nachher pixel-identisch (gleicher Lauf 2026072215), Fallback ohne Manifest greift einmalig ohne 404-Sturm, Warmer idempotent+fail-safe (4 Probeläufe), Diff-Beleg: wind/scalar/fusion/radolan/Decode/`dwd-wind.ts`/`netlify.toml` unberührt, Typecheck grün, Konsole sauber. 🔴 An Jan: Netlify-Deploy (Durable-`hit`-Header + Latenz je Param), Repo-Var `SITE_URL` vorhanden aus T1, `warm-grib`-Cron aktiviert sich mit dem Push — Branch-Protection kann den Bot-Commit-back blocken (wie T1). Nebenbefund (vorbestehend, dev-only): StrictMode+kalter Cache kann den Temp-Mount-Load verklemmen (`tempLoadingRef`) — dokumentiert in Audit §G, nicht angefasst.

**2026-07-22 — Infrastruktur-Phase T2b (EPS/icosahedral-Transport, GT2b):** Die 4–15-s-Kaltload-Sünder (ICON-D2-EPS, Fusion-Engine) laufen jetzt über den durablen `/_dwd_grib`-Proxy: Edge-Whitelist um den EPS-Baum erweitert, `iconD2EpsSource` mit Base-Split (Listing auf `/_dwd_opendata`, Bytes über den Proxy), Warm-Cron wärmt den eigenen EPS-Lauf (5 Variablen × Steps 0/3/6 + clat/clon) mit unabhängigen Fail-Safes je Familie und `eps`-Manifest-Abschnitt. Output-Identität bewiesen: SHA-256 beider Pfade bit-identisch, Fusion-Sonde (2646 Werte) vierfach reproduziert deterministisch; Verifier 73/73, Typecheck grün, Diff auf genau 4 Dateien begrenzt (Fusion/Decode/Radar/Wind unberührt). Verify-Log audit/layer-transport.md §I; T2b-4 (Vor-Resampling) bewusst offen; Prod-Durable-`hit` = Jans Gate. Uncommitted.

**2026-07-24 · Feature-Phase F1 — Gewitterpotenzial-Layer UMGESETZT (Gate GF1 grün).** Neuer, standardmäßig **inaktiver** Karten-Layer „Gewitter": fusioniert die drei nativen ICON-D2-Felder `cape_ml` (Energie) × `cin_ml` (Deckel) × `lpi` (Blitzbereitschaft) zu einem 0–100-Index je Zelle — flächige Vorwarnung 0–12 h **vor** dem ersten Radarecho. **Diagnose-First** (`audit/gewitterpotenzial.md` §8): alle drei Felder sind reguläre lat-lon-Gitter (GDT 0) → Decode wie Temp/Böen, **kein** EPS-/Decode-Eingriff; CIN decodiert **am echten Feld** als positiver Betrag (0..322 J/kg) **mit −999,9-Fill** auf ~236 k Zellen → `cinGate` sentinel-fest (|CIN|≥900 → kein Deckel), sign-agnostisch; Domänenrand = NaN → transparent (nie 0). Architektur-Entscheidung (§8.4): rohe 3-Feld-Fusion über `fetchStepField` (wie `iconD2TempSource` t_2m+hsurf→ein Canvas), **nicht** `fetchIconD2Grid` (Uint8-quantisiert). Neue Dateien: `src/radar/thunderPotential.ts` (reine Fusion + Harness, 13/13), `src/sources/iconD2Thunder.ts` (Grid-Loader), `scripts/verify-thunder.mjs`. Additive Seams in `MapView.tsx` (LayerKey/Option/statuses/Ref/Init-ScalarLayer/2×Visibility/refresh/Slider-Effekt/DECK_GROUPS/Legende) + `convectiveIndex.ts` (nur 2× `export` von `ramp`/`capeScore`, Verhalten unverändert), `LayerIcon.tsx` (Gewitterwolke-Case), `LayerInfoPanel.tsx` (Tooltip). **Lazy-Load MCP-belegt** (kalt-Cache, entkappter Resource-Buffer): 0 `cape_ml`/`cin_ml`/`lpi`-Requests vor Aktivierung, nach Toggle alle drei via `/_dwd_grib` (Lauf 2026072406). Rendering: 24.07. ist eine schwache Konvektionslage über DACH (max Score 19 = „gering"), Diurnalzyklus korrekt (Nachmittag Step 10 max 42), Domänenrand 17 % transparent, volle Rampe rendert bei erzwungenem visRange {0,0} (Beleg des Render-Pfads). Mobile 390×844: Toggle im Sheet (Gruppe Niederschlag), Touch-Target 358×56 px, Legende voll lesbar. `npm run typecheck` grün, Konsole (Desktop+Mobile) fehlerfrei. `warm-grib`-Vorwärmung der drei Params bewusst vertagt (§6). Uncommitted; Dev lief auf :5193.

**Selbstverifikation F1 (5 Fragen, Beleg-gestützt):**
1. **Alle bestehenden Funktionen erhalten?** Ja — F1 ist rein additiv: neuer Layer + neue Dateien; die einzige Änderung an Bestandscode ist `export` vor `ramp`/`capeScore` (Verhalten byte-gleich) sowie additive Seams (neue Objekt-Einträge/Effekte/JSX-Zweige). Kein bestehender Layer/Loader/Decode/Shader angefasst. `git diff` bestätigt: Wind/RGBA8/Fusion/EPS/Radar/Decode unberührt.
2. **Desktop unverändert?** Ja — der Layer ist nicht im `initialActive`-Default und Sichtbarkeit=off; mit ausgeschaltetem Layer ist die Karte unverändert (nur eine zusätzliche Toggle-Zeile im Panel = die Funktion selbst). MCP-Desktop vor Toggle = normale Karte.
3. **Touch-Targets ≥ 44×44 px?** Ja — Gewitter-Toggle im Mobile-Sheet gemessen 358×56 px.
4. **Konsole frei von neuen Errors/Warnings?** Ja — MCP `list_console_messages` (error/warn) Desktop und Mobile: leer.
5. **Keine Long Tasks > 200 ms in der Interaktion?** Loader läuft off-main (bz2-Worker-Pool + Grid-Decode-Pool, geerbt aus der Temp/Böen-Pipeline), progressiv je Frame; MCP-Kaltload aller 13 Frames ~1,7 s ohne UI-Blockade, Slider reagiert flüssig. (Belastbares Long-Task-Trace nur Real-Device, da die MCP-Emulation rAF drosselt — bekannte Randbedingung.)

**2026-07-24 · Feature-Phase F2 — Blitz-Vorhersage-Layer (LPI) UMGESETZT (Gate GF2 grün).** Neuer, standardmäßig **inaktiver** Karten-Layer **„Blitzprognose"**: rendert das native ICON-D2-Feld `lpi_max` (Lightning Potential Index, J/kg) als flächiges Blitz-**Risiko** über DACH und läuft über den Slider 0–12 h in die **Zukunft** — eine echte Prognose, klar getrennt vom bestehenden „Blitze"-Layer (`dwdLightning.ts`, `Accumulated_Flash_Area` = gemessene Einschläge der letzten 60 Min). Der einfachere Zwilling von F1 (ein Feld statt drei, **keine** Fusion). **Diagnose-First** (`audit/blitz-vorhersage.md` §8): `lpi_max` ist ein reguläres lat-lon-Gitter (GDT 0, `regular-lat-lon` single-level) → Decode wie Temp/Böen, **kein** EPS-/Decode-/`frameAtValidTime`-Eingriff; `lpi_max` ist als Intervall-Maximum am Analyse-Schritt t+0 strukturell 0 → gelöst mit dem vorhandenen `minStepHours=1`-Knopf (wie Böen) + Loader lädt Steps 1–12; Domänenrand = NaN → transparent. Architektur-Entscheidung (§8.4, wie F1): Rohwert über `fetchStepField` (Böen ist der Ein-Feld-Präzedenzfall), **nicht** `fetchIconD2Grid(kind:'max')` — das existiert nicht (`GridToU8Kind` = precip|cloud|cape), und `'cape'`-Quantisierung zerquetscht LPIs 0..~30-J/kg-Bereich. Neue Dateien: `src/sources/iconD2Lpi.ts` (Grid-Loader, R = lpi/30 normiert), `src/radar/lightningPotential.ts` (reine `lpiRisk`/`lpiLevelOf`-Rampe + Harness, 6/6), `scripts/verify-lpi.mjs`. Additive Seams in `MapView.tsx` (LayerKey `'lightningfc'`/Option/statuses/Ref/`LIGHTNINGFC_LAYER_ID`+`lpiRamp`+Init-ScalarLayer/addLayers/2×Visibility/refresh-Zweig/Slider-Effekt mit `minStepHours=1`/Legende/DECK_GROUPS) + `LayerIcon.tsx` (Umriss-Bolt-Case) + `LayerInfoPanel.tsx` (Tooltip). Palette **bewusst violett-forciert** (Gelb→Amber→Rot→Magenta→Elektrik-Violett) → optisch getrennt von der amber-Blitzortung UND der Gewitter-Rampe. **Lazy-Load MCP-belegt** (Dev :5194): Kartenstart 194 fetch/xhr, **0** `lpi_max`; nach Toggle Steps 001–012 via `/_dwd_grib` (Lauf 2026072406). „Blitze" + „Blitzprognose" gleichzeitig aktiv, optisch/tooltip-unterscheidbar („Prognose ≠ Messung", Verweis aufeinander). 24.07. ruhige Lage → LPI ~0 → weitgehend transparent (dokumentiert-korrekt, §2.4). Mobile 390×844: Toggle im „Layer"-Sheet (Gruppe „Punkte & Vertrauen", neben „Blitze"), Touch-Target 56 px, Legende sichtbar. `dwdLightning.ts`/`gribDecode.ts`/`ScalarLayer.ts`/Wind-Shader/RGBA8/Fusion/EPS/Radar: leerer Diff. `npm run typecheck` grün, `npm run verify:lpi` 6/6, Konsole (Desktop+Mobile) fehlerfrei. `warm-grib`-Vorwärmung von `lpi_max` bewusst vertagt (§6). Uncommitted; Dev lief auf :5194.

**2026-07-24 · Feature-Phase F3 — Simuliertes-Radar-Layer (dbz_cmax) UMGESETZT (Gate GF3 grün).** Neuer, standardmäßig **inaktiver** Karten-Layer **„Sim-Radar"**: rendert die native ICON-D2-Composite-Reflektivität `dbz_cmax` (dBZ) in der **gewohnten Radar-Optik** über DACH und läuft über den Slider 0–12 h in die **Zukunft** — simulierte Modell-Reflektivität, die das Radarbild **jenseits** des 2-h-Nowcast-Horizonts verlängert (dort ist das gemessene Radar/„Niederschlag" präziser; klar als „simuliert" gelabelt). **Diagnose-First** (`audit/simuliertes-radar.md` §8): Live-Sonde bestätigt `dbz_cmax` als **regular-lat-lon**-Gitter (Steps 000–048) → Decode wie Temp/Böen; **Step 000 vorhanden → instantanes Säulen-Maximum, t+0 gültig, KEIN `minStepHours`** (anders als `lpi_max`); Reuse via `fetchStepField` (nicht `fetchIconD2Grid` — `GridToU8Kind` kennt kein `'cmax'`, würde `gribGridDecode.ts` anfassen). Für die „gewohnte Optik" wurde `radarModel.ts` um den **additiven** Export `dbzToMmh` (Inverse zu `mmhToDbz`, Marshall-Palmer) ergänzt — **kein** bestehendes Verhalten geändert (Bestand byte-identisch; Präzedenz F1/`convectiveIndex.ts`); der Layer bildet dBZ → mm/h → `t = mm/h/PRECIP_VMAX` ab und rendert mit **derselben `precipRainRamp` wie `nowcast`** → dasselbe mm/h = dieselbe Farbe wie das gemessene Radar. Neue Dateien: `src/sources/iconD2Dbz.ts` (Ein-Feld-Loader, Steps 000–012, R=t + NaN-Maske), `scripts/verify-simradar.mjs` (21/21 PASS: dBZ↔mm/h-Rundlauf, Monotonie, 5–12-dBZ-`visRange`-Fenster, Optik-Konsistenz). Additive Seams in `MapView.tsx` (LayerKey `'simradar'`/Option/statuses/Ref/`SIMRADAR_LAYER_ID`+Init-ScalarLayer mit `colorRamp: precipRainRamp` + `visRange {0.004,0.011}`/addLayers/2×Visibility/refresh-Zweig/Slider-Effekt **ohne** `minStepHours`/Legende/DECK_GROUPS) + `LayerIcon.tsx` (Radar-Sweep-Case) + `LayerInfoPanel.tsx` (Tooltip). **Lazy-Load MCP-belegt** (Dev :5195, Desktop 1440×900): Kartenstart 126 fetch/xhr, **0** `dbz_cmax`; nach Toggle Directory-Probe `/_dwd_opendata/…/dbz_cmax/` + Steps **000–012** via `/_dwd_grib` (Lauf 2026072409, regular-lat-lon), alle 200. Horizont-Mehrwert bei **+6 h** verifiziert (Layer aktiv jenseits des Nowcast-Endes). 24.07. DACH-weit trocken (0 % Niederschlag) → `dbz_cmax` flächig ~0 → Layer korrekt transparent (kein Fehler, wie F1/F2 an ruhigen Tagen; Renderpfad über 13 dekodierte Frames + leere Konsole belegt). Mobile 390×844: Toggle im „Layer"-Sheet (Gruppe Niederschlag, neben „Niederschlag"), Touch-Target 453×56 px, Legende „Sim-Radar · simuliert" sichtbar. `nowcast`/RainLayer/RADOLAN/`radarModel.ts`-Verhalten/`ScalarLayer.ts`/`gribGridDecode.ts`/Wind-Shader/RGBA8/Fusion/EPS: unberührt (`radarModel.ts`-Diff = nur der neue `dbzToMmh`-Export). `npm run typecheck` grün, `npm run verify:simradar` 21/21, Konsole (Desktop+Mobile) fehlerfrei. `echotop`-Punkt-Readout + `warm-grib`-Vorwärmung von `dbz_cmax` bewusst vertagt (§6). Uncommitted; Dev lief auf :5195.

**2026-07-24 · Feature-Phase F4 — Schnee-Layer (h_snow / snow_gsp) UMGESETZT (Gate GF4 grün).** Neuer, standardmäßig **inaktiver** Karten-Layer **„Schnee"** mit zwei umschaltbaren Modi (In-Layer-Segment analog `SAT_PRODUCT`): **Schneedecke** (`h_snow`, aktuelle Höhe, m→cm, instantan → t+0) und **Neuschnee** (akkumulierter Schneefall `snow_gsp`+`snow_con`, kg/m² = mm SWE → cm). Zeigt die Schnee-**MENGE als Fläche** (cm) — **nicht** die Schneegrenzen-**Linie** (das bleibt der bestehende ML-`snowline`-Layer, unverändert). **Diagnose-First mit echtem Decode** (`audit/schnee.md` §8, temporäres `diag-snow-fields.mjs`): alle Schnee-Felder **regular-lat-lon** GDT 0 (Steps 000–048) → Decode wie Temp/Böen; **kritische `freshsnw`-Korrektur am Feld belegt** — `freshsnw` dekodiert zu [0,00…1,00] = Frische-/Albedo-Faktor (cat/num 1/203), **NICHT** cm-Neuschnee → Neuschnee kommt aus `snow_gsp` (1/56) + `snow_con` (1/55); `h_snow` (1/11) instantan → t+0 gültig, **kein `minStepHours`**; `snow_gsp` Step 0 = 0 (akkumuliert seit Laufbeginn) → **`minStepHours=1`** + Design-Entscheidung „kumulativer Wert statt Δ" (erfüllt V-SCHNEE #4 „Summe wächst mit Horizont"). Reuse via **additivem** Export `freshSnowCmFromSwe` in `alpineSplit.ts` (nutzt die vorhandene `SNOW_RATIO_CM_PER_MM`-Konstante; `rho_snow` bevorzugt, aber nur im plausiblen Frischschnee-Bereich 30–250 kg/m³, sonst 10:1 — verhindert Unterschätzung durch alten dichten Pack; Bestand byte-identisch). Neue Dateien: `src/sources/iconD2Snow.ts` (Ein-/Mehrfeld-Loader je Modus, R = cm/VMAX + NaN-Maske; VMAX Decke 150 / Neu 50), `scripts/verify-snow.mjs` (20/20 PASS am echten `freshSnowCmFromSwe`). Additive Seams in `MapView.tsx` (LayerKey `'snow'`, `SnowMode`-State + Labels analog `SAT_PRODUCT`, `snowModeRef`+`snowSeqRef`-Guard, `SNOW_LAYER_ID`+Init-ScalarLayer mit `snowRamp` aus `precipPhase.ts`, beide Visibility-Blöcke, refresh-Zweig, Slider-Effekt mit modusabhängigem `minStepHours`, Modus-Wechsel-Effekt = lazy Refetch, `snowSeg`-Umschalter in Desktop-Dock + Mobile-Sheet, Legende, `DECK_GROUPS`) + `LayerIcon.tsx` (Schneeflocke-über-Linie) + `LayerInfoPanel.tsx` (+`SNOW`-Gradient). **Lazy-Load + Modus-Lazy MCP-belegt** (Dev :5197): Kartenstart **0** Schnee-Requests; Toggle (Default Decke) → `h_snow` Steps 000–024 via `/_dwd_grib` (Lauf 2026072409); Modus→Neuschnee lädt LAZY `snow_gsp`+`snow_con`+`rho_snow` Steps **001**–024 nach. cm-Plausibilität visuell: Ostalpen-Gletscher (Hohe Tauern/Ötztal) blau (bis 261 cm) > Flachland transparent (MCP-Recenter). 24.07. sommertrocken → Neuschnee flächig 0 = korrekt. Mobile 390×844: Toggle 453×56 px, Modus-Buttons 209×44 px, kein Horizontal-Scroll, Legende sichtbar. `snowline`/`climaField`/`precipPhase.ts`-Verhalten/`ScalarLayer.ts`/`gribGridDecode.ts`/Wind-Shader/RGBA8/Fusion/EPS/Radar: unberührt (`git status` leer; `alpineSplit.ts`-Diff = nur der neue Export). `npm run typecheck` grün, `npm run verify:snow` 20/20, Konsole (Desktop+Mobile) fehlerfrei. `freshsnw`-Optik-Anreicherung + `warm-grib`-Vorwärmung bewusst vertagt (§6). Uncommitted; Dev lief auf :5197.

**2026-07-24 · Feature-Phase F5 — Rotationspotenzial-Layer (Experten, uh_max×uh_max_low×sdi_2) UMGESETZT (Gate GF5 grün).** Neuer, standardmäßig **inaktiver** **Experten-**Karten-Layer **„Rotation"**: fusioniert die nativen ICON-D2-Felder `uh_max` (Updraft-Helicity 2–5 km) + `uh_max_low` (0–3 km) + `sdi_2` (Supercell Detection Index) zu einem **geglätteten** 0–100-**VERDACHTS**-Score für rotierende Aufwinde/Superzellen (Großhagel/organisierte Schwergewitter), Slider 0–12 h. Storm-Chaser-Nische. **⚠️ Ehrlichkeits-Leitplanken §0 sind gate-blockierend** und im UI umgesetzt. **Diagnose-First mit echtem Decode** (`audit/rotationspotenzial.md` §8, temporäres `diag-rotation-fields.mjs`): (a) alle drei Felder **regular-lat-lon GDT 0** (1215×746, DACH) → Decode wie Temp/Böen via `fetchStepField`, **nicht** EPS/`fetchIconD2Grid` (Rohfusion braucht Float); (b) Identität via GRIB-Sektion 4 bestätigt (uh_max cat7/num15 level 2000, uh_max_low level 0, sdi_2 cat7/num193), UH **signiert** → Fusion über `|·|` (rotationsrichtungs-invariant); (c) **kritischer Kalibrier-Befund:** ICON-D2-`uh_max` ist **einstellig** (m²/s², Hotspots ~4–8) — **~100× kleiner** als der Spec-Vorschlag (25…250) annahm, `sdi_2` betragsmäßig winzig (|max|<5e-4 am ruhigen 24.07.). Mit den Spec-Zahlen bliebe der Layer dauerhaft leer. (d) **STOPP & FRAGEN** (§0/§6c): die harte Vorgabe „gegen echten Schwergewitter-Lauf kalibrieren" ist mangels verfügbarem Lauf nicht erfüllbar → **Jan gab „konservativ bauen" frei**; Rampen an gemessener Skala verankert (`uhScore` 5→15…60→100; `sdiScore` boost-only), großzügige Aktivierungsschwelle ~Score 20, oberer Anker als nachkalibrierbar markiert. Neue Dateien: `src/radar/rotationPotential.ts` (reine Fusion + 3×3-Max→5×5-Mittel-Glättung + Harness + `window.__verifyRotationPotential`, 30/30), `src/sources/iconD2Rotation.ts` (Loader, Steps 1–12, `minStepHours=1`, R=geglätteter Score/100 + NaN-Maske), `scripts/verify-rotation.mjs`. Additive Seams in `MapView.tsx` (LayerKey `'rotation'`/Option/statuses/Ref/`ROTATION_LAYER_ID`+`rotationRamp`+Init-ScalarLayer `visRange {0.18,0.24}`/addLayers/2×Visibility/refresh-Zweig/Slider-Effekt `minStepHours=1`/Legende/DECK_GROUPS) + `LayerIcon.tsx` (Rotations-Spirale) + `LayerInfoPanel.tsx` (+`ROTATION`-Gradient). **Lazy-Load im CDP-Waterfall belegt** (Dev :5198): vor Toggle **0** Rotations-Requests (Cache geleert), nach Toggle je Steps **001–012 × {uh_max,uh_max_low,sdi_2}** = 36 via `/_dwd_grib` (Lauf 2026072412), alle 200; generischer Proxy → kein `dwd-grib.ts`/`warm-grib`-Eingriff. `minStepHours=1` greift (kein Step 000); SDI-Sign/Range im Decode dokumentiert (Konsole `sdi_2 decode min=-2.01e-4 max=1.99e-4`). 24.07. rotationsschwach → nach Glättung erwartungsgemäß transparent über DACH (ehrlicher Under-Paint, §0.4). **Ehrlichkeit (gate-blockierend) im UI belegt:** Tooltip **und** Legende (Desktop+Mobile) benennen „kein amtliches Warnprodukt, kein Warnersatz — maßgeblich sind die DWD-Warnungen", „Verdacht ≠ Ereignis", „hohe Fehlalarmrate", „Experten-Layer", „bewusst geglättet"; **Wort „Tornado" nirgends** (§4.2-Titelvorschlag bewusst zugunsten §0.2 in „Superzellen: Großhagel, organisierte Schwergewitter" umformuliert). Mobile 390×844: Toggle im „Layer"-Sheet (Gruppe Niederschlag, neben „Gewitter"), Touch-Target 358×56 px, violette Aktiv-Optik + Spiral-Icon, Legende voll sichtbar. `convectiveIndex.ts` nur gelesen (`import ramp`), `dwdAlerts`/Wind-Shader/RGBA8/Fusion/EPS/Radar unberührt. `npm run typecheck` grün, `npm run verify:rotation` 30/30, Konsole (Desktop+Mobile) fehlerfrei. `uh_max_med`/`w_ctmax` + `warm-grib`-Vorwärmung + Nachkalibrierung des oberen Ankers gegen einen echten Superzellen-Lauf bewusst vertagt (§6). Uncommitted; Dev lief auf :5198.

**Selbstverifikation F5 (5 Fragen, Beleg-gestützt):**
1. **Alle bestehenden Funktionen erhalten?** Ja — F5 ist rein additiv: neuer Layer + 3 neue Dateien; Bestandscode nur additiv erweitert (Union/Records/Seams). `convectiveIndex.ts` nur gelesen (`import { ramp }`, die `M`-Markierung stammt aus F1). Kein bestehender Layer/Loader/Decode/Shader angefasst; `dwdAlerts`/Wind/RGBA8/Fusion/EPS/Radar unberührt.
2. **Desktop unverändert?** Ja — Layer nicht im `initialActive`-Default, Sichtbarkeit=off; mit ausgeschaltetem Layer ist die Karte die Standardkarte (nur eine zusätzliche Toggle-Zeile = die Funktion selbst). MCP-Desktop vor Toggle = normale Karte.
3. **Touch-Targets ≥ 44×44 px?** Ja — Rotation-Toggle im Mobile-Sheet gemessen 358×56 px.
4. **Konsole frei von neuen Errors/Warnings?** Ja — MCP `list_console_messages` (error/warn) Desktop und Mobile: leer. Einzige Rotations-Ausgabe = ein bewusster Dev-Debug (`sdi_2 decode …`, Beleg für §7.3).
5. **Keine Long Tasks > 200 ms in der Interaktion?** Loader läuft off-main (bz2-Worker-Pool + Grid-Decode geerbt aus der Temp/Böen-Pipeline), progressiv je Frame; Glättung ist O(w·h·25) auf dem ~700-breiten Subsample-Grid je Frame (vernachlässigbar). Slider reagierte flüssig. (Belastbares Long-Task-Trace nur Real-Device, da MCP-Emulation rAF drosselt — bekannte Randbedingung.)

**2026-07-24 · Konsolidierungs-Phase N1 — Eine Niederschlags-Ansicht „Niederschlag · jetzt–12 h" (Radar → Modell, nahtlos) UMGESETZT (Gate GN1 grün).** Die zuvor über `precipFrameReady` + diverse Sichtbarkeits-Booleans in `MapView.tsx` VERSTREUTE Radar↔Modell-Quellenwahl an EINE reine, testbare Stelle gehoben (`src/nowcast/precipSource.ts`) und den separaten SIM-Radar-Layer (F3, `simradar`/`iconD2Dbz`) restlos stillgelegt — Jans Entscheidung: 2–12 h bleiben der Fusion-/Modell-Niederschlag (mm/h), nicht `dbz_cmax`. **Kernbefund der Code-Lesung (Spec-Addendum §10):** der 2-h-Seam existiert bereits IM `PrecipCompositor` (per-Zelle Radar→ICON-D2 bei identischer Palette), und die RainLayer↔`precip-forecast`-Wahl ist die Fusion⇄Native-Modellquellen-Achse (nicht die Stunde) → NICHT umbauen, nur zentralisieren. **N1-1** `precipSource.ts`: `resolvePrecipSource(hour,country,avail)` → `{kind:'radar'|'model', ready, seamBlend, hold?}` + `precipCompositeReady`/`precipCompositeKind` + `verifyPrecipSource()`; `RADAR_HORIZON_H` DE 2/AT 3/CH 0,5 (CH strikt `<0,5`, deckungsgleich Kompositor+Legacy); Lücken-Fallback `hold:'radar'` (nie leer). **N1-2** `scripts/verify-precip-source.mjs` (Node --experimental-strip-types, kein Vitest) → **30/30 PASS**, inkl. Komposit-`ready` ≡ altes `precipFrameReady` über alle 2³×4 Verfügbarkeits-Kombinationen (Output-Identität by construction gesperrt). **N1-3** `precipFrameReady` zu dünnem Wrapper um `precipCompositeReady(precipAvailability())` gemacht — Sichtbarkeits-Booleans byte-genau erhalten, kein DE/AT/CH-Bruch. **N1-4** `seamBlend` (monoton, geprüft) als Naht-Metadaten (kein Live-Opazitäts-Crossfade → Output-Identität bleibt); Timeline spannt die volle Spanne AUCH im `START_NOW_ONLY`-Testmodus: `sliderMax` liest bei aktivem Niederschlag/Wolken den ICON-D2-Horizont, und `installIconD2` lädt bei aktivem Niederschlag (`nowcastActiveRef`) den vollen 0–12-h-Precip-Horizont statt des +2-h-Fensters (MCP: Slider 0–24 h in beiden Modi). **N1-5** SIM-Radar restlos entfernt: alle Seams in `MapView.tsx` (Import/`SIMRADAR_LAYER_ID`/LayerKey/Option/statuses/Refs/Layer-Init/addLayers/2×Visibility/installSimRadar/refresh/lazy-Effekt/Slider-Effekt/Legende/DECK_GROUPS), `src/sources/iconD2Dbz.ts` **gelöscht**, `LayerIcon`/`LayerInfoPanel`-Bezug entfernt; `git grep simradar` in `src/` **leer** (nur `Sim-Radar`-Historie im `radarModel.ts`-Kommentar); `radarModel.ts` (`dbzToMmh`/`mmhToDbz`) bleibt und speist weiter das Regenradar (`expertDbz`); F3-Doku (`audit/simuliertes-radar.md` + plan/checklist/tests) als „stillgelegt zugunsten N1" markiert (Historie erhalten). **N1-6** Label/Tooltip/Info-Panel/Deck-Sub auf „Niederschlag · jetzt–12 h (Radar → Modell, nahtlos)"; keine Radar/Modell-Wahl mehr in der UI. **N1-7** `docs/niederschlag-architektur.md` (PrecipSource-Abstraktion, RainLayer/ScalarLayer, Quellen DE RADOLAN-RV/AT INCA/CH rzc 0–2 h · Fusion 2–12 h, Umschaltlogik, 0–12-h) + Verweis im Radar-Feature-Katalog. **Verify (V-NIEDERSCHLAG, MCP Dev :5203):** Harness 30/30; `npm run typecheck` grün; Konsole (Desktop+Mobile) leer; Datenlage „DACH-KOMPOSIT · DE RADOLAN · AT INCA · CH RZC · + ICON-D2"; kein SIM-Radar-Toggle/-Legende/-Deck (Snapshot Desktop+Mobile); DE/AT/CH-Tabs + Fusion/Native-Selektor funktionsfähig; `git diff -- src/fusion/` **leer**; Slider 0–24 h in beiden Modi; Mobile 390×844 Touch-Target 358×56 px; Screenshots in `audit/screenshots/niederschlag/`. **Output-Identität** by construction (kein Render-Pfad geändert) + Harness-gesperrt. Uncommitted (Jans Commit-Gate); Dev lief auf :5203. Hinweis: WebGL-Rendern im MCP-Emulator ist NICHT pixel-repräsentativ (CLAUDE.md) → Feld-Screenshots dokumentieren UI/Label/Legende, nicht die Precip-Pixel.

**Selbstverifikation N1 (5 Fragen, Beleg-gestützt):**
1. **Alle bestehenden Funktionen erhalten?** Ja — die vereinheitlichte Ansicht rendert je Stunde denselben Layer wie zuvor: kein Render-Pfad (`precipComposite`, `RainLayer`, `ScalarLayer`, Fusion-Effekte, Sichtbarkeits-Booleans) geändert; `precipFrameReady` delegiert byte-genau (Harness). `flownowcast`/`poprob`/Model-Switcher/Fusion⇄Native/Regenradar/`radarModel.ts` unberührt; `git diff -- src/fusion/` leer. Einzige gewollte Wegnahme: der separate SIM-Radar-Layer (Jans Auftrag „ersetzt durch gemeinsame Ansicht").
2. **Desktop unverändert (außer gewollt)?** Ja — nur die gewollten Änderungen: SIM-Radar-Toggle/-Legende/-Deck weg, Label „Niederschlag · jetzt–12 h", Slider volle Spanne. Sonst identische Karte (MCP-Snapshot).
3. **Touch-Targets ≥ 44×44 px?** Ja — Niederschlag-Toggle im Mobile-„Layer"-Sheet gemessen 358×56 px.
4. **Konsole frei von neuen Errors/Warnings?** Ja — MCP `list_console_messages` (error/warn) Desktop und Mobile: leer.
5. **Keine Long Tasks > 200 ms?** Kein neuer Rechenpfad — die Zentralisierung ist reine O(1)-Entscheidungslogik (drei Land-Resolves pro Sichtbarkeits-Auswertung); Kompositor/Loader unverändert. Slider reagierte flüssig. (Belastbares Long-Task-Trace nur Real-Device, MCP drosselt rAF — bekannte Randbedingung.)

**2026-07-24 · N1-Revision (Jan): „Niederschlag auf 2 h verkürzt" — Modellhälfte raus.** Nach Rückmeldung „wir verkürzen beim niederschlag wieder auf 2h, das ist besser" die Ansicht von „jetzt–12 h (Radar → Modell nahtlos)" auf **rein gemessenes Radar/Nowcast** reduziert: per Land bis zum Nowcast-Horizont (DE ≤2 h RADOLAN-RV · AT ≤3 h INCA · CH <0,5 h rzc); die Modell-/Fusionshälfte (2–12 h, ICON-D2 UND Fusions-`precip-forecast`) ist **draußen** — kürzer & ehrlicher. **Delta:** `precipSource.ts` jetzt radar-only (`resolvePrecipSource→{kind:'radar',ready}`, jenseits Horizont `ready:false`; kein `seamBlend`/`hold`/`modelHorizonH`; neu `precipRadarHorizonHours`), Harness auf 30/30 angepasst. `MapView`: Kompositor OHNE `d2`; `NOWCAST_LAYER_ID` einzige Precip-Quelle (auch Fusion-Modus), `precip-forecast` fest `false`; `sliderMax` nutzt für Niederschlag `precipRadarHorizonHours` (≤3 h) statt des 12-h-ICON-D2-Horizonts (`cloudsHorizonHours` für Wolken abgespalten); meine vorige N1-4-Slider/Loader-Verlängerung (`nowcastActiveRef`, `installIconD2`-nowOnly) zurückgenommen; Status-Caption ohne „+ ICON-D2". Label/Tooltip/Info-Panel/Deck-Sub → „Niederschlag · jetzt–2 h". `iconD2Ref`/`installIconD2` bleiben (speisen weiter die `confidence`-PoP-Heuristik für AT/CH), werden aber nicht mehr gerendert. SIM-Radar-Stilllegung unverändert. **Verify (MCP Dev :5204):** Harness 30/30, typecheck grün, Konsole leer; Testmodus mit aktivem Niederschlag → Slider `valuemax="3"` (AT INCA), Datenlage „DACH-KOMPOSIT · DE RADOLAN · AT INCA · CH RZC" ohne ICON-D2, Toggle-Beschreibung „…keine Modell-Verlängerung"; kein Sim-Radar. `git diff -- src/fusion/` leer. Doku (`docs/niederschlag-architektur.md`, Spec §11, tests V-NIEDERSCHLAG, checklist GN1) auf 2 h nachgezogen. Offen für Jan: der Slider endet bei aktivem Niederschlag am MAX geladenen Radar-Horizont (3 h wegen AT INCA), nicht hart bei 2 h — bewusst so (Jan wählte „per-Land DE2/AT3/CH0,5", nicht „pauschal hart 2 h"). Uncommitted.
